---
phase: 05-outer-tier-body-findability-galaxy-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Scripts/TierClassifier.cs
  - EcoSpace.Tests/TierClassifierTests.cs
  - Scripts/Render/SkyboxRenderer.cs
  - Shaders/skybox.gdshader
autonomous: false
requirements: [RND-02, RND-07]
must_haves:
  truths:
    - "In Universe space the destination galaxies are visible on the skybox (they no longer vanish)"
    - "The home galaxy stays suppressed while the ship is inside its SOI"
    - "Galaxy discs render with believable 3D tilt (foreshortening) without collapsing into a sky-spanning ring or ghosting at the antipode"
    - "The retired skybox star-point loop no longer draws stars (stars come from the StarPointRenderer instead)"
  artifacts:
    - path: "Scripts/TierClassifier.cs"
      provides: "Galaxy-unconditional sky routing (D-49)"
      contains: "ObjectType == UniObject.Type.Galaxy"
    - path: "EcoSpace.Tests/TierClassifierTests.cs"
      provides: "Galaxy-in-current-tier classification coverage"
      contains: "Galaxy"
    - path: "Shaders/skybox.gdshader"
      provides: "Reworked galaxy loop with D-59 safe-basis tilt + D-54 volumetric look; star loop removed (D-56)"
      contains: "galaxy_disc_coords_tilted"
  key_links:
    - from: "Scripts/TierClassifier.cs"
      to: "Scripts/Render/SkyboxRenderer.cs"
      via: "Classify(body,ship)==NextTierSkybox for galaxies → SkyboxRenderer pushes them with no filter change"
      pattern: "TierClassifier\\.Classify"
    - from: "Scripts/Render/SkyboxRenderer.cs"
      to: "Shaders/skybox.gdshader"
      via: "galaxy_orientations[i].xyz (disc_normal) already pushed → consumed by tilt function"
      pattern: "galaxy_orientations"
---

<objective>
Make galaxies visible in Universe space (D-48/D-49) and reinstate galaxy disc tilt (D-59) with
the Star-Nest-influenced volumetric look (D-54), while retiring the skybox star-point loop (D-56)
now that stars come from the StarPointRenderer (05-01). Galaxies stay sky-only forever (D-28
REAFFIRMED as D-48) — the fix is routing `Type==Galaxy` to the skybox unconditionally so
current-tier galaxies in Universe space draw, not just next-tier-out ones.

Purpose: Closes the P2 `galaxy-visibility-in-universe-space` gap and the `galaxy-disc-tilt`
polish item; records the D-28→D-48 decision.
Output: TierClassifier Galaxy guard + tests; SkyboxRenderer star-push removal; skybox.gdshader
galaxy rework (tilt + volumetric overlay) with the star loop removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-outer-tier-body-findability-galaxy-visibility/05-CONTEXT.md
@.planning/phases/05-outer-tier-body-findability-galaxy-visibility/05-RESEARCH.md
@.planning/phases/05-outer-tier-body-findability-galaxy-visibility/05-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift/source-grounding verification):
- New TierClassifier branch: the `body.ObjectType == UniObject.Type.Galaxy → NextTierSkybox` guard (D-49)
- New skybox.gdshader function: `galaxy_disc_coords_tilted(eye_dir, galaxy_dir, disc_normal)`
- New skybox.gdshader constants: `TILT_FLOOR`, `detail_blend` (Claude's Discretion values)
- New/augmented TierClassifierTests cases covering Galaxy-in-current-tier
REMOVED symbols (now dead): skybox.gdshader `star_dirs`/`star_colors`/`star_sizes`/`star_count`
uniforms + the star loop; SkyboxRenderer `_dirs`/`_colors`/`_sizes` fields + their star push.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: star_dirs -->
<!-- planner-discipline-allow: star_count -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Type==Galaxy to the skybox unconditionally (D-49) + tests</name>
  <read_first>
    - Scripts/TierClassifier.cs (lines 21 SkyTier enum; 54-73 Classify — the Root check at 58, the CurrentSpace match at 62, the upward walk at 70, Beyond at 73; line 2 "Pure C# — no Godot dependency" rule)
    - Scripts/UniObject.cs (line 106 `ObjectType = Type.None`; Type enum members incl. Galaxy — confirm exact member name)
    - EcoSpace.Tests/TierClassifierTests.cs (existing 16 cases — copy the test-construction idiom for building UniObject ship/body pairs; do NOT depend on Godot)
    - 05-RESEARCH.md lines 266-278 (Pitfall 5 — Galaxy misclassified as CurrentTierMesh in Universe space), 404-427 (the exact TierClassifier change + "unit test update required")
    - 05-PATTERNS.md lines 220-249 (insert guard AFTER Root check, BEFORE CurrentSpace match; keep pure C#)
  </read_first>
  <files>Scripts/TierClassifier.cs, EcoSpace.Tests/TierClassifierTests.cs</files>
  <behavior>
    - ship in Universe space, body = Galaxy in Universe space → NextTierSkybox (was CurrentTierMesh — the bug)
    - ship in Galaxy space, body = Galaxy (next tier out) → NextTierSkybox (unchanged behavior, now via the new guard)
    - ship in Star space, body = Galaxy → NextTierSkybox
    - body = Galaxy with CurrentSpace == Root → Skip (Root check still wins; guard is placed AFTER it)
    - existing non-Galaxy cases (Star/Planet CurrentTierMesh / NextTierSkybox / Beyond / Skip) → all 16 unchanged
  </behavior>
  <action>Insert a D-49 guard in `Classify` immediately AFTER the `body.CurrentSpace == Space.Root → Skip` check (line 58) and BEFORE the `body.CurrentSpace == ship.CurrentSpace → CurrentTierMesh` check (line 62): `if (body.ObjectType == UniObject.Type.Galaxy) return SkyTier.NextTierSkybox;` with a comment citing D-49 and the Pitfall-5 reason (prevents Galaxy-in-Universe-space returning CurrentTierMesh, which WorldRenderer skips, leaving nothing drawn). Confirm the exact Type member name from UniObject.cs (Galaxy). Keep TierClassifier pure C# — no Godot dependency (line 2 rule); `UniObject.Type.Galaxy` is global-namespace, allowed. Then augment TierClassifierTests.cs with the Galaxy-in-current-tier cases from the behavior block (ship Universe + Galaxy Universe → NextTierSkybox is the critical regression-guard), following the existing test-construction idiom. Do NOT change the non-Galaxy assertions.</action>
  <verify>
    <automated>dotnet test EcoSpace.Tests/EcoSpace.Tests.csproj --filter "FullyQualifiedName~TierClassifier"</automated>
    grep -c 'UniObject.Type.Galaxy' Scripts/TierClassifier.cs  # expect >= 1
  </verify>
  <acceptance_criteria>
    - TierClassifier returns `NextTierSkybox` for every `ObjectType == Type.Galaxy` body regardless of the ship's tier (including Universe-space same-tier), except when the Root check already returned Skip.
    - All prior 16 non-Galaxy test cases still pass; new Galaxy-in-current-tier case(s) pass (`dotnet test` green).
    - TierClassifier.cs remains free of any `using Godot;` / Godot-type dependency (pure C#, line-2 rule).
    - Satisfies: D-49 (galaxies always sky entities), D-48 (current-tier galaxies reach the skybox), Pitfall 5.
  </acceptance_criteria>
  <done>Galaxy bodies classify to NextTierSkybox at every tier; tests green; classifier stays pure C#.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the SkyboxRenderer star push (D-56) — galaxies untouched</name>
  <read_first>
    - Scripts/Render/SkyboxRenderer.cs (lines 8 read-only contract; 158 the `Classify != NextTierSkybox` filter — REQUIRES NO CHANGE per D-49 fix; 168-179 SyncSkyPoints LCA position; 208-209 home-galaxy suppression guard — PRESERVE; 239-245 star push to REMOVE; 248-257 galaxy uniform push incl. galaxy_orientations — PRESERVE; the `_dirs`/`_colors`/`_sizes` field declarations to remove)
    - 05-RESEARCH.md lines 429-436 (no SkyboxRenderer code change needed beyond removing dead star push), 606-609 (deprecated star arrays), 637-639 (open question 3 — no test impact)
    - 05-PATTERNS.md lines 175-216 (classify filter unchanged; star push to remove; home-galaxy suppression to preserve; galaxy uniform push unchanged)
  </read_first>
  <files>Scripts/Render/SkyboxRenderer.cs</files>
  <action>Remove the now-dead star-point push: delete the `star_count`/`star_dirs`/`star_colors`/`star_sizes` `SetShaderParameter` block (lines ~239-245) and the corresponding `_dirs`/`_colors`/`_sizes` array fields and any per-star fill loop that populated them (the star points are now drawn by StarPointRenderer from 05-01, D-56). Do NOT touch: the `if (TierClassifier.Classify(body, ship) != SkyTier.NextTierSkybox) continue;` filter (it now correctly admits current-tier galaxies via the 05-02 Task-1 fix — no change needed), the home-galaxy suppression guard `if (UniMath.FindLca(ship, body, objs) == body.Index) continue;` (PRESERVE — keeps the home galaxy hidden while inside it, including in Universe space), the galaxy uniform push block (`galaxy_count`/`galaxy_dirs`/`galaxy_colors`/`galaxy_sizes`/`galaxy_types`/`galaxy_orientations` — PRESERVE; galaxy_orientations.xyz is the disc_normal consumed by Task 4's tilt). Preserve the read-only-consumer contract (line 8). If the SkyboxRenderer accumulates galaxies and stars in one pass, keep the galaxy accumulation and drop only the star accumulation.</action>
  <verify>
    <automated>dotnet build EcoSpace.csproj -c Debug</automated>
    grep -c 'star_dirs' Scripts/Render/SkyboxRenderer.cs  # expect 0 (star push fully removed)
  </verify>
  <acceptance_criteria>
    - SkyboxRenderer no longer pushes `star_dirs`/`star_colors`/`star_sizes`/`star_count`; the `_dirs`/`_colors`/`_sizes` fields are gone (`grep -c 'star_dirs' Scripts/Render/SkyboxRenderer.cs` == 0).
    - The galaxy uniform push (incl. `galaxy_orientations`) and the home-galaxy suppression guard are intact and unchanged.
    - The `Classify != NextTierSkybox` filter is unchanged (current-tier galaxies admitted via the Task-1 classifier fix, not a filter edit).
    - `dotnet build` succeeds; read-only contract preserved (no UniVec3 mutation / TranslatePos).
    - Satisfies: D-56 (skybox star loop retired on the C# side), D-48/D-49 (galaxy push path preserved).
  </acceptance_criteria>
  <done>Dead star push removed; galaxy push + home-galaxy suppression preserved; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the skybox.gdshader star loop + star uniforms (D-56)</name>
  <read_first>
    - Shaders/skybox.gdshader (lines 1-29 header; 36-49 star_dirs/star_colors/star_sizes/star_count uniform declarations — REMOVE; 135-145 the `for (int i=0; i<star_count; i++)` star loop — REMOVE; galaxy loop + galaxy uniforms — KEEP, reworked in Task 4)
    - 05-PATTERNS.md lines 336-349 (star loop + star uniforms to remove)
    - 05-RESEARCH.md lines 606-609 (deprecated star loop/uniforms)
  </read_first>
  <files>Shaders/skybox.gdshader</files>
  <action>Delete the star-point loop (`for (int i = 0; i < star_count; i++) { ... col += star_colors[i]... }`, ~lines 135-145) and the now-unused star uniform declarations `star_dirs[MAX_STARS]`, `star_colors[MAX_STARS]`, `star_sizes[MAX_STARS]`, `star_count`, and `MAX_STARS` if it is no longer referenced by anything (check the galaxy path does not reuse it). Keep ALL galaxy machinery (galaxy_dirs/galaxy_colors/galaxy_sizes/galaxy_types/galaxy_orientations, MAX_GALAXIES, the galaxy loop, galaxy_disc_coords, spiral_galaxy/elliptical_galaxy, the `d > 0.0` antipode gate) — those are reworked in Task 4, not removed. The header comment's star-specific notes (e.g. the kelvinvanhoorn star_sizes reference) can be trimmed but is not required. Do NOT echo the removed identifier names as literal "what NOT to do" comments — just remove them.</action>
  <verify>
    <automated>MISSING — GDShader is GPU-compiled; proof is Godot loading skybox.gdshader with no compile error (Task-4 in-game check). Grep gate below is static proof.</automated>
    grep -vE '^\s*//' Shaders/skybox.gdshader | grep -c 'star_count'  # expect 0 (star loop + uniform gone, comments excluded)
  </verify>
  <acceptance_criteria>
    - The skybox star loop and `star_dirs`/`star_colors`/`star_sizes`/`star_count` uniform declarations are removed (`grep -vE '^\s*//' Shaders/skybox.gdshader | grep -c 'star_count'` == 0).
    - All galaxy uniforms, the galaxy loop, galaxy_disc_coords, and the `d > 0.0` gate remain present (verified by Task 4 reading them).
    - skybox.gdshader still compiles in Godot (no orphaned reference to a removed uniform) — observable in-game (Task 4).
    - Satisfies: D-56 (skybox star loop retired on the shader side; galaxies stay).
  </acceptance_criteria>
  <done>Star loop + star uniforms removed from skybox.gdshader; galaxy path intact; shader compiles.</done>
</task>

<task type="auto">
  <name>Task 4: Rework galaxy loop — D-59 safe-basis tilt + D-54 volumetric overlay</name>
  <read_first>
    - Shaders/skybox.gdshader (lines 103-110 galaxy_disc_coords stable basis — the function to extend/replace; 161 the `} else if (d > 0.0) {` antipode gate — PRESERVE; 167-177 galaxy loop disc_bright computation incl. galaxy_orientations[i].w seed and galaxy_types branch)
    - 05-RESEARCH.md lines 249-264 (Pitfall 4 — ring collapse landmine, the MANDATORY safe approach), 301-306 (Pitfall 7 antipode), 467-514 (Star-Nest volumetric look — keep existing functions, add overlay, license note), 518-565 (the exact D-59 safe-basis tilt math + TILT_FLOOR), 618-620 (A3/A5 — iteration cost + look are play-test items)
    - 05-PATTERNS.md lines 351-399 (extend galaxy_disc_coords → galaxy_disc_coords_tilted; preserve d>0.0 gate; add Star-Nest overlay; disc_normal = galaxy_orientations[i].xyz already pushed)
    - 05-CONTEXT.md D-59 (MANDATORY safe approach; ring-collapse landmine, commit fef1d91), D-54 (shared look, NO true nebula), D-58 (build first, tune in play-test)
    - .planning/debug/galaxy-sky-disc-antipode.md (root-cause of the ring collapse + fef1d91 fix)
  </read_first>
  <files>Shaders/skybox.gdshader</files>
  <action>Add `galaxy_disc_coords_tilted(vec3 eye_dir, vec3 galaxy_dir, vec3 disc_normal)`: build the stable face-on tangent basis FROM `galaxy_dir` ONLY (`up0 = abs(galaxy_dir.y) < 0.99 ? (0,1,0) : (1,0,0); t1 = normalize(cross(up0, galaxy_dir)); t2 = cross(galaxy_dir, t1)`) — NEVER build the basis from `disc_normal` (that is the fef1d91 ring-collapse landmine, D-59). Compute face-on uv = (dot(delta,t1), dot(delta,t2)). Add tilt by foreshortening: `tilt_factor = abs(dot(disc_normal, galaxy_dir))`; `safe_tilt = clamp(tilt_factor, TILT_FLOOR, 1.0)` with a new `const float TILT_FLOOR` (~0.15, Claude's Discretion) whose value MUST stay > 0 so the compression never divides toward collapse; project uv onto the in-plane disc_normal direction and compress the minor axis by `safe_tilt` per the RESEARCH D-59 math (lines 532-560). In the galaxy loop, call the tilted variant passing `galaxy_orientations[i].xyz` as `disc_normal` (already pushed by SkyboxRenderer — no C# change). PRESERVE the `d > 0.0` front-hemisphere gate exactly (Pitfall 7 antipode ghost). Then add the D-54 Star-Nest volumetric overlay AFTER `disc_bright` is computed: a low-iteration (4) fractal-fold layer `p = abs(p)/dot(p,p) - 0.53` accumulating brightness, clamped [0,1], blended in via a new `const float detail_blend` (Claude's Discretion) — this is "inspired by", a few lines of well-known folding math, NOT a verbatim Star-Nest copy (license note RESEARCH line 514); keep it cheap (A3) and respect D-54's NO-true-nebula boundary. Per D-58/D-52, exact TILT_FLOOR/detail_blend/iteration counts are in-game tuning — get believable tilt + a working overlay first; do not pre-design a fade curve.</action>
  <verify>
    <automated>MISSING — GDShader visual/GPU; proof is the in-game checkpoint. Grep gate below is static proof.</automated>
    grep -c 'galaxy_disc_coords_tilted' Shaders/skybox.gdshader  # expect >= 1 (function defined + called)
  </verify>
  <acceptance_criteria>
    - `galaxy_disc_coords_tilted` exists, builds its basis from `galaxy_dir` (NOT `disc_normal`), and clamps tilt by `TILT_FLOOR > 0` (D-59 mandatory safe approach — no ring collapse when disc_normal ≈ galaxy_dir ≈ world-up).
    - The galaxy loop passes `galaxy_orientations[i].xyz` as disc_normal and preserves the `d > 0.0` gate (no antipode ghost, Pitfall 7).
    - A low-iteration Star-Nest fractal overlay is blended into `disc_bright` (D-54 shared look, no true nebula).
    - In-game (checkpoint): galaxy discs show believable 3D tilt, never collapse to a sky-spanning ring/band, and never ghost at the antipode.
    - Satisfies: D-59 (safe-basis tilt), D-54 (volumetric look unified with stars), D-58 (tuning deferred to play-test).
  </acceptance_criteria>
  <done>Tilted galaxy disc coords + volumetric overlay in place; basis from galaxy_dir; TILT_FLOOR clamp; antipode gate preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Play-test — galaxies visible in Universe space + tilted discs (no ring/ghost)</name>
  <action>Blocking human play-test (no code change). Run Main.tscn and follow how-to-verify; tune skybox.gdshader TILT_FLOOR/detail_blend by feel (D-58) and record the D-28→D-48 decision + the D-52 home-galaxy-exit-pop watch item in the SUMMARY.</action>
  <what-built>Galaxies now reach the skybox in Universe space (D-48/D-49 via the TierClassifier
  Galaxy-unconditional guard + preserved SkyboxRenderer galaxy push), the skybox star loop is
  retired (D-56 — stars come from StarPointRenderer), and the galaxy disc shader has safe-basis
  3D tilt (D-59) plus a Star-Nest volumetric overlay (D-54).</what-built>
  <how-to-verify>
    Run Main.tscn (F5) in Godot. Then:
    1. Fly out to Universe space. EXPECTED: the destination galaxies are visible on the skybox
       (they no longer vanish). This is the P2 fix — re-runs Phase-03 UAT around intergalactic
       visibility. The home galaxy should be SUPPRESSED while you are still inside its SOI, and
       appear once you exit it (D-48/D-52 watch item: does its disc pop on the Galaxy→Universe
       crossing? Report — tuning is deferred per D-52, fix only if it visibly pops).
    2. Look at a galaxy disc off-axis. EXPECTED: believable 3D tilt/foreshortening — an ellipse,
       not a flat face-on circle, and NOT a sky-spanning band/ring. CRITICAL: confirm no galaxy
       collapses to a ring and no galaxy appears twice (ghost 180° away). (D-59 landmine.)
    3. Confirm the star field still looks right (stars now drawn by StarPointRenderer, not the
       skybox) — no missing stars, no double-drawn stars.
    Tuning (skybox.gdshader constants): TILT_FLOOR (raise if a near-edge-on disc still collapses;
    lower for more dramatic tilt), detail_blend (Star-Nest overlay strength). Adjust by feel (D-58).
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with tuned TILT_FLOOR/detail_blend values), or describe what looks wrong (galaxy missing in Universe space, ring collapse, antipode ghost, home-galaxy pop).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | Single-player, offline, local-only Godot desktop game. No network, no auth, no persisted user data, no remote input beyond local keyboard/mouse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | TILT_FLOOR clamp in the galaxy shader | mitigate | `clamp(tilt_factor, TILT_FLOOR, 1.0)` with `TILT_FLOOR > 0` prevents a divide-toward-collapse in the foreshorten math (stability, not security). Enforced as a shader const > 0. |
| T-05-04 | Tampering | Authored galaxy disc_normal / shader constants | accept | Dev-time authored test data; worst case is a render glitch (handled by the TILT_FLOOR clamp + d>0.0 gate), not a security event. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed (Star-Nest is a technique reference; the folding formula is well-known math). Gate not applicable. |

No high-severity threats. Rendering-only change in an offline single-player game.
</threat_model>

<verification>
- `dotnet test ...~TierClassifier` → green incl. new Galaxy-in-current-tier cases (Task 1).
- `dotnet build EcoSpace.csproj` → 0 errors (Task 2).
- In-game play-test: galaxies visible in Universe space; home galaxy suppressed while inside; tilted discs with no ring collapse / no antipode ghost; star field unchanged.
- Records the D-28 → D-48 decision (galaxies sky-only, REAFFIRMED) in the SUMMARY.
</verification>

<success_criteria>
- ROADMAP Success Criterion 2 (partial): destination galaxies visible in Universe space with disc-growth + HUD-distance approach cues (the marker half lands in 05-03).
- ROADMAP Success Criterion 4 (partial): D-28 revisited and recorded as D-48 (galaxies sky-only, reaffirmed).
- D-48/D-49/D-54/D-56/D-59 implemented; ring-collapse landmine avoided (basis from galaxy_dir + TILT_FLOOR clamp + d>0.0 gate).
</success_criteria>

<output>
Create `.planning/phases/05-outer-tier-body-findability-galaxy-visibility/05-02-SUMMARY.md` when done.
Record: the D-28→D-48 decision (galaxies sky-only, reaffirmed); tuned TILT_FLOOR/detail_blend values;
the D-52 home-galaxy-exit-pop watch-item observation; A3/A5 (Star-Nest cost/look) outcome.
</output>
