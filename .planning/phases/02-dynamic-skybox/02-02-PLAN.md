---
phase: 02-dynamic-skybox
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Scripts/Render/SkyboxRenderer.cs
  - Scripts/Render/WorldRenderer.cs
files_modified_note: "Plan 03 runs in the same wave but touches only EcoSpace.Tests/* — zero overlap with this plan."
autonomous: false
requirements: [RND-05, RND-07]
must_haves:
  truths:
    - "Sky points are magnitude-ranked: a high-luminosity near sibling is visibly brighter/larger than a dim or distant one (D-17)"
    - "Every authored next-tier body is always at least one lit dithered pixel — no real body is ever invisible (D-19 minimum-brightness floor)"
    - "Each sky point carries its body's authored BaseColor (D-18)"
    - "Bright sky points bloom through the existing WorldEnvironment glow, like the star mesh does (D-20)"
    - "SkyboxRenderer can report, for any body it tracks, the cached world-fixed sky direction; WorldRenderer can report a tracked body's render-space position — the two data sources for an instant exact-match point<->mesh swap (RND-07/D-21 baseline)"
  artifacts:
    - path: "Scripts/Render/SkyboxRenderer.cs"
      provides: "Magnitude model + min-floor + bloom-feeding brightness + cached sky directions for handoff"
      contains: "LuminosityScale"
    - path: "Scripts/Render/WorldRenderer.cs"
      provides: "Public accessor for a tracked body's last render-space position (handoff baseline)"
      contains: "GetRenderPosition"
  key_links:
    - from: "Scripts/Render/SkyboxRenderer.cs"
      to: "UniObject.Luminosity"
      via: "inverse-square magnitude model raw = Luminosity / dist^2"
      pattern: "Luminosity"
    - from: "Scripts/Render/SkyboxRenderer.cs"
      to: "Shaders/skybox.gdshader"
      via: "star_colors.a = apparent brightness (>1 feeds glow), star_sizes = magnitude-ranked disc"
      pattern: "SetShaderParameter"
    - from: "Scripts/Render/SkyboxRenderer.cs"
      to: "Scripts/Render/WorldRenderer.cs"
      via: "handoff alignment reads WorldRenderer.GetRenderPosition for a body currently a mesh"
      pattern: "GetRenderPosition"
---

<objective>
Refine the Plan 01 slice from uniform placeholder dots into the real RND-05 magnitude-ranked sky and build the RND-07 handoff baseline machinery (D-21 instant exact-match, no crossfade). This plan replaces the placeholder brightness/size with the inverse-square luminosity-times-distance model (D-17), applies the minimum-brightness floor (D-19), carries each body's BaseColor (D-18), feeds the shared WorldEnvironment glow (D-20), and exposes the paired data sources (cached sky direction + WorldRenderer render position) that a future visible promotion/demotion (Phase 3) needs to align a point and its mesh on the same screen pixel with matched color and brightness.

Purpose: Turn the proven projection into the honest, Elite/Frontier magnitude-ranked sky and lay the exact-match handoff groundwork without yet performing a visible swap (the visible Star<->Galaxy promotion is Phase 3 per D-24).

Output: After this plan, the sibling stars are ranked by real brightness with a guaranteed-visible floor and bloom, and the codebase exposes the two aligned data structures (sky direction cache + mesh render-position accessor) proving the handoff is buildable.
</objective>

## Phase Goal

**As a** pilot, **I want** the distant stars to differ in brightness by their true luminosity and distance yet never vanish entirely, **so that** the sky reads as a real, navigable cosmos where the brightest neighbours stand out.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dynamic-skybox/02-CONTEXT.md
@.planning/phases/02-dynamic-skybox/02-RESEARCH.md
@.planning/phases/02-dynamic-skybox/02-PATTERNS.md
@.planning/phases/02-dynamic-skybox/02-01-SUMMARY.md
</context>

## Artifacts this phase produces (introduced by this plan)

- **`SkyboxRenderer` magnitude exports** — new `[Export]` properties: `float LuminosityScale`, `float MinBrightFloor`, `float SizePerBright`, `float MinStarSize`, `float MaxStarSize` (D-17/D-19 tuning knobs).
- **`SkyboxRenderer` handoff cache** — new `Dictionary<int, Vector3>` of last-computed world-fixed sky directions keyed by body index, plus a public accessor to read it.
- **`WorldRenderer.GetRenderPosition(int bodyIdx)`** — new public accessor returning the last render-space `Vector3` for a tracked mesh body (RND-07 baseline; consumes the existing per-frame `renderPositions` data).

<tasks>

<task type="auto">
  <name>Task 1: Implement the magnitude model (D-17/D-18/D-19) and bloom feed (D-20)</name>
  <read_first>
    - Scripts/Render/SkyboxRenderer.cs (full — the Plan 01 SyncSkyPoints loop and placeholder brightness/size to replace)
    - .planning/phases/02-dynamic-skybox/02-01-SUMMARY.md (the authored sibling luminosities/distances + visible-from space)
    - .planning/phases/02-dynamic-skybox/02-RESEARCH.md (Pattern 3 magnitude formula + starting values, Code Examples: SkyboxRenderer per-frame push, Pitfall 7 zero-luminosity default)
    - .planning/phases/02-dynamic-skybox/02-PATTERNS.md ("Export Properties with Backing Fields" + "ShaderMaterial Parameter Push")
    - Shaders/skybox.gdshader (confirm star_colors.a is used as brightness multiplier and COLOR > 1 feeds glow)
    - Main.tscn (confirm glow_enabled = true on the Environment — D-20 reuse path)
  </read_first>
  <action>
    Add the magnitude tuning `[Export]` properties to `SkyboxRenderer` per D-17/D-19: `LuminosityScale` (default ~4e6f so a solar-luminosity star at ~4 ly ≈ 1.0 apparent — tune per the 02-01-SUMMARY authored distances), `MinBrightFloor` (default ~0.1f, D-19), `SizePerBright` (default ~0.002f), `MinStarSize` (default ~0.0003f), `MaxStarSize` (default ~0.005f). Plain `[Export] public float X { get; set; } = default;` auto-properties are sufficient since uniforms re-push every frame.

    In `SyncSkyPoints()`, replace the Plan 01 placeholder brightness/size with the real model: compute the true ship->body distance in metres via the Double3 hierarchy walk already present (reuse `AbsolutePositionInRoot`; keep all distance math in `double`, Pitfall 5). Compute `raw = dist > 0 ? (float)(body.Luminosity / (dist * dist)) : <safe>` then `brightness = Mathf.Max(raw * LuminosityScale, MinBrightFloor)` (D-19 floor guarantees ≥1 visible pixel; also guard `dist == 0` and `Luminosity <= 0` to avoid NaN/div-by-zero). Compute `size = Mathf.Clamp(brightness * SizePerBright, MinStarSize, MaxStarSize)` so brighter = larger disc (magnitude ranking, D-17). Set `_colors[count] = new Color(body.BaseColor.R, body.BaseColor.G, body.BaseColor.B, brightness)` — BaseColor carries the hue (D-18), alpha carries apparent brightness which the shader multiplies into COLOR; values >1 feed the existing WorldEnvironment glow automatically (D-20 — no new glow node, reuse Main.tscn glow). Keep `star_count` clamped to `[0, MaxStars]`. Do NOT add a second dither or glow pass.
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" && dotnet build EcoSpace.sln -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds with 0 errors.
    - `SkyboxRenderer` declares the five magnitude `[Export]` floats (`LuminosityScale`, `MinBrightFloor`, `SizePerBright`, `MinStarSize`, `MaxStarSize`).
    - `SyncSkyPoints` computes `body.Luminosity / (dist*dist)`, applies `Mathf.Max(..., MinBrightFloor)`, and sets the color alpha to the computed brightness (grep: `Luminosity` and `MinBrightFloor` both referenced in SyncSkyPoints).
    - No `TranslatePos` / no `LocalPos`/`ChildIndices` write added (read-only rule preserved).
  </acceptance_criteria>
  <done>Sky points are magnitude-ranked with a guaranteed minimum floor, carry BaseColor, and feed the existing bloom — the build is clean and the renderer stays read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Build the RND-07 handoff baseline data sources (D-21) — sky direction cache + WorldRenderer render-position accessor</name>
  <read_first>
    - Scripts/Render/WorldRenderer.cs (full — the per-frame `renderPositions` Dictionary built in SyncBodies lines 193-258; add a public accessor backed by a persisted copy)
    - Scripts/Render/SkyboxRenderer.cs (full — SyncSkyPoints; add a per-body direction cache)
    - .planning/phases/02-dynamic-skybox/02-RESEARCH.md (Pattern 5 Handoff Machinery — exactly what Phase 2 builds vs defers; color/brightness match by construction)
    - .planning/phases/02-dynamic-skybox/02-CONTEXT.md (D-21 instant exact-match no-crossfade; D-22 swap on scale crossing only; D-24 Phase 2 builds machinery, Phase 3 does the visible swap)
  </read_first>
  <action>
    In `WorldRenderer`, persist the per-frame `renderPositions` so it survives past `SyncBodies` (store into a private `Dictionary<int, Vector3> _lastRenderPositions` field, replacing/clearing each frame) and add `public bool GetRenderPosition(int bodyIdx, out Vector3 pos)` that returns the last tracked render-space position for a body currently rendered as a mesh (false if not tracked). This is the mesh-side data source for an exact-match swap — it reuses existing math, adds no new rendering, and does NOT change WorldRenderer's read-only behavior.

    In `SkyboxRenderer`, cache each next-tier body's last computed world-fixed direction in a private `Dictionary<int, Vector3> _skyDirs` (keyed by body Index, updated in SyncSkyPoints) and add a `public bool GetSkyDirection(int bodyIdx, out Vector3 dir)` accessor. Document (class/method summary) that these two accessors are the RND-07/D-21 baseline: at a future tier crossing (Phase 3, D-24) the promotion logic compares a body's sky direction against its mesh render position to align them on one screen pixel for a pop-free instant swap — Phase 2 builds ONLY the data structures, performs NO visible swap and NO crossfade (D-21). Color match is automatic because both renderers read `body.BaseColor` (D-18); brightness match is automatic because both derive from `body.Luminosity`. Do not add timers, alpha blends, or crossfades.
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" && dotnet build EcoSpace.sln -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds with 0 errors.
    - `Scripts/Render/WorldRenderer.cs` declares a public `GetRenderPosition` accessor returning a tracked body's render-space `Vector3`.
    - `Scripts/Render/SkyboxRenderer.cs` declares a `_skyDirs` cache and a public `GetSkyDirection` accessor.
    - No crossfade/alpha-blend/Tween/timer added (grep `Tween` and `crossfade` in both files returns 0) — D-21 instant exact-match only.
    - Neither file adds a `TranslatePos` call or a `LocalPos`/`ChildIndices` write.
  </acceptance_criteria>
  <done>The two aligned handoff data sources (sky direction cache + mesh render-position accessor) exist and build cleanly, proving the RND-07 exact-match swap is constructible without performing the visible Phase 3 swap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The sky is now magnitude-ranked (D-17): brightness = real Luminosity / distance^2 scaled, clamped up by the minimum-brightness floor (D-19), carrying each body's BaseColor (D-18), with bright points blooming through the existing WorldEnvironment glow (D-20). The RND-07 handoff baseline data sources are in place (cached sky directions + WorldRenderer render-position accessor) but no visible swap is performed (that is Phase 3, D-24).
  </what-built>
  <how-to-verify>
    1. Launch the scene and fly to the space where the sibling stars are visible (per 02-01-SUMMARY). Confirm the points now differ in brightness/size: the high-luminosity sibling (e.g. Sirius-analog) is clearly the brightest/largest and the dim red-dwarf analog is the faintest — magnitude ranking is visible (D-17).
    2. Confirm EVERY authored sibling is visible as at least one lit pixel — none is fully black/missing (D-19 floor). If a dim one disappears, lower `MinBrightFloor` is the wrong direction — report it; the floor should keep it visible.
    3. Confirm each point shows its authored hue (warm-white, red, blue-white) — not a uniform white (D-18).
    4. Confirm the brightest point produces a visible glow halo (bloom) consistent with how the home star mesh blooms (D-20).
    5. Tune (optional): adjust `LuminosityScale` / `MinBrightFloor` / `SizePerBright` in the SkyboxRenderer inspector and confirm the sky responds live, then report the values that look right so they can be recorded as defaults.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting good tuning values), or describe what was wrong (no ranking, a body invisible, wrong hues, no bloom).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same as Plan 01: single-player, offline rendering subsystem fed by hand-authored in-process data. No network, auth, input parsing, persistence, or external trust boundary. The only safety concern is engine-correctness in the magnitude math and the handoff data structures.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (engine-correctness) | magnitude model `Luminosity / dist^2` | mitigate | Guard `dist == 0` and `Luminosity <= 0` before division; `MinBrightFloor` clamps the result; `size` clamped to `[MinStarSize, MaxStarSize]` so no NaN/Inf reaches the shader uniforms |
| T-02-05 | Tampering (engine-correctness) | `_skyDirs` / `_lastRenderPositions` dictionaries | mitigate | Keyed by validated body Index; accessors return `false` on miss rather than throwing; cleared/rebuilt per frame so stale entries cannot accumulate |
| T-02-06 | Information disclosure | n/a | accept | No PII, no secrets, no network — offline rendering only |
</threat_model>

<verification>
- `dotnet build EcoSpace.sln` returns 0 errors after each task.
- SkyboxRenderer and WorldRenderer remain read-only (no TranslatePos, no LocalPos/ChildIndices writes).
- No crossfade/Tween/alpha-blend in the handoff path (D-21).
- Human-verify checkpoint confirms magnitude ranking, the visibility floor, BaseColor hues, and bloom.
</verification>

<success_criteria>
- Magnitude-ranked sky points with a guaranteed minimum-brightness floor (RND-05 D-17/D-19).
- Points carry BaseColor (D-18) and bloom via the shared glow (D-20).
- RND-07 baseline machinery built: aligned sky-direction cache + mesh render-position accessor, color/brightness matched by construction, no visible swap, no crossfade (D-21/D-24).
- Build clean; both renderers read-only.
</success_criteria>

<output>
Create `.planning/phases/02-dynamic-skybox/02-02-SUMMARY.md` when done. Record the final magnitude tuning defaults (LuminosityScale/MinBrightFloor/SizePerBright/min-max sizes) and confirm the handoff accessors compile and are ready for the Phase 3 visible swap.
</output>
