---
phase: 06-targeting-system
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Scripts/Hud/TargetSelectorPanel.cs
  - Main.tscn
autonomous: false
requirements: [D-54, D-55, D-56]
must_haves:
  truths:
    - "Pressing Tab (toggle_target_panel) opens/closes a compact side list panel"
    - "The panel lists every targetable body grouped under GALAXY / STAR / PLANET headers with name + distance"
    - "The currently active target is visually marked in the list"
    - "Selecting an entry sets the active target via Hud.SetTargetIndex (panel never touches sim state)"
    - "Opening the panel frees the cursor and closing it restores flight mouse-capture without fighting the T-key mode"
  artifacts:
    - path: "Scripts/Hud/TargetSelectorPanel.cs"
      provides: "Compact tier-grouped selection panel (Control, read-only, phosphor-green)"
      contains: "TargetSelectorPanel"
    - path: "Main.tscn"
      provides: "TargetSelectorPanel node under CanvasLayer wired to Hud + FlightController"
      contains: "TargetSelectorPanel"
  key_links:
    - from: "Scripts/Hud/TargetSelectorPanel.cs"
      to: "Scripts/Hud/Hud.cs"
      via: "panel reads GetTargetCandidates() / ActiveTargetIndex and writes via SetTargetIndex"
      pattern: "SetTargetIndex|GetTargetCandidates"
---

<objective>
Build the cross-space target selector UI (D-54): a compact phosphor-green side panel grouped by
tier (GALAXY/STAR/PLANET) that lists every targetable body with name + distance, marks the active
one, and selects via Hud.SetTargetIndex. The panel replaces the retired Tab cycle (D-56) and is
toggled by the new toggle_target_panel action. It reconciles cursor freeing with the
FlightController T-key Confined/Captured mouse mode.

Purpose: a single discoverable place to pick ANY body in the universe.
Output: new Scripts/Hud/TargetSelectorPanel.cs, modified Main.tscn.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-targeting-system/06-CONTEXT.md
@.planning/phases/06-targeting-system/06-PATTERNS.md
@CLAUDE.md
@Scripts/Hud/Hud.cs
@Scripts/Flight/FlightController.cs
@Scripts/UniObject.cs
@Scripts/Math/UniMath.cs
@Main.tscn
</context>

<interface_context>
Consumes (from 06-01):
- `Hud.GetTargetCandidates()` → IReadOnlyList&lt;int&gt; of GameObjects indices in Galaxy→Star→Planet order.
- `Hud.SetTargetIndex(int candidatePosition)` — candidate-list POSITION (0..count-1), not a raw GameObjects index.
- `Hud.ActiveTargetIndex` → GameObjects index of the active target (to mark the selected row).
- `Hud.FormatDistance(double)` (public static) — distance column formatting.
- `Hud.PhosphorGreen` (public Color [Export]) — match the HUD color.
- Input action `toggle_target_panel` (bound to Tab, from 06-01).

Reads sim state read-only (D-53): UniObject.Name, UniObject.ObjectType, UniObject.CurrentSpace,
and distance via UniMath.Distance(ship, body, gameObjects). NEVER mutate GameObjects / LocalPos.

FlightController mouse mode (Scripts/Flight/FlightController.cs):
- _Ready sets Input.MouseMode = Captured (line ~281).
- _UnhandledInput toggles Captured↔Visible on the T key (lines ~305-322).
- _Input only accumulates steering when MouseMode == Captured (line ~292).
The panel must coordinate with this single global Input.MouseMode (see Task 2 reconciliation rule).
</interface_context>

<artifacts_produced>
NEW symbols this plan introduces (exclude from drift checks):
- Scripts/Hud/TargetSelectorPanel.cs — class TargetSelectorPanel : Control (namespace Hud)
- TargetSelectorPanel [Export]s: HudPath, FlightPath, WorldPath, PhosphorGreen (optional override)
- Main.tscn node: CanvasLayer/TargetSelectorPanel
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement TargetSelectorPanel (tier-grouped list, read-only, phosphor-green)</name>
  <read_first>
    Scripts/Hud/Hud.cs (namespace/header lines 1-27, PhosphorGreen export lines 49-50,
    WorldPath resolution lines 117-135, FormatDistance lines 589-596, SpaceTierName lines 533-541,
    SetTargetIndex/GetTargetCandidates/ActiveTargetIndex from 06-01);
    Scripts/UniObject.cs (Type, Space, Name, ObjectType);
    Scripts/Math/UniMath.cs (Distance);
    06-CONTEXT.md §Specific Ideas (the side-panel mock).
  </read_first>
  <files>Scripts/Hud/TargetSelectorPanel.cs</files>
  <action>
    Create namespace Hud, public partial class TargetSelectorPanel : Control. Carry the read-only
    consumer doc-comment contract verbatim (read-only consumer of GameWorld state — must not mutate
    sim state). Add [Export] NodePath HudPath, FlightPath, WorldPath and resolve them in _Ready with
    the WorldPath→FindChild("Main") fallback pattern from Hud._Ready; resolve the Hud node via
    HudPath (fallback FindChild type search) and the FlightController via FlightPath. Add an
    [Export] Color PhosphorGreen defaulting to new Color(0.1f, 1.0f, 0.3f).

    Build the list using a VBoxContainer of Labels (one Label per tier header, one per body row),
    rebuilt each time the panel is shown and refreshed each _Process while visible so the distance
    column stays live. Group rows under the headers "GALAXY", "STAR", "PLANET" (shortened
    SpaceTierName per the mock); only emit a header when that tier has ≥1 body. For each candidate
    index from Hud.GetTargetCandidates(): read the body via the (uint)idx bounds-check guard, format
    "{Name}  {Hud.FormatDistance(UniMath.Distance(ship, body, gameObjects))}", and mark the row that
    equals Hud.ActiveTargetIndex with a leading marker glyph (the mock uses a left-pointing marker
    next to the active body). Apply PhosphorGreen modulate to every label.

    Selection: support keyboard up/down to move a highlight cursor over body rows and Enter to
    commit (call Hud.SetTargetIndex(candidatePosition) where candidatePosition is the row's index
    into the GetTargetCandidates() list — NOT a GameObjects index). Also allow mouse click on a row
    to select. The panel writes ONLY through Hud.SetTargetIndex; it never sets _targetIndex or any
    GameObjects element directly (D-53). Start hidden (Visible = false) with
    MouseFilter = MouseFilterEnum.Ignore.
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" && dotnet build EcoSpace.csproj -c Debug -v q 2>&1 | grep -v '^#' | grep -Ei 'error|Build succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build succeeds, 0 errors / 0 warnings.
    - Panel rows show name + distance grouped under GALAXY/STAR/PLANET; headers only appear for non-empty tiers.
    - The active target row is visually marked.
    - Selection commits exclusively through Hud.SetTargetIndex; no direct sim-state writes (grep: panel does not reference GameObjects[..]= or .LocalPos =).
    - All distances via UniMath.Distance.
  </acceptance_criteria>
  <done>A read-only tier-grouped selector panel exists and compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Toggle open/close, reconcile cursor with the T-key mouse mode, wire into Main.tscn</name>
  <read_first>
    Scripts/Flight/FlightController.cs (_Ready MouseMode line ~281, _UnhandledInput T-key toggle
    lines ~305-322, _Input capture guard line ~292);
    Main.tscn (CanvasLayer + Hud + DirMarker nodes lines 45-101 and 158-174, FlightController node
    lines 186-191, Camera3D node line 175);
    06-01 project.godot toggle_target_panel action.
  </read_first>
  <files>Scripts/Hud/TargetSelectorPanel.cs, Main.tscn</files>
  <action>
    In TargetSelectorPanel, handle the toggle in _UnhandledInput on
    @event.IsActionPressed("toggle_target_panel"): flip Visible. Reconciliation rule with the
    FlightController T-key Confined/Captured mode (Claude's-discretion in 06-CONTEXT.md, resolved
    here): the panel is the temporary cursor owner. On OPEN: record the current Input.MouseMode,
    then set Input.MouseMode = Visible and MouseFilter = MouseFilterEnum.Stop so rows are clickable
    and steering stops (FlightController._Input already no-ops when not Captured). On CLOSE: set
    MouseFilter = Ignore and restore Input.MouseMode = Captured (the flight default). Mark the
    toggle event handled (GetViewport().SetInputAsHandled()) so the T-key handler and other
    consumers do not double-process it. Because the panel runs its toggle in _UnhandledInput and
    FlightController's T-key is also in _UnhandledInput, they do not conflict (different actions:
    Tab vs T); document this in a comment. While the panel is open, pressing T should not be needed —
    but if pressed it still toggles FlightController mode; that is acceptable and the panel re-asserts
    Visible on its next selection interaction. Keep it simple: panel open ⇒ Visible cursor, panel
    closed ⇒ Captured.

    Wire Main.tscn: add a Control node named TargetSelectorPanel under CanvasLayer (sibling of Hud),
    script = the new TargetSelectorPanel.cs ext_resource, anchored to the right edge as a compact
    side panel (e.g. right-anchored, ~220px wide), mouse_filter = 2 (Ignore) initially, with
    HudPath = NodePath("../Hud"), FlightPath = NodePath("../../FlightController"),
    WorldPath left empty (FindChild fallback). Add the VBoxContainer child the script populates (or
    create it in code in _Ready — pick one and be consistent). Use Edit (scoped) on Main.tscn — add
    the new ext_resource line and the new node block; do NOT rewrite the whole scene file.
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" && dotnet build EcoSpace.csproj -c Debug -v q 2>&1 | grep -v '^#' | grep -Ei 'error|Build succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build succeeds, 0 errors / 0 warnings.
    - Main.tscn contains a TargetSelectorPanel node under CanvasLayer with HudPath/FlightPath wired and an ext_resource for the script.
    - Toggle action opens/closes the panel; open ⇒ Input.MouseMode Visible + MouseFilter Stop, close ⇒ Captured + Ignore.
    - The toggle event is marked handled so it does not collide with the FlightController T-key handler.
  </acceptance_criteria>
  <done>Tab opens/closes the panel with correct cursor handling; panel is wired in the scene.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Play-test the cross-space selector panel</name>
  <action>Blocking human play-test — the executor pauses and the developer runs the steps in
  &lt;how-to-verify&gt; in the Godot editor, then approves or reports issues. No code is written in this task.</action>
  <what-built>
    The cross-space target selector: Tab opens a compact right-side panel grouped GALAXY/STAR/PLANET
    listing every body with name + live distance, the active target marked; up/down + Enter (or
    click) selects; the cursor frees on open and re-captures on close. FlightController ease-out
    still reads the selected target.
  </what-built>
  <how-to-verify>
    1. Launch the project in the Godot editor (F5) — open the Main scene.
    2. Press Tab. CONFIRM: the side panel appears on the right, the cursor becomes visible, and the
       ship stops responding to mouse steering.
    3. CONFIRM the panel shows GALAXY / STAR / PLANET headers (only for tiers that have bodies) with
       name + distance per row, and the currently active target row is marked.
    4. Use Up/Down then Enter (and also try clicking a row) to select a body in a DIFFERENT space
       (e.g. select a galaxy or a sibling star). CONFIRM the active-target mark moves and the TGT
       readout in the HUD updates to that body's name + distance.
    5. Press Tab again. CONFIRM the panel closes, the cursor re-captures, and mouse steering resumes.
    6. CONFIRM pressing the old Tab-cycle no longer "cycles" targets behind the panel (selection is
       only via the panel now).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what is wrong (e.g. cursor not freeing, distances not updating, wrong tier grouping).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | Local single-player offline desktop game. No network, no auth, no untrusted input, no persisted user data. Input is local keyboard/mouse only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | panel mutating sim state | mitigate | Panel selects only through Hud.SetTargetIndex and reads via GetTargetCandidates/UniMath; never writes GameObjects/LocalPos. Verified by grep + review. |
| T-06-05 | Denial of Service | per-frame label rebuild while open | accept | Only rebuilds while the panel is visible; list size is a few dozen rows in the MVP scene. Negligible. |
| T-06-06 | (network/auth/info-disclosure) | n/a | accept | No network boundary, no secrets, no untrusted input in scope. |

No high-severity threats. No package installs (no schema/ORM in this C#/.NET game).
</threat_model>

<verification>
- dotnet build clean (0/0).
- grep: TargetSelectorPanel does not contain "GameObjects[" assignment or ".LocalPos =".
- Main.tscn contains the TargetSelectorPanel node + ext_resource.
- Human play-test checkpoint approved.
</verification>

<success_criteria>
- Tab opens/closes a tier-grouped cross-space selector; selection drives the active target read-only.
- Cursor frees on open / re-captures on close without breaking the T-key flight mode.
- FlightController ease-out follows the panel-selected target.
</success_criteria>

<output>
Create `.planning/phases/06-targeting-system/06-02-SUMMARY.md` when done.
</output>
