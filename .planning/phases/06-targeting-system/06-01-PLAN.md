---
phase: 06-targeting-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Scripts/Hud/Hud.cs
  - project.godot
autonomous: true
requirements: [D-55, D-56, D-53]
must_haves:
  truths:
    - "ActiveTargetIndex can resolve a body in ANY space (galaxy, star, planet), not only the ship's current-parent tier"
    - "A public SetTargetIndex(int) lets an external panel set the active target without the panel touching sim state"
    - "The Tab cycle_target input is retired — pressing Tab no longer changes the target"
    - "The 2D DrawArc target circle no longer renders (superseded by the 3D marker in a later plan)"
    - "Hud still never mutates GameObjects, LocalPos, or any sim state as a rendering side effect"
  artifacts:
    - path: "Scripts/Hud/Hud.cs"
      provides: "Full-hierarchy targetable set + tier-ordered candidate list + SetTargetIndex + ActiveTargetIndex returning a GameObjects index for any space"
      contains: "BuildFullHierarchyTargetList"
    - path: "project.godot"
      provides: "toggle_target_panel input action; cycle_target removed"
      contains: "toggle_target_panel"
  key_links:
    - from: "Scripts/Flight/FlightController.cs"
      to: "Scripts/Hud/Hud.cs"
      via: "FlightController reads Hud.ActiveTargetIndex for the D-43 target ease-out — signature unchanged"
      pattern: "ActiveTargetIndex"
---

<objective>
Convert the Hud target system from current-tier-only (D-12/D-45) to a cross-space,
panel-driven selector core (D-55/D-56), and remove the superseded 2D circle path (D-50),
WITHOUT breaking the read-only contract (D-53) or the FlightController.ActiveTargetIndex
ease-out contract (D-43).

This is the foundation plan: it gives the panel (06-02) a candidate API to render and a
SetTargetIndex method to call, and it gives the 3D marker (06-03) a cross-space
ActiveTargetIndex to mark. No new UI or 3D rendering is added here.

Purpose: a single read-only owner of the active cross-space target that the panel writes
and that FlightController + the marker read.
Output: modified Scripts/Hud/Hud.cs, modified project.godot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-targeting-system/06-CONTEXT.md
@.planning/phases/06-targeting-system/06-PATTERNS.md
@CLAUDE.md
@Scripts/Hud/Hud.cs
@Scripts/Math/UniMath.cs
@Scripts/UniObject.cs
@Scripts/Flight/FlightController.cs
</context>

<interface_context>
Existing contracts this plan MUST preserve unchanged:
- `Hud.ActiveTargetIndex` (public int property) — FlightController.cs line ~504 reads it for the
  D-43 ease-out. Return type and name stay identical; only its INTERNAL resolution changes
  (now resolves over the full hierarchy candidate list instead of parent+siblings).
- `Hud.FormatDistance(double)` (public static) — reused as-is by the panel (06-02) and label (06-03).
- `Hud.UpdateDirectionMarker(Double3)` and `HideMarker()` — kept verbatim (06-03 label rides them).
- `UniMath.RelativeMetres(from,to,objs)` / `UniMath.Distance(a,b,objs)` — the ONLY sanctioned
  cross-frame math (CLAUDE.md). Never use `body.LocalPos.ToDouble3()` or cross-scale UniVec3
  subtraction for a cross-space body.

New contracts this plan PRODUCES (consumed downstream):
- `public void SetTargetIndex(int candidateIndex)` — clamps and stores into _targetIndex
  (read-only of SIM state; _targetIndex is internal HUD ownership, not sim state). Called by
  TargetSelectorPanel (06-02).
- `public System.Collections.Generic.IReadOnlyList<int> GetTargetCandidates()` — returns the
  tier-ordered GameObjects indices of every targetable body (for the panel to render). Each
  element is a GameObjects index; the panel reads name/distance off GameObjects + UniMath.
- The tier order is Galaxy, then Star, then Planet (matches the user mock in 06-CONTEXT.md).
</interface_context>

<artifacts_produced>
NEW symbols this plan introduces (exclude from drift checks):
- Hud.BuildFullHierarchyTargetList(...) — private, replaces BuildTargetableList usage
- Hud.SetTargetIndex(int) — public
- Hud.GetTargetCandidates() — public (IReadOnlyList<int>)
- project.godot input action: toggle_target_panel
REMOVED symbols (intentional, not drift):
- Hud._showTargetCircle, _targetCirclePos, _targetCircleRadius fields
- Hud.UpdateTargetCircle(...), Hud._Draw() override, the QueueRedraw() call in _Process
- Hud._Input(...) cycle_target handler; project.godot cycle_target action
- Hud.MIN_CIRCLE_RADIUS / MAX_CIRCLE_RADIUS / CIRCLE_BODY_PADDING consts (move to 06-03 if needed there)
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace targetable set with a cross-space full-hierarchy candidate list</name>
  <read_first>
    Scripts/Hud/Hud.cs (BuildTargetableList lines 501-531, ActiveTargetIndex lines 98-113,
    UpdateContextLabel lines 197-234, UpdateTargetReadout lines 238-271, SpaceTierName lines 533-541);
    Scripts/UniObject.cs (Type enum, Space enum, ObjectType, ChildIndices);
    Scripts/Math/UniMath.cs (Distance, RelativeMetres).
  </read_first>
  <files>Scripts/Hud/Hud.cs</files>
  <action>
    Add a private BuildFullHierarchyTargetList(int shipIndex, List&lt;UniObject&gt; gameObjects)
    that walks ALL entries in gameObjects (not parent+siblings) and returns a
    List&lt;TargetEntry&gt; of every targetable body, ordered by Space tier: Galaxy first, then
    Star, then Planet (per D-55 and the user mock). Skip: the ship itself, null entries,
    out-of-range via the (uint)idx bounds-check guard, and Root/Universe-space container objects
    that are not selectable bodies. Galaxies ARE selectable (D-55) — do NOT skip Type.Galaxy here
    (that was the old current-tier exclusion). Use body.ObjectType to assign the tier bucket;
    within a bucket preserve GameObjects order. Keep the existing TargetEntry struct (Index only).

    Repoint ActiveTargetIndex, UpdateContextLabel, and UpdateTargetReadout to call
    BuildFullHierarchyTargetList(shipIndex, gameObjects) instead of
    BuildTargetableList(ship.ParentIndex, shipIndex, gameObjects). ActiveTargetIndex keeps its
    exact public signature and the Mathf.Clamp(_targetIndex, 0, count-1) behavior — only the
    source list changes, so a galaxy/star/planet in any space can now be the active target.

    For UpdateContextLabel keep the existing "nearest" computation but source candidates from the
    new list; keep the Galaxy "nearest" exclusion ONLY for the nearest-body readout (a galaxy is
    not a body you are "near"), NOT for the selectable set. All distance math via UniMath.Distance
    / UniMath.RelativeMetres (LCA path) — never raw LocalPos.ToDouble3() for cross-space bodies.

    Remove the now-unused BuildTargetableList(parentIdx, shipIndex, gameObjects) method ONLY after
    confirming no remaining caller references it (UpdateTargetCircle and _Input are removed in
    Task 2/3). Preserve the read-only doc-comment contract on every new method (read-only consumer
    of GameWorld state).
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" && dotnet build EcoSpace.csproj -c Debug -v q 2>&1 | grep -v '^#' | grep -Ei 'error|Build succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build succeeds with 0 errors, 0 warnings.
    - BuildFullHierarchyTargetList returns galaxies, stars, and planets across spaces in
      Galaxy→Star→Planet tier order; the ship is never in the list.
    - ActiveTargetIndex public signature unchanged; FlightController still compiles against it.
    - No method in Hud.cs calls body.LocalPos.ToDouble3() for a cross-space body; cross-frame
      distance/direction goes through UniMath only.
  </acceptance_criteria>
  <done>Hud resolves the active target over the full hierarchy; build is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SetTargetIndex + GetTargetCandidates; retire Tab cycle and its input action</name>
  <read_first>
    Scripts/Hud/Hud.cs (_Input lines 463-486, ActiveTargetIndex lines 98-113);
    project.godot (cycle_target action lines 66-69 + roll_left/throttle_up entries lines 39-65 for the
    InputEventKey block format to copy).
  </read_first>
  <files>Scripts/Hud/Hud.cs, project.godot</files>
  <action>
    In Hud.cs add: public void SetTargetIndex(int candidateIndex) that stores
    Mathf.Clamp(candidateIndex, 0, count-1) into _targetIndex, where count is
    GetTargetCandidates().Count (clamp to 0 and return early when count == 0). Add
    public System.Collections.Generic.IReadOnlyList&lt;int&gt; GetTargetCandidates() that builds the
    full-hierarchy list (Task 1) for the current ship and returns the GameObjects indices in tier
    order; return an empty list when the ship/world is not ready. Both methods are read-only of SIM
    state (they only write the internal HUD _targetIndex) — document this in their doc-comments
    (D-53). Note: _targetIndex is the candidate-list position, and the panel selects by candidate
    position; SetTargetIndex takes the candidate position, NOT a raw GameObjects index.

    Remove the entire _Input(InputEvent) override that handled "cycle_target" (Hud.cs lines ~463-486).
    The panel (06-02) owns selection now (D-56).

    In project.godot: remove the cycle_target action block (lines ~66-69) and add a new action
    toggle_target_panel bound to the Tab physical key (physical_keycode 4194306, the same key the
    retired cycle_target used) — reuse the exact InputEventKey object format from the neighboring
    actions. This frees Tab for the panel open/close toggle that 06-02 implements.
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" && dotnet build EcoSpace.csproj -c Debug -v q 2>&1 | grep -v '^#' | grep -Ei 'error|Build succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build succeeds, 0 errors / 0 warnings.
    - No "cycle_target" reference remains in Hud.cs or project.godot (grep clean).
    - project.godot contains a toggle_target_panel action bound to physical key 4194306 (Tab).
    - SetTargetIndex clamps to the candidate-list bounds and writes only _targetIndex.
  </acceptance_criteria>
  <done>Tab no longer cycles targets; the panel API (SetTargetIndex/GetTargetCandidates) exists; toggle_target_panel is bound.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove the 2D DrawArc target circle path</name>
  <read_first>
    Scripts/Hud/Hud.cs (target-circle state lines 58-63, _Process line 181-182,
    UpdateTargetCircle lines 346-440, _Draw lines 442-459, MIN/MAX/PADDING consts lines 350-355,
    _worldRenderer field line 56 and its resolution lines 137-143, FindNodeByType lines 548-562).
  </read_first>
  <files>Scripts/Hud/Hud.cs</files>
  <action>
    Remove the 2D circle entirely (superseded by the 3D sphere-outline marker in 06-03, D-50):
    delete the _showTargetCircle / _targetCirclePos / _targetCircleRadius fields, the
    UpdateTargetCircle(...) method, the _Draw() override, the MIN_CIRCLE_RADIUS /
    MAX_CIRCLE_RADIUS / CIRCLE_BODY_PADDING consts, and the call to UpdateTargetCircle(...) plus
    the QueueRedraw() call in _Process (remove QueueRedraw only if no other _Draw consumer remains
    — there is none after _Draw is deleted).

    The _worldRenderer field + its resolution in _Ready was only used by the render-set gate for
    the 2D circle (D-46 gate, removed by D-51). Remove _worldRenderer, its _Ready resolution block
    (lines ~137-143), and the FindNodeByType&lt;T&gt; helper IF no other code references them.
    Keep _camera (still used by UpdateDirectionMarker). Keep UpdateDirectionMarker and HideMarker
    verbatim — the 06-03 tracking label rides them.

    Do NOT add the literal phrase the 06-03 marker's acceptance will negative-grep for; this task
    only deletes 2D-circle code.
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" && dotnet build EcoSpace.csproj -c Debug -v q 2>&1 | grep -v '^#' | grep -Ei 'error|Build succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build succeeds, 0 errors / 0 warnings.
    - No _Draw override, no DrawArc call, no _showTargetCircle / UpdateTargetCircle remain in Hud.cs.
    - UpdateDirectionMarker and HideMarker are unchanged.
    - The build runs with no unresolved references to _worldRenderer / FindNodeByType (either both
      removed, or kept only if still referenced).
  </acceptance_criteria>
  <done>The 2D circle path is gone; Hud compiles clean and keeps the off-screen edge marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | Local single-player offline desktop game. No network, no auth, no untrusted input, no persistence of user data. Input is keyboard/mouse from the local user only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Hud read-only contract (D-53) | mitigate | All new methods (SetTargetIndex/GetTargetCandidates/BuildFullHierarchyTargetList) only read GameObjects and write the internal _targetIndex; never mutate UniObject/LocalPos/ChildIndices. Enforced by doc-comment + code review. |
| T-06-02 | Denial of Service | full-hierarchy walk each frame | accept | Hierarchy ≤ a few dozen bodies in the hand-authored MVP scene; per-frame O(n) scan is negligible. No external input drives n. |
| T-06-03 | (network/auth/info-disclosure) | n/a | accept | No network boundary, no secrets, no untrusted input in scope. |

No high-severity threats. No package installs in this plan (no schema/ORM in this C#/.NET game).
</threat_model>

<verification>
- dotnet build clean (0/0) after each task.
- grep: no "cycle_target" anywhere; "toggle_target_panel" present in project.godot.
- grep: no "_Draw"/"DrawArc"/"UpdateTargetCircle" in Hud.cs.
- ActiveTargetIndex signature unchanged (FlightController compiles).
</verification>

<success_criteria>
- The active target can be any body in any space (galaxy/star/planet).
- Panel API (SetTargetIndex / GetTargetCandidates) is available and read-only of sim state.
- Tab no longer cycles; 2D circle removed; edge marker kept.
- Existing 48/48 unit tests still green (no test file touched, but build must stay green).
</success_criteria>

<output>
Create `.planning/phases/06-targeting-system/06-01-SUMMARY.md` when done.
</output>
