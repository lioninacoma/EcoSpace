---
context: phase
phase: 06-targeting-system
task: 0
total_tasks: 3
status: planning_complete_ready_to_execute
last_updated: 2026-06-20T22:04:30.011Z
---

<current_state>
Phase 6 (Targeting & Navigation HUD) **planning is complete and committed** (commit `68345da`).
Three plans created across three waves, all validated. **No execution has started — no code written.**
This is a clean pause at the plan→execute boundary. Branch: `main`. Phase 5 (Rendering Overhaul) is complete.
</current_state>

<completed_work>

Completed:
- Task 1: Gather Phase 6 context (06-CONTEXT.md + 06-PATTERNS.md) — Done (commit 0d60e66)
- Task 2: Plan Phase 6 — 3 plans / 3 waves, frontmatter + structure validated, ROADMAP finalized — Done (commit 68345da)

Plans created:
- `.planning/phases/06-targeting-system/06-01-PLAN.md` (Wave 1, autonomous)
- `.planning/phases/06-targeting-system/06-02-PLAN.md` (Wave 2, has play-test checkpoint)
- `.planning/phases/06-targeting-system/06-03-PLAN.md` (Wave 3, has play-test checkpoint)
</completed_work>

<remaining_work>

- Execute 06-01 (Wave 1): cross-space selector core in Hud.cs — full-hierarchy candidate set, SetTargetIndex/GetTargetCandidates, retire Tab cycle, remove 2D DrawArc circle. Files: Scripts/Hud/Hud.cs, project.godot. Not started.
- Execute 06-02 (Wave 2, blocked on 06-01): TargetSelectorPanel UI + Main.tscn wiring + cursor/mouse-mode reconciliation + blocking play-test. Files: Scripts/Hud/TargetSelectorPanel.cs, Main.tscn. Not started.
- Execute 06-03 (Wave 3, blocked on 06-02): 3D sphere-outline marker from UniObject + target_outline.gdshader + name+distance tracking label + Main.tscn wiring + blocking play-test. Files: Scripts/Render/TargetMarkerRenderer.cs, Shaders/target_outline.gdshader, Main.tscn. Not started.
</remaining_work>

<decisions_made>

- 3 plans / 3 sequential waves (not parallel): tight Hud.cs coupling + shared Main.tscn force ordering — 06-02 consumes the 06-01 Hud selector API; 06-03 shares Main.tscn with 06-02 and reads the cross-space ActiveTargetIndex from 06-01.
- Active-target ownership stays in Hud (`_targetIndex`); the panel writes via a new public `SetTargetIndex(int)`. Preserves the FlightController `ActiveTargetIndex` D-43 ease-out contract while keeping Hud read-only of SIM state (D-53).
- Tab repurposed: `cycle_target` retired (D-56) → new `toggle_target_panel` action bound to Tab. Panel is the cursor owner (open ⇒ Input.MouseMode Visible, close ⇒ Captured), reconciled with the 01-03 FlightController T-key Confined/Captured mode (different keys: Tab vs T).
- Marker technique: unit SphereMesh + unlit rim/fresnel outline shader (single-pass, discard interior fragments) — Claude's-discretion per D-50, PATTERNS.md recommendation.
- Marker placement/size computed purely from UniObject via UniMath LCA + observer-unit math (`metres / ship.LocalPos.Scale * factor`), NO render-set gate (D-51) — so a cross-space target incl. a no-mesh galaxy in Universe space still gets a correctly-placed marker.
</decisions_made>

<blockers>
- None. Clean pause. (Two blocking human play-test checkpoints exist inside 06-02 and 06-03; they only matter once execution reaches those tasks.)
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-targeting-system/06-CONTEXT.md` — the authoritative user decisions D-50..D-57 (locked; non-negotiable).
2. `.planning/phases/06-targeting-system/06-PATTERNS.md` — analog files, exact line numbers, and code excerpts for all 4 deliverables (untracked planning input; do not delete).
3. The three PLAN.md files above — the execution prompts.
4. `CLAUDE.md` §"Position Math (UniVec3 / UniMath)" — all cross-frame math via UniMath LCA helpers (hard rule).
5. `.planning/phases/05-rendering-overhaul/05-03-SUMMARY.md` §"Observer-Unit Render Conversion" — the `metres / ship.LocalPos.Scale * factor` rule the marker MUST use.

## Critical Anti-Patterns (do NOT repeat these)
- [OBSERVER-UNIT CONVERSION]: Never use `metres * factor` alone for marker position/radius → always `metres / ship.LocalPos.Scale * factor` (canonical Phase-5 lesson; `metres * factor` is wrong at any non-unity scale).
- [READ-ONLY HUD/MARKER]: Hud and TargetMarkerRenderer must NEVER mutate GameObjects / LocalPos / call TranslatePos as a rendering side effect (D-53). The panel selects only through `Hud.SetTargetIndex`.
- [CROSS-FRAME MATH]: Never use `body.LocalPos.ToDouble3()` or cross-scale UniVec3 subtraction for a cross-space body → use `UniMath.RelativeMetres` / `UniMath.Distance` (LCA path).

## Infrastructure State
- Branch: `main`. Phase 5 complete (5/8 phases, 16/16 plans). Build is green (48/48 unit tests per Phase 5 close).
- Untracked: `.planning/phases/06-targeting-system/06-PATTERNS.md` (planning input artifact — leave in place; it is required reading for execution).
- No running servers/watchers. No async jobs.

<context>
Nothing is in-flight. The plan→execute boundary is clean. Decision coverage (D-50..D-57) is fully verified across the 3 plans; all 3 pass frontmatter + structure validation. The next session should just begin executing Phase 6 from Wave 1.
</context>

<next_action>
Start with: `/gsd-execute-phase 6` to begin Wave 1 (06-01-PLAN.md). No code has been written yet.
</next_action>
