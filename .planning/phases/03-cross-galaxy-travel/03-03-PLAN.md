---
phase: 03-cross-galaxy-travel
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Scripts/Render/WorldRenderer.cs
autonomous: false
requirements:
  - RND-02
  - RND-04
  - RND-05
  - RND-07
user_setup: []

must_haves:
  truths:
    - "In Galaxy space the current galaxy's stars/suns render as emissive sphere meshes (promoted from skybox light-points)"
    - "WorldRenderer never renders a galaxy body as a mesh (galaxies are sky-only, D-28) even when the ship is in Universe space and the galaxy is the current-tier body"
    - "Star identification is by ObjectType==Star, not by the fragile name check, so multiple stars across multiple galaxy systems all render correctly"
    - "Crossing the Star↔Galaxy boundary, a star's switch between sky-point and emissive mesh is visually continuous — no perceptible pop in position, brightness, or color (RND-07)"
  artifacts:
    - path: "Scripts/Render/WorldRenderer.cs"
      provides: "IsStarBody driven by ObjectType==Star; SyncBodies skips Type==Galaxy bodies; Galaxy-space stars render as emissive meshes via GalaxyRenderFactor"
      contains: "UniObject.Type.Star"
  key_links:
    - from: "Scripts/Render/WorldRenderer.cs"
      to: "Scripts/UniObject.cs ObjectType"
      via: "IsStarBody / Galaxy-skip routing"
      pattern: "ObjectType == UniObject\\.Type\\.(Star|Galaxy)"
    - from: "Scripts/Render/WorldRenderer.cs GetRenderPosition"
      to: "Scripts/Render/SkyboxRenderer.cs GetSkyDirection"
      via: "RND-07 handoff baseline (same per-frame hierarchy math → same screen pixel at the crossing)"
      pattern: "GetRenderPosition"
---

<objective>
Deliver the Galaxy-tier mesh + visible-handoff vertical slice: route bodies by the explicit `ObjectType` (Plan 03-01) so that in Galaxy space the current galaxy's stars become emissive sphere meshes, galaxies are never rendered as world-space meshes (D-28), and the Star↔Galaxy point↔mesh swap built in Phase 2 becomes visibly continuous (RND-07).

Purpose: This closes the galaxy-tier portions of RND-02/RND-04 (in-galaxy stars as emissive meshes), RND-05 (in Galaxy space the sky carries only other galaxies), and RND-07 (the Star↔Galaxy handoff is visible and pop-free) — phase success criterion 5.

Output: A running game where, after entering a galaxy's SOI, the galaxy's member stars appear as emissive meshes and the sky there shows only the 2 other galaxies; flying back out, the stars demote to sky points with no perceptible pop.
</objective>

## Phase Goal

**As a** pilot in a 1:1-scale retro space sim, **I want to** fly from my home galaxy across intergalactic space into other galaxies, **so that** the cosmos feels like one continuous traversable universe rather than a single isolated system.

(This plan delivers the observable step: arriving inside a galaxy, its stars resolve into real emissive meshes — the destination feels like a place, and the sky↔mesh transition is seamless.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-cross-galaxy-travel/03-CONTEXT.md
@.planning/phases/03-cross-galaxy-travel/03-RESEARCH.md
@.planning/phases/03-cross-galaxy-travel/03-PATTERNS.md
@.planning/phases/03-cross-galaxy-travel/03-01-SUMMARY.md
@.planning/phases/02-dynamic-skybox/02-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols modified in THIS plan (no genuinely new public symbols — `ObjectType`/`Type.Galaxy`/`Type.Star` were created in Plan 03-01):

- `WorldRenderer.IsStarBody` — body identification changes from `body.Name == "STAR"` to `body.ObjectType == UniObject.Type.Star`
- `WorldRenderer.SyncBodies` — adds `Type.Galaxy` skip guards in the parent-render path and the sibling loop
- `WorldRenderer.GalaxyRenderFactor` — confirmed/tuned for Galaxy space (already `1e-8f`; `RenderFactorFor` already routes Galaxy → it)

<tasks>

<task type="auto">
  <name>Task 1: Route WorldRenderer by ObjectType — emissive Galaxy-space star meshes, skip galaxies, verify RND-07 handoff</name>
  <files>Scripts/Render/WorldRenderer.cs</files>
  <read_first>
    - Scripts/Render/WorldRenderer.cs (the file being modified — `IsStarBody` at line 384, `SyncBodies` parent-render + sibling loop lines 200-298, `RenderBodyAt` emissive-star path lines 403-460, `GetOrCreateMesh` star-vs-lit material branch lines 483-555, `RenderFactorFor` lines 126-133, `GalaxyRenderFactor` line 64, `GetRenderPosition` RND-07 accessor lines 320-325, `ComputeStarRenderPosFromHierarchy` lines 347-376 which also calls IsStarBody)
    - Scripts/Render/SkyboxRenderer.cs (the `GetSkyDirection` cache accessor lines 217-222 — the sky-side half of the RND-07 handoff this plan visibly exercises)
    - .planning/phases/03-cross-galaxy-travel/03-PATTERNS.md §"Scripts/Render/WorldRenderer.cs" (IsStarBody replacement, the Galaxy-skip placement in both the parent block and sibling loop, RenderFactorFor already-correct note)
    - .planning/phases/03-cross-galaxy-travel/03-RESEARCH.md §"Pattern 4" (Type routing + GalaxyRenderFactor math: galaxy-tier star at 4.2 ly = 39,700 render units inside the 1e6 far plane), §"Pattern 7" (RND-07 Star↔Galaxy handoff — why same-math per-frame guarantees alignment), §"Common Pitfalls" Pitfall 4 (must skip Type==Galaxy or it renders a galaxy-sized sphere)
    - .planning/phases/03-cross-galaxy-travel/03-CONTEXT.md §decisions D-38 (Type routing), D-39 (GalaxyRenderFactor), D-31 (no proximity promotion — member stars appear at SOI entry only, not by approach), and §02-CONTEXT D-21 (instant exact-match swap, no crossfade) / D-22 (scale-crossing-only promotion)
    - .planning/phases/03-cross-galaxy-travel/03-01-SUMMARY.md (confirm every star/galaxy body's ObjectType tagging and the home-galaxy member-star set that becomes Galaxy-space meshes)
  </read_first>
  <action>
    Per D-38: replace `IsStarBody(UniObject body) => body.Name == "STAR"` with `body.ObjectType == UniObject.Type.Star`. This is the single identification point used by both `RenderBodyAt` (emissive star material + per-frame `StarRendering.ApparentBrightness` emission) and `ComputeStarRenderPosFromHierarchy`; switching to Type makes every star across the home + destination + cluster galaxies render as an emissive mesh when it is in the ship's current tier — covering the galaxy-tier portion of RND-02/RND-04 (in Galaxy space the galaxy's member stars are emissive meshes), reusing the existing emissive-star path with no new material code.
    Per D-28/Pitfall 4: add `if (body.ObjectType == UniObject.Type.Galaxy) continue;` in the sibling loop (after the existing `if (body == null) continue;` at line 248) AND guard the parent-render path so a galaxy parent (ship in Universe space, galaxy is the current-tier body that TierClassifier returns as CurrentTierMesh) is NOT rendered as a sphere mesh — skip the `RenderBodyAt` parent call and its activeIndices/renderPositions registration when `parent.ObjectType == UniObject.Type.Galaxy`. Without this skip, WorldRenderer would build a galaxy-sized sphere in render units, violating D-28. Galaxies remain entirely sky-shader rendered (Plan 03-01).
    Confirm `GalaxyRenderFactor` (line 64, `1e-8f`) and the `RenderFactorFor` Galaxy case (line 130) are correct for Galaxy space per D-39/Pattern 4 (galaxy-tier star at 4.2 ly = 3.97e12 Galaxy units × 1e-8 = 39,700 render units, inside the 1e6 far plane). Tune only if play-test shows galaxy-space stars clipping the far plane or sitting sub-pixel-invisible at expected distances; otherwise leave as-is and note in SUMMARY.
    Do NOT add any proximity-based promotion of out-of-tier member stars (D-31/D-22) — member stars become meshes strictly when the ship enters Galaxy space (the existing TierClassifier CurrentTierMesh path), not by approach distance. The RND-07 visible handoff requires no explicit at-transition alignment code (Pattern 7): both `WorldRenderer.GetRenderPosition` (mesh side) and `SkyboxRenderer.GetSkyDirection` (sky side) derive position from the same per-frame `UniMath` hierarchy math, so a star's sky-point direction and its mesh render position already coincide on the same screen pixel at the crossing; color matches via shared `BaseColor` (D-18) and brightness via shared `StarRendering` (D-30). Verify this alignment in the human-check rather than adding swap code.
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" &amp;&amp; dotnet build EcoSpace.csproj -v quiet 2>&amp;1 | tail -5</automated>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" &amp;&amp; grep -v '^[[:space:]]*//' Scripts/Render/WorldRenderer.cs | grep -c 'body.Name == "STAR"'</automated>
    <human-check>Launch the game and fly from the home system out to Galaxy space (exit the home star's SOI so the ship is a Galaxy-space body): confirm (1) the home galaxy's member stars (home STAR + ALPHA CEN/BARNARD/SIRIUS) now appear as emissive sphere meshes, not sky points; (2) the sky in Galaxy space shows ONLY the 2 other galaxies (no star points — RND-05 galaxy tier); (3) no galaxy is rendered as a solid sphere mesh anywhere. Then fly back down into a star's SOI and out again, watching one specific star at the Star↔Galaxy crossing: confirm its point→mesh (and mesh→point) swap is visually continuous — no jump in screen position, no flash in brightness, no color shift (RND-07). Repeat the entry/exit at the destination galaxy after flying there (Plan 03-02) to confirm the full far-end chain.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: `WorldRenderer.IsStarBody` returns `body.ObjectType == UniObject.Type.Star`; the grep gate for the old `body.Name == "STAR"` on non-comment lines returns 0.
    - Source assertion: `SyncBodies` contains `body.ObjectType == UniObject.Type.Galaxy` skip guards in BOTH the sibling loop and the parent-render path.
    - Source assertion: no proximity-based promotion code exists (member stars promote only via the TierClassifier CurrentTierMesh path — confirm by inspection, D-31/D-22).
    - Build assertion: `dotnet build EcoSpace.csproj` succeeds with 0 errors.
    - Behavior (human-check): Galaxy-space member stars render as emissive meshes; the Galaxy-space sky shows only the other galaxies; galaxies are never meshes; the Star↔Galaxy point↔mesh swap is pop-free in position/brightness/color.
  </acceptance_criteria>
  <done>WorldRenderer routes meshes by ObjectType: Galaxy-space member stars are emissive meshes (RND-02/04 galaxy tier), galaxies are sky-only (D-28), and the Star↔Galaxy handoff is visibly continuous (RND-07). Phase success criterion 5 is met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored ObjectType → render routing | A body's `ObjectType` decides whether WorldRenderer builds a mesh; mis-tagged data is the only realistic fault |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service | WorldRenderer galaxy mesh | mitigate | Explicit `Type==Galaxy` skip in both render paths prevents constructing a galaxy-scale sphere mesh (Pitfall 4); render loop stays bounded by parent.ChildIndices |
| T-03-07 | Tampering | mis-tagged ObjectType (e.g. a galaxy left as None) | accept | Hand-authored data only; Plan 03-01 acceptance criteria assert every body's ObjectType; no untrusted input path |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 3 (RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `dotnet build EcoSpace.csproj` succeeds with 0 errors.
- The name-based star check is gone (grep gate = 0); galaxies are skipped in both render paths.
- Manual flight: Galaxy-space member stars are emissive meshes; Galaxy-space sky shows only other galaxies; Star↔Galaxy swap is pop-free.
</verification>

<success_criteria>
- In Galaxy space the current galaxy's stars/suns render as emissive sphere meshes promoted from skybox light-points, and the sky there carries only other galaxies (phase success criterion 5; galaxy-tier RND-02/04/05).
- The Star↔Galaxy skybox↔mesh handoff is visually continuous — no perceptible pop in position, brightness, or color (RND-07).
- Galaxies are never rendered as world-space meshes (D-28).
</success_criteria>

<output>
Create `.planning/phases/03-cross-galaxy-travel/03-03-SUMMARY.md` when done. MUST record whether GalaxyRenderFactor needed tuning for Galaxy space and the observed RND-07 handoff quality (any residual pop) for the end-of-phase review.
</output>
