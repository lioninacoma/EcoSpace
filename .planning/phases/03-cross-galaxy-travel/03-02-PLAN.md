---
phase: 03-cross-galaxy-travel
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Scripts/Flight/FlightController.cs
autonomous: false
requirements:
  - TRV-02
user_setup: []

must_haves:
  truths:
    - "At intergalactic scale the ship reaches FTL-equivalent speed via the existing distance-based auto-scale (no manual mode switch)"
    - "The player can fly from inside the home galaxy's SOI, out into intergalactic (Universe) space, and into a second galaxy's SOI with no crash, pop, or loading screen"
    - "As the ship approaches a galaxy disc, max speed eases down automatically so the arrival decelerates rather than overshooting (trust-the-curve, D-36)"
    - "A full-throttle galaxy-to-galaxy crossing completes in a deliberate, reasonable play-session duration (D-35)"
  artifacts:
    - path: "Scripts/Flight/FlightController.cs"
      provides: "MaxSpeed setter with the SpeedOfLight cap removed and a NaN/Infinity-safe guard; intergalactic default MaxSpeed"
      contains: "System.Math.Max(0.0, value)"
  key_links:
    - from: "Scripts/Flight/FlightController.cs"
      to: "Scripts/TestSetup.cs galaxy RadiusMeters"
      via: "UpdateSpeedEnvelope nearest-surface scan (galaxy RadiusMeters>0 feeds the curve)"
      pattern: "RadiusMeters"
---

<objective>
Deliver the intergalactic-flight vertical slice: remove the speed-of-light cap that silently prevents intergalactic velocities, raise the default MaxSpeed to an FTL-equivalent value tuned for a reasonable crossing time, and rely on the existing distance→speed curve (which now sees the galaxy `RadiusMeters` authored in Plan 03-01) to decelerate smoothly on approach.

Purpose: This is the TRV-02 core — the player must actually be able to fly between galaxies. It is a constraint removal + tuning on the existing D-06/07/08 curve (D-35), not a new flight mode.

Output: A running game where the player can throttle up in the home system, accelerate to FTL magnitudes once in open intergalactic space, fly to the destination galaxy, and ease into its SOI — completing the crossing in a deliberate play session.
</objective>

## Phase Goal

**As a** pilot in a 1:1-scale retro space sim, **I want to** fly from my home galaxy across intergalactic space into other galaxies, **so that** the cosmos feels like one continuous traversable universe rather than a single isolated system.

(This plan delivers the observable step: the journey is actually flyable end-to-end at FTL speed with natural deceleration on arrival.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cross-galaxy-travel/03-CONTEXT.md
@.planning/phases/03-cross-galaxy-travel/03-RESEARCH.md
@.planning/phases/03-cross-galaxy-travel/03-PATTERNS.md
@.planning/phases/03-cross-galaxy-travel/03-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols modified in THIS plan (no genuinely new public symbols):

- `FlightController.MaxSpeed` setter — `SpeedOfLight` clamp removed, replaced with `System.Math.Max(0.0, value)`
- `FlightController._maxSpeed` default — raised from `1e11` to an intergalactic value (tuned, ~2e20 m/s starting point)
- `FlightController.SpeedOfLight` const — retained as comment-only reference or removed (no longer used as a cap)
- Possible NaN/Infinity finiteness guard in `ApplyMotion` before `TranslatePos` (security mitigation T-03-04)

<tasks>

<task type="auto">
  <name>Task 1: Remove the SpeedOfLight cap, raise MaxSpeed default, and guard against non-finite motion</name>
  <files>Scripts/Flight/FlightController.cs</files>
  <read_first>
    - Scripts/Flight/FlightController.cs (the file being modified — the `SpeedOfLight` const at line 42, the `_maxSpeed` field + `MaxSpeed` setter at lines 133-143, the `MinSpeed`/`SpeedEasing` setters at lines 124-155 showing the `System.Math.Max(0.0, value)` guard idiom, `UpdateSpeedEnvelope` lines 373-442 showing how `RadiusMeters>0` bodies feed the `nearest` scan, `ApplyMotion` lines 452-470)
    - .planning/phases/03-cross-galaxy-travel/03-RESEARCH.md §"Pattern 5" (MaxSpeed extension), §"FTL Overshoot / Tunneling Analysis" (why no special braking guard is needed), §"Common Pitfalls" Pitfall 3 (galaxy RadiusMeters drives deceleration) and Pitfall 7 (SpeedEasing ghost-speed at entry), §"Security Domain" (V5 + the NaN/Infinity threat)
    - .planning/phases/03-cross-galaxy-travel/03-PATTERNS.md §"Scripts/Flight/FlightController.cs" (exact setter replacement)
    - .planning/phases/03-cross-galaxy-travel/03-CONTEXT.md §decisions D-35 (design to a target crossing time), D-36 (trust the curve, FTL overshoot flagged as test item — add no guard unless it proves real)
    - .planning/phases/03-cross-galaxy-travel/03-01-SUMMARY.md (confirm galaxy `RadiusMeters = 5e20` was authored — the curve depends on it)
  </read_first>
  <action>
    Per D-35/Pattern 5: change the `MaxSpeed` setter from `_maxSpeed = Mathf.Clamp(value, 0.0, SpeedOfLight)` to `_maxSpeed = System.Math.Max(0.0, value)` — this removes the `3e8` speed-of-light cap that currently makes intergalactic speeds impossible, while the `Max(0.0, value)` keeps it non-negative and (since `Math.Max` propagates NaN to the right operand) does not silently introduce a negative. Raise the `_maxSpeed` field default from `1e11` to `2e20` (m/s) as the starting intergalactic value: at the ~2.4e22 m home→destination separation this targets a ~2-minute full-throttle crossing (D-35). Mark this default with a comment as a play-test tuning knob — the exact crossing-time number is planner/play-test discretion per D-35; if the crossing feels too fast/slow, the value is the single knob to adjust (e.g. 4e19 → ~10 min). Keep the `SpeedOfLight` const only as a comment-only reference (it is no longer a cap) or remove it; do NOT use it in any setter.
    Add the security mitigation (RESEARCH §V5, threat T-03-04): in `ApplyMotion`, before calling `_world.TranslatePos`, guard against a non-finite `CurrentSpeed` (e.g. `if (!double.IsFinite(CurrentSpeed)) return;`) so a pathological export value can never push a NaN/Infinity delta into `TranslatePos` and corrupt positions. Do NOT add any FTL overshoot / sub-stepping / per-frame step-clamp braking guard — D-36 defers that unless tunneling is observed in play-test; the existing iterative MaxIterations=32 SOI logic + the SpeedEasing ease-out are the accepted mechanism. Note Pitfall 7 (the SpeedEasing "ghost speed" at galaxy entry is by design, D-07) for the end-of-phase play-test, but change nothing for it here.
  </action>
  <verify>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" &amp;&amp; dotnet build EcoSpace.csproj -v quiet 2>&amp;1 | tail -5</automated>
    <automated>cd "C:/Users/frede/workspace/godot/eco-space" &amp;&amp; grep -v '^[[:space:]]*//' Scripts/Flight/FlightController.cs | grep -c 'Mathf.Clamp(value, 0.0, SpeedOfLight)'</automated>
    <human-check>Launch the game. Throttle up in the home system (speed should auto-scale and crawl near bodies, unchanged). Fly outward: confirm speed climbs to FTL magnitudes (HUD should show ly/s-class units via the D-10 ladder) once in open intergalactic space. Fly toward the destination galaxy and confirm: (1) you reach its SOI and cross into Galaxy space with no crash; (2) speed eases down automatically as you approach the disc (no manual braking, no overshoot through the galaxy). Time the full home→destination crossing at full throttle and note whether it feels reasonable (target ~minutes) — this is the data for the end-of-phase MaxSpeed tuning decision.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Scripts/Flight/FlightController.cs` `MaxSpeed` setter uses `System.Math.Max(0.0, value)` and no longer references `SpeedOfLight` as a clamp (the grep gate for `Mathf.Clamp(value, 0.0, SpeedOfLight)` on non-comment lines returns 0).
    - Source assertion: `_maxSpeed` default is an intergalactic value (≥ 1e19), not `1e11`.
    - Source assertion: `ApplyMotion` contains a `double.IsFinite` (or equivalent) guard on `CurrentSpeed` before `TranslatePos`.
    - Source assertion: no FTL overshoot / sub-stepping / step-clamp braking code was added (D-36 — confirm by inspection that the only change to motion is the finiteness guard).
    - Build assertion: `dotnet build EcoSpace.csproj` succeeds with 0 errors.
    - Behavior (human-check): ship reaches FTL speed in open space, crosses into the destination galaxy SOI without crash, and decelerates automatically on approach.
  </acceptance_criteria>
  <done>The ship can fly between galaxies: FTL-equivalent top speed via the uncapped distance curve, natural ease-out on approach (the galaxy RadiusMeters from Plan 03-01 feeds the scan), and a finiteness guard protecting position integrity. Crossing-time feel is captured for the end-of-phase tuning decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor export → motion | `MaxSpeed` is now uncapped editor data feeding per-frame `TranslatePos` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | FlightController MaxSpeed setter + ApplyMotion | mitigate | `System.Math.Max(0.0, value)` keeps MaxSpeed non-negative; `double.IsFinite(CurrentSpeed)` guard in ApplyMotion blocks NaN/Infinity deltas reaching TranslatePos (RESEARCH §V5) |
| T-03-05 | Denial of Service | FTL overshoot / SOI tunneling | accept | RESEARCH §FTL analysis: galaxy SOI (5e20 m) ≈ 150 frames to cross at 2e20 m/s; iterative MaxIterations=32 handles multi-boundary frames; speed curve decelerates before star-SOI scale. Flagged for play-test (D-36); no guard added unless observed |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 3 (RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `dotnet build EcoSpace.csproj` succeeds with 0 errors.
- The SpeedOfLight clamp is gone from the MaxSpeed setter; a finiteness guard protects motion.
- Manual flight: home system → FTL open space → destination galaxy SOI entry, no crash, automatic deceleration on approach.
</verification>

<success_criteria>
- Player can fly from the home galaxy's SOI into intergalactic space and into a second galaxy's SOI with no crash/pop/loading screen (phase success criterion 2).
- Ship speed reaches FTL-equivalent magnitudes via auto-scaling; crossing completes in a reasonable session (phase success criterion 4).
- The one continuous distance→speed model is extended to Universe scale, not replaced (D-35).
</success_criteria>

<output>
Create `.planning/phases/03-cross-galaxy-travel/03-02-SUMMARY.md` when done. MUST record the chosen `_maxSpeed` default and the observed home→destination crossing time, flagged as the input to the end-of-phase MaxSpeed tuning decision (D-35).
</output>
