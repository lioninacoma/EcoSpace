---
phase: 05-rendering-overhaul
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - Scripts/Render/WorldRenderer.cs
  - Scripts/Render/LuminousPassRenderer.cs
  - Main.tscn
  - Shaders/luminous_pass.gdshader
requirements: [RND-03, RND-06]
autonomous: false

must_haves:
  truths:
    - "The near-star glow/halo pass composes in HDR/linear BEFORE the 8-bit dither quantizes the whole frame — one unified palette edge-to-edge, no banding seam between the glow pass and the rest of the scene (D-05)"
    - "Render-pass ordering is correct and verified: sky shader (behind geometry) → 3D opaque meshes → luminous_pass blend_add transparent quad → WorldEnvironment glow → CanvasLayer dither LAST (D-05, RESEARCH render-pass ordering)"
    - "Dead skybox-era code is removed: WorldRenderer._lastRenderPositions cache + the per-frame writes to it are gone now that the descriptor's Direction is the single source (D-02 cleanup)"
    - "The public GetRenderPosition / GetRenderRadius accessors STILL WORK (Hud._Draw target circle, D-46, depends on them): GetRenderPosition is kept as a thin cache-free accessor reading the live mesh transform, GetRenderRadius is unchanged — no HUD regression"
    - "Stale LuminousPassRenderer galaxy uniforms are confirmed gone (removed in Plan 3) — no dead galaxy push remains now that galaxies live in the sky shader (D-13 cleanup)"
    - "The 1:1 unit-space render (per-space 1e-8 factors, 1e6 far plane), the body_lit dither look, and the galaxy skip guard are preserved — no regression (RND-06, RND-03, D-28)"
    - "CRT scanlines (RND-01 CRT portion) remain OUT of scope (D-06) — this plan is dither composition + cleanup only"
    - "A final per-tier parity/improvement play-test across all four spaces (Planet/Star/Galaxy/Universe) confirms the no-regression bar is met or beaten (D-04)"
  artifacts:
    - path: "Scripts/Render/WorldRenderer.cs"
      provides: "Floating-origin mesh sync with the dead _lastRenderPositions cache removed; thin cache-free GetRenderPosition"
      contains: "RenderFactorFor"
    - path: "Main.tscn"
      provides: "Verified/locked node + CanvasLayer ordering so the dither composes over the glow pass (D-05)"
      contains: "CanvasLayer"
  key_links:
    - from: "Shaders/luminous_pass.gdshader"
      to: "Shaders/dithering.gdshader"
      via: "luminous pass (blend_add, 3D transparent) composes before the canvas_item dither quantizes the composited frame (D-05)"
      pattern: "blend_add"
    - from: "Scripts/Hud/Hud.cs"
      to: "Scripts/Render/WorldRenderer.cs"
      via: "Hud._Draw target circle reads GetRenderPosition / GetRenderRadius — must keep working after the cache removal (D-46)"
      pattern: "GetRenderPosition|GetRenderRadius"
  prohibitions:
    - "Must NOT reintroduce crt.gdshader or any CRT scanline effect (D-06 — RND-01 CRT portion deferred)."
    - "Must NOT add hint_depth_texture to dithering.gdshader (canvas_item) — keep the dither a pure canvas_item quantizer (Godot bug #74464)."
    - "Must NOT change the per-space 1e-8 render factors, the 1e6 far plane, or the 8-bit palette (RND-06 / dither look preserved)."
    - "Must NOT break the Hud._Draw target circle — GetRenderPosition / GetRenderRadius must still resolve a live mesh (D-46). Removing the cache must not remove the public accessor that Hud calls."
    - "Must NOT remove WorldRenderer's galaxy skip guard or break the floating-origin mesh sync while removing the dead cache (D-28 / T-03-06)."
    - "Must NOT remove or disable the sky shader / SkyboxRenderer — distant stars + galaxies render there (D-07 reversed)."
    - "Must NOT reintroduce a manual clip-space billboard MultiMesh (StarPointRenderer anti-pattern)."
---

<objective>
Finalize the rendering overhaul: verify and lock the D-05 composition ordering so the near-star glow/halo pass composes in HDR/linear BEFORE the 8-bit dither quantizes the whole frame (unified palette edge-to-edge, no banding seam), remove the remaining dead skybox-era code (`WorldRenderer._lastRenderPositions` cache + its per-frame writes — superseded by the descriptor's `Direction`, D-02) while keeping the Hud-facing `GetRenderPosition`/`GetRenderRadius` accessors working, confirm no stale galaxy uniforms remain in `LuminousPassRenderer`, and run the final per-tier parity/improvement play-test across all four spaces. CRT stays OUT (D-06).

Purpose: Lock in D-05 ordering, retire the last dual-cache remnant so the descriptor is the single source of truth (D-02), and confirm the whole overhaul meets or beats the no-regression bar (D-04) across every tier.
Output: Verified glow-before-dither ordering; dead WorldRenderer cache removed (Hud accessors intact); final all-tier play-test pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rendering-overhaul/05-CONTEXT.md
@.planning/phases/05-rendering-overhaul/05-RESEARCH.md
@.planning/phases/05-rendering-overhaul/05-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (05-04) is the final plan of Phase 5. It changes the items marked **[this plan]**:

- `WorldRenderer`: `_lastRenderPositions` dictionary field + its per-frame writes removed; `GetRenderPosition` rewritten as a thin cache-free accessor reading the live mesh `_meshes[bodyIdx]` transform; `GetRenderRadius` unchanged — **[this plan]**
- `Main.tscn`: node / CanvasLayer ordering verified (and adjusted only if a seam is found) so the dither composes over the glow pass — **[this plan]**
- `luminous_pass.gdshader`: confirmed `blend_add` 3D-transparent (no change unless a composition seam requires it) — **[this plan, verify-only by default]**
- `LuminousPassRenderer`: confirmed no stale galaxy uniform push remains (removed in Plan 3) — **[this plan, verify-only]**

Already done: descriptor pipeline (Plan 1); skybox refeed + brightness floor + galaxy_disc_weights + NearStarEmissionFloor (Plan 2); near-star glow narrowing + LOD-relaxed gate + GalaxyDiscWeight band fix (Plan 3). The sky shader + SkyboxRenderer remain (D-07 reversed).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify/lock D-05 glow-before-dither composition ordering; confirm no stale galaxy push</name>
  <files>Main.tscn, Shaders/luminous_pass.gdshader, Scripts/Render/LuminousPassRenderer.cs</files>
  <read_first>
    - .planning/phases/05-rendering-overhaul/05-RESEARCH.md (Render Pass Ordering lines 168-178; Architectural Responsibility Map — HDR→8-bit dither row line 99; D-05 lines 23; Assumptions A5/A6 lines 586-587 the SubViewport fallback only-if-needed)
    - Shaders/dithering.gdshader (confirm shader_type canvas_item + hint_screen_texture reads AFTER the 3D scene — quantizes the composited frame; NO hint_depth_texture)
    - Shaders/luminous_pass.gdshader (the blend_add spatial transparent pass that must compose before the dither; verify render_mode still blend_add after Plan 3)
    - Scripts/Render/PostProcessRenderer.cs (the CanvasLayer dither host — runs after the 3D transparent pass)
    - Scripts/Render/LuminousPassRenderer.cs (confirm the Plan-3 galaxy-uniform removal landed — no galaxy SetShaderParameter remains)
    - Main.tscn (node draw order: Camera3D-child LuminousPassRenderer quad in the 3D pass; the CanvasLayer with the dither ColorRect after; WorldEnvironment glow settings lines 36-39)
  </read_first>
  <action>
    Verify the render-pass ordering matches D-05 (RESEARCH Render Pass Ordering): the sky shader renders behind all geometry; 3D opaque meshes (planets + near-star spheres) write depth; the `LuminousPassRenderer` Camera3D-child `blend_add` quad composes the near-star glow/halo additively into the HDR 3D buffer; WorldEnvironment glow processes that buffer; the CanvasLayer dither (`dithering.gdshader`, canvas_item, `hint_screen_texture`) runs LAST and quantizes the fully-composited frame so the 8-bit palette applies once, edge-to-edge. This was structurally set up earlier (the luminous quad is a Camera3D child → naturally before the CanvasLayer). This task VERIFIES it; only adjust Main.tscn node order / CanvasLayer layer indices (scoped `Edit`, never a full Write) if the play-test reveals a banding seam or a layer-order inversion (e.g. the glow pass rendering after the dither, or on a CanvasLayer above it). Do NOT add depth hints to dithering.gdshader and do NOT introduce any HDR SubViewport / CompositorEffect rework unless the play-test proves the additive-in-transparent-pass approach fails to compose before the dither — that is the documented fallback (RESEARCH A5/A6), used only if needed. No CRT (D-06).

    Also confirm the Plan-3 cleanup landed: `LuminousPassRenderer` pushes only star uniforms (no `galaxy_dirs`/`galaxy_disc_weights`/`galaxy_count` push) and `luminous_pass.gdshader` has no galaxy loop — galaxies live in the sky shader (D-13). If any stale galaxy push or uniform remains, remove it now.
  </action>
  <acceptance_criteria>
    - Source assertion: `Shaders/dithering.gdshader` is still `shader_type canvas_item` and contains NO `hint_depth_texture`.
    - Source assertion: `Shaders/luminous_pass.gdshader` render_mode still contains `blend_add`.
    - Source assertion (negative): `Scripts/Render/LuminousPassRenderer.cs` contains no `galaxy_disc_weights` push and no galaxy uniform arrays (Plan-3 cleanup confirmed).
    - Source assertion: `Main.tscn` hosts the dither CanvasLayer such that it draws after the Camera3D-child LuminousPassRenderer quad (the dither is the last post step).
    - Build command: `dotnet build EcoSpace.csproj` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build C:/Users/frede/workspace/godot/eco-space/EcoSpace.csproj</automated>
  </verify>
  <done>The near-star glow pass composes before the 8-bit dither (D-05 ordering confirmed/locked); the dithering shader is an unchanged canvas_item quantizer; no stale galaxy push remains; build clean. Any seam found at play-test is fixed via Main.tscn node/layer order only.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the dead _lastRenderPositions cache; keep GetRenderPosition / GetRenderRadius working for the HUD (D-02 cleanup)</name>
  <files>Scripts/Render/WorldRenderer.cs</files>
  <read_first>
    - Scripts/Render/WorldRenderer.cs (the file being modified — the _lastRenderPositions dictionary field line 149, the per-frame writes lines 228 and 301-304, the GetRenderPosition accessor lines 327-332, the GetRenderRadius accessor lines 344-353 reading _meshes directly, the _meshes dictionary line 152, the SyncBodies loop, RenderFactorFor lines 126-133, and the galaxy skip guards lines 239/255)
    - Scripts/Hud/Hud.cs (CALLERS — line 385 `_worldRenderer.GetRenderPosition(tgtIdx, out Vector3 renderPos)` and line 419 `_worldRenderer.GetRenderRadius(tgtIdx, out float renderRadius)`; the target-circle D-46 outline; confirm exactly what these need so the rewrite keeps them working)
    - .planning/phases/05-rendering-overhaul/05-RESEARCH.md (State of the Art — _lastRenderPositions collapse note lines 476-477; Runtime State Inventory — no persistent state)
    - .planning/phases/05-rendering-overhaul/05-CONTEXT.md (D-02 single descriptor replaces dual caches; Carried Forward — read-only renderer contract)
    - STATE.md (D-46 — Hud._Draw world-pinned circle gated on WorldRenderer.GetRenderPosition; DrawArc; MIN_CIRCLE_RADIUS floor)
  </read_first>
  <action>
    In `Scripts/Render/WorldRenderer.cs`, remove the now-redundant `_lastRenderPositions` dictionary field and the per-frame writes to it (the `_lastRenderPositions.Clear()` at the top of SyncBodies and the copy loop at the end that persists `renderPositions` into it) — the unified descriptor's `Direction` (Plan 1) is now the single source of truth (D-02). The transient per-frame `_renderPositions` scratch used inside SyncBodies for star_dir computation stays (it is still needed within the frame).

    Hud._Draw (D-46) calls BOTH `GetRenderPosition` (Hud.cs line 385) and `GetRenderRadius` (line 419) — do NOT break them (prefer RESEARCH/PATTERNS option (a)). Rewrite `GetRenderPosition(int bodyIdx, out Vector3 pos)` as a thin cache-free accessor that reads the LIVE mesh transform: if `_meshes.TryGetValue(bodyIdx, out var mesh)` and `mesh.Visible`, return `true` with `pos = mesh.Position`; else `pos = default` and return `false`. `GetRenderRadius` already reads `_meshes[bodyIdx]` directly — leave it unchanged. This keeps the HUD target circle working with no separate parallel cache.

    Preserve the floating-origin `SyncBodies`/`RenderBodyAt` loop, the `RenderFactorFor` per-space factor switch, the `NearStarEmissionFloor` clamp added in Plan 2, and the galaxy skip guards (`parent.ObjectType != UniObject.Type.Galaxy` / `body.ObjectType == UniObject.Type.Galaxy → continue`) verbatim — do not regress D-28/T-03-06 or the 1:1 unit-space render (RND-06). Confirm a clean build + green tests.
  </action>
  <acceptance_criteria>
    - Source assertion (negative): `Scripts/Render/WorldRenderer.cs` no longer declares the `_lastRenderPositions` dictionary field nor writes to it.
    - Source assertion: `GetRenderPosition` still exists and reads from `_meshes` (the live mesh transform), not from a `_lastRenderPositions` cache; `GetRenderRadius` still exists.
    - Source assertion: `WorldRenderer.cs` still contains `RenderFactorFor`, `NearStarEmissionFloor`, and the galaxy skip guard (`UniObject.Type.Galaxy`) — no regression to D-28, D-12, or the 1:1 render factors.
    - Source assertion: `Scripts/Hud/Hud.cs` still calls `GetRenderPosition` and `GetRenderRadius` and `dotnet build` proves no dangling reference.
    - Build/test command: `dotnet build EcoSpace.csproj` exits 0 and `dotnet test EcoSpace.Tests/EcoSpace.Tests.csproj` exits 0 (no regression).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build C:/Users/frede/workspace/godot/eco-space/EcoSpace.csproj && dotnet test C:/Users/frede/workspace/godot/eco-space/EcoSpace.Tests/EcoSpace.Tests.csproj</automated>
  </verify>
  <done>The dead _lastRenderPositions cache is removed; GetRenderPosition is a thin cache-free accessor reading the live mesh transform and GetRenderRadius is unchanged, so the Hud target circle still works; floating-origin sync, render factors, NearStarEmissionFloor, and galaxy skip guard preserved; build + tests clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final play-test gate — dither composition + all-tier parity/improvement (Planet/Star/Galaxy/Universe)</name>
  <action>Final human play-test gate per D-08 Plan 4, closing Phase 5. The executor pauses; the user verifies in-engine per the steps below. No code is written in this task.</action>
  <what-built>The final composition + cleanup: the near-star glow pass composing cleanly into the 8-bit dither (one unified palette), the dead skybox-era WorldRenderer cache removed (HUD accessors kept working), and the whole two-layer drawer split (sky shader for distant bodies + post-process glow for near stars + meshes for planets/near stars) ready for a final all-tier parity/improvement check. CRT stays out (D-06).</what-built>
  <how-to-verify>
    1. Run Main in Godot 4.6.2. Confirm a CLEAN build and that the HUD target circle (D-46) still draws correctly around a targeted body (the GetRenderPosition cache removal must not have broken it — cycle targets and confirm the world-pinned circle follows the mesh).
    2. Dither composition (D-05): across all spaces, confirm the 8-bit palette is consistent EDGE-TO-EDGE — the near-star glow/halo from the luminous pass is dithered with the SAME palette as the meshes and the sky-shader background, with NO banding seam or untouched-HDR halo where the glow pass and the rest of the scene meet.
    3. FINAL per-tier parity/improvement pass (D-04 — the phase acceptance gate). For EACH space, confirm it is no worse than the pre-overhaul build (improvement welcome):
       - Planet space: planet mesh + dither + body_lit terminator correct; near sun handled.
       - Star space: planets + sun as bright meshes (D-12); near-star glow halo wrapping the mesh; continuous 3-stage point→glow→mesh handoff (no pop, D-11).
       - Galaxy space: the current galaxy's stars as meshes/points; sibling stars findable via the brightness floor (P1); home galaxy suppressed inside its SOI.
       - Universe space: galaxies visible as sky-shader discs (P2); continuous disc↔stars crossfade fading in/out by distance with no SOI pop (D-13); tilt preserved; no antipodal ghost.
    4. Confirm NO manual billboard artifacts, NO CRT scanlines (correctly absent per D-06), and the established 8-bit/dithered look is preserved or improved. Confirm the sky shader is still the distant-body drawer (D-07 reversed — skybox kept).
    5. Confirm no Output-panel errors/warnings and stable per-frame performance across tiers.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5 if the palette is unified, the HUD circle still works, and every tier meets-or-beats the pre-overhaul look (P1/P2 closed, 3-stage star handoff + galaxy crossfade pop-free); otherwise describe the remaining issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none external) | Local single-player Godot render pipeline operating on hand-authored in-process test data. No network, no untrusted input, no auth, no persistence, no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-NA | (none) | Dither composition + cache cleanup | accept | No applicable threat surface (local render pipeline, authored data, no external/untrusted input). |
| T-05-07 | Tampering | Dead-cache removal breaks the Hud target circle (dangling GetRenderPosition) | mitigate | GetRenderPosition kept as a thin cache-free accessor reading the live mesh transform; GetRenderRadius unchanged; `dotnet build` + play-test verify the circle still draws. |

Posture: local/offline/no-untrusted-input. ASVS L1 (block-on high) passes trivially. No package installs — no supply-chain threat.
</threat_model>

<verification>
- `dotnet build EcoSpace.csproj` exits 0 (clean).
- `dotnet test EcoSpace.Tests/EcoSpace.Tests.csproj` exits 0; no regression.
- dithering shader unchanged canvas_item; near-star glow composes before dither (D-05 ordering); no stale galaxy push.
- Dead _lastRenderPositions cache removed; HUD target circle still functions (GetRenderPosition/GetRenderRadius intact).
- Final all-tier (Planet/Star/Galaxy/Universe) parity/improvement play-test passes (D-04); CRT absent (D-06).
</verification>

<success_criteria>
- The near-star glow pass composes in HDR before the 8-bit dither — unified palette edge-to-edge (D-05, RND-03).
- The descriptor is the single source of truth — the dead _lastRenderPositions cache is gone, HUD accessors preserved (D-02 cleanup).
- The 1:1 unit-space render and dither look are preserved (RND-06, RND-03); no CRT (D-06).
- Final per-tier play-test confirms the no-regression bar met-or-beaten and P1/P2 closed; no StarPointRenderer billboard (D-04).
</success_criteria>

<output>
Create `.planning/phases/05-rendering-overhaul/05-04-SUMMARY.md` when done.
</output>
