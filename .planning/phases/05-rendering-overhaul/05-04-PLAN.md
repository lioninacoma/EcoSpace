---
phase: 05-rendering-overhaul
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - Scripts/Render/WorldRenderer.cs
  - Main.tscn
  - Shaders/luminous_pass.gdshader
autonomous: false
requirements: [RND-01, RND-03, RND-06]

must_haves:
  truths:
    - "The luminous pass composes in HDR/linear BEFORE the 8-bit dither quantizes the whole frame — one unified palette edge-to-edge, no banding seam between the luminous pass and the rest of the scene (D-05, RND-01/RND-03)"
    - "The 1:1 unit-space render (per-space 1e-8 factors, 1e6 far plane) and the body_lit dither look are preserved — no regression (RND-06, RND-03)"
    - "Dead skybox code is gone: WorldRenderer._lastRenderPositions cache + GetRenderPosition accessor are removed now that the descriptor's Direction replaces them (D-02 cleanup)"
    - "A final per-tier parity/improvement play-test across all four spaces (Planet/Star/Galaxy/Universe) confirms the no-regression bar is met or beaten (D-04)"
    - "CRT scanlines (RND-01 CRT portion) remain OUT of scope (D-06) — this plan is dither composition + cleanup only"
  artifacts:
    - path: "Scripts/Render/WorldRenderer.cs"
      provides: "Mesh sync with the dead _lastRenderPositions cache removed (descriptor is the single source)"
      contains: "RenderFactorFor"
  key_links:
    - from: "Shaders/luminous_pass.gdshader"
      to: "Shaders/dithering.gdshader"
      via: "luminous pass renders in 3D transparent pass; dither (canvas_item, after 3D scene) quantizes the composited frame — correct ordering (D-05)"
      pattern: "blend_add"
  prohibitions:
    - "Must NOT reintroduce crt.gdshader or any CRT scanline effect (D-06 — RND-01 CRT portion deferred)."
    - "Must NOT add `hint_depth_texture` to dithering.gdshader (canvas_item) — keep the dither pass a pure canvas_item quantizer (Pitfall 1)."
    - "Must NOT change the per-space 1e-8 render factors, the 1e6 far plane, or the 8-bit palette (RND-06 / D-27 preserved)."
    - "Must NOT remove WorldRenderer's galaxy skip guard or break the floating-origin mesh sync while removing the dead cache."
    - "Must NOT reintroduce a manual clip-space billboard MultiMesh (StarPointRenderer anti-pattern)."
---

<objective>
Finalize the rendering overhaul: ensure the luminous pass composes cleanly in HDR/linear BEFORE the 8-bit dither quantizes the whole frame (D-05) so the palette is unified edge-to-edge with no banding seam; remove the remaining dead skybox-era code (`WorldRenderer._lastRenderPositions` cache + `GetRenderPosition` accessor, now superseded by the descriptor's `Direction` — D-02); and run the final per-tier parity/improvement play-test across all four spaces. CRT (RND-01 CRT portion) stays OUT (D-06).

Purpose: Lock in D-05 composition ordering, retire the last dual-cache remnant so the descriptor is the single source of truth, and confirm the whole overhaul meets or beats the no-regression bar across every tier.
Output: Verified dither-after-luminous ordering; dead WorldRenderer cache removed; final all-tier play-test pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rendering-overhaul/05-CONTEXT.md
@.planning/phases/05-rendering-overhaul/05-RESEARCH.md
@.planning/phases/05-rendering-overhaul/05-PATTERNS.md
@.planning/phases/05-rendering-overhaul/05-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (05-04) modifies `Scripts/Render/WorldRenderer.cs` (removes the `_lastRenderPositions` cache + `GetRenderPosition` accessor) and verifies/adjusts the D-05 dither composition ordering (Main.tscn node order + luminous_pass.gdshader blend if needed). No new files. SkyboxRenderer + skybox.gdshader were already removed in Plan 3. This is the final plan of Phase 5.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify/lock D-05 dither composition ordering (luminous pass HDR/linear before dither)</name>
  <files>Main.tscn, Shaders/luminous_pass.gdshader</files>
  <read_first>
    - .planning/phases/05-rendering-overhaul/05-RESEARCH.md (Summary point 2 lines 75; Architecture Diagram render pass order lines 169-178; D-05 lines 18; Validation Architecture — dither row line 650)
    - .planning/phases/05-rendering-overhaul/05-PATTERNS.md (luminous_pass render modes lines 308-312)
    - Shaders/dithering.gdshader (confirm canvas_item + hint_screen_texture reads AFTER the 3D scene — the dither quantizes the composited frame)
    - Shaders/luminous_pass.gdshader (the blend_add spatial pass that must compose before the dither)
    - Scripts/Render/PostProcessRenderer.cs (the CanvasLayer dither host — confirm it runs after the 3D transparent pass)
    - Main.tscn (node draw order: Camera3D-child luminous quad in the 3D pass, CanvasLayer dither after — confirm WorldEnvironment glow ordering)
  </read_first>
  <action>
    Confirm the render-pass ordering matches D-05: the luminous pass (Camera3D-child spatial quad, `blend_add`) renders in the 3D transparent pass; WorldEnvironment glow runs after it; the CanvasLayer dither (`dithering.gdshader`, canvas_item, `hint_screen_texture`) runs LAST and quantizes the composited frame — so the 8-bit palette is applied once, edge-to-edge, over (scene meshes + luminous pass + glow). This was structurally set up in Plan 2 (luminous quad is a Camera3D child → naturally before the CanvasLayer); this task VERIFIES it and only adjusts node order / CanvasLayer layer indices in Main.tscn if the play-test reveals a banding seam or a layer-order inversion. If the luminous contribution is NOT being captured by the dither (e.g. it renders after the dither, or on a separate CanvasLayer above it), fix the node/layer order in Main.tscn (scoped `Edit`, never full Write) so the dither reads it. Do NOT add depth hints to dithering.gdshader and do NOT introduce any HDR SubViewport/CompositorEffect rework unless the play-test proves the additive-in-transparent-pass approach fails to compose before dither (that is the documented fallback, RESEARCH A5/A6 — only if needed). No CRT (D-06).
  </action>
  <acceptance_criteria>
    - Source assertion: `Shaders/dithering.gdshader` is still `shader_type canvas_item` and contains NO `hint_depth_texture`.
    - Source assertion: `Shaders/luminous_pass.gdshader` render_mode still contains `blend_add` (composes additively into the scene buffer before dither).
    - Source assertion: `Main.tscn` hosts the dither CanvasLayer such that it draws after the Camera3D-child luminous quad (the dither is the last post step).
    - Test-command: `dotnet build EcoSpace.csproj` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "shader_type canvas_item" C:/Users/frede/workspace/godot/eco-space/Shaders/dithering.gdshader && dotnet build C:/Users/frede/workspace/godot/eco-space/EcoSpace.csproj</automated>
  </verify>
  <done>The luminous pass composes before the 8-bit dither (D-05 ordering confirmed); dithering shader is an unchanged canvas_item quantizer; build clean. Any seam found at play-test is fixed via Main.tscn node/layer order only.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead skybox-era WorldRenderer cache (_lastRenderPositions + GetRenderPosition)</name>
  <files>Scripts/Render/WorldRenderer.cs</files>
  <read_first>
    - .planning/phases/05-rendering-overhaul/05-PATTERNS.md (WorldRenderer modified section lines 613-637 — remove _lastRenderPositions + GetRenderPosition in Plan 4 only; preserve galaxy skip guard + RenderFactorFor)
    - .planning/phases/05-rendering-overhaul/05-RESEARCH.md (State of the Art — _lastRenderPositions / GetRenderPosition collapse lines 577-578)
    - Scripts/Render/WorldRenderer.cs (the _lastRenderPositions dictionary, the GetRenderPosition accessor, the floating-origin SyncBodies loop, the galaxy skip guard, RenderFactorFor — only the dead cache is removed; everything else stays)
    - Scripts/Hud/Hud.cs (CHECK: Hud._Draw / target-circle code uses WorldRenderer.GetRenderPosition per STATE D-46 — if it still depends on GetRenderPosition, do NOT break it; either keep a thin GetRenderPosition that derives from the descriptor/mesh transform, or repoint the Hud caller to the surviving source)
    - Scripts/Render/LuminousDescriptorBuilder.cs (the descriptor Direction that now supersedes _lastRenderPositions)
  </read_first>
  <action>
    Remove the now-redundant `_lastRenderPositions` dictionary from `WorldRenderer.cs` and the per-frame writes to it, now that the unified descriptor's `Direction` (Plan 1) is the single source of truth (D-02). BEFORE deleting the `GetRenderPosition` accessor, check every caller — STATE.md D-46 records that `Hud._Draw` (the world-pinned target circle) gates on `WorldRenderer.GetRenderPosition`. If Hud (or any other code) still calls `GetRenderPosition`/`GetRenderRadius`, do NOT break it: either (a) keep `GetRenderPosition`/`GetRenderRadius` as thin accessors that read the current mesh instance transform / render radius directly (no separate cache dictionary), or (b) repoint the caller to the surviving mesh-transform source. Prefer (a) — keep the public accessor, drop only the dead parallel `_lastRenderPositions` cache. Preserve the floating-origin `SyncBodies`/`RenderBodyAt` loop, the `RenderFactorFor` per-space factor switch, and the galaxy skip guard (`body.ObjectType == UniObject.Type.Galaxy → continue` / `parent.ObjectType != Galaxy`) verbatim — do not regress D-28/T-03-06 or the 1:1 unit-space render (RND-06). Confirm clean build + green tests and that the HUD target circle still functions (verified at the play-test gate).
  </action>
  <acceptance_criteria>
    - Source assertion: `Scripts/Render/WorldRenderer.cs` no longer declares the `_lastRenderPositions` dictionary field.
    - Source assertion: `WorldRenderer.cs` still contains `RenderFactorFor` and the galaxy skip guard (`UniObject.Type.Galaxy`) — no regression to D-28 or the 1:1 render factors.
    - Source assertion: no compile-time break — if `Hud.cs` calls `GetRenderPosition`, that accessor still exists (thin, cache-free) OR the caller is repointed; `dotnet build` proves no dangling reference.
    - Test-command: `dotnet build EcoSpace.csproj` exits 0; `dotnet test EcoSpace.Tests/EcoSpace.Tests.csproj` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build C:/Users/frede/workspace/godot/eco-space/EcoSpace.csproj && dotnet test C:/Users/frede/workspace/godot/eco-space/EcoSpace.Tests/EcoSpace.Tests.csproj</automated>
  </verify>
  <done>The dead _lastRenderPositions cache is removed; the descriptor is the single source of body direction; GetRenderPosition either survives as a thin cache-free accessor or its callers are repointed; floating-origin sync, render factors, and galaxy skip guard preserved; build + tests clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final play-test gate — dither composition + all-tier parity/improvement (Planet/Star/Galaxy/Universe)</name>
  <action>Final human play-test gate per D-08 Plan 4, closing Phase 5. The executor pauses and the user verifies in-engine per the steps below; no code is written in this task.</action>
  <what-built>The final composition + cleanup: the luminous pass composing cleanly into the 8-bit dither (one unified palette), the dead skybox-era WorldRenderer cache removed, and the whole unified rendering layer ready for a final all-tier parity/improvement check. CRT stays out (D-06).</what-built>
  <how-to-verify>
    1. Run Main in Godot 4.6.2. Confirm a CLEAN build and that the HUD target circle (D-46) still draws correctly around a targeted body (the GetRenderPosition cleanup must not have broken it).
    2. Dither composition (D-05): across all spaces, confirm the 8-bit palette is consistent EDGE-TO-EDGE — the star glow / galaxy disc from the luminous pass is dithered with the SAME palette as the meshes and background, with NO banding seam or untouched-HDR halo where the luminous pass and the rest of the scene meet.
    3. FINAL per-tier parity/improvement pass (D-04 — the phase acceptance gate). For EACH space, confirm it is no worse than the pre-overhaul build (improvement welcome):
       - Planet space: planet mesh + dither + body_lit terminator correct; near sun handled.
       - Star space: planets + sun meshes; star glow/points; continuous point↔mesh handoff (no pop).
       - Galaxy space: current galaxy's stars as meshes/points; sibling stars findable (P1); home galaxy suppressed.
       - Universe space: galaxies visible as discs (P2); continuous disc↔stars crossfade; tilt preserved; no antipodal ghost.
    4. Confirm NO manual billboard artifacts, NO CRT scanlines (correctly absent per D-06), and the established 8-bit/dithered look is preserved or improved.
    5. Confirm no Output-panel errors/warnings and stable per-frame performance across tiers.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5 if the palette is unified, the HUD circle still works, and every tier meets-or-beats the pre-overhaul look (criteria #1–4 satisfied, P1/P2 closed); otherwise describe the remaining issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none external) | Single-player, offline, local desktop space-sim. No network, no untrusted input, no auth, no persistence, no external services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | Dead-cache removal breaks Hud target circle (dangling GetRenderPosition) | mitigate | Caller audit before deletion; keep GetRenderPosition as a thin cache-free accessor or repoint Hud; `dotnet build` + play-test verify the circle still draws |
| T-05-08 | Denial of Service | Dither/luminous ordering inversion blanks the frame | accept | Local visual-only failure caught at the play-test gate; no security impact, trivially revertable on the branch (D-10) |

Posture: local/offline/no-untrusted-input. No high-severity threats; ASVS L1 (block-on high) passes trivially. No package installs — no supply-chain threat.
</threat_model>

<verification>
- `dotnet build EcoSpace.csproj` exits 0; `dotnet test` exits 0 (no regression).
- Dithering shader unchanged canvas_item; luminous pass composes before dither (D-05 ordering).
- Dead _lastRenderPositions cache removed; HUD target circle still functions.
- Final all-tier (Planet/Star/Galaxy/Universe) parity/improvement play-test passes (D-04); CRT absent (D-06).
</verification>

<success_criteria>
- The 8-bit/dither post-process and unified body-lighting are preserved/improved and integrated — the look does not regress (criterion #3, RND-01 palette / RND-03 / RND-06).
- The descriptor is the single source of truth — no dual cache remains (D-02 cleanup).
- Final per-tier play-test confirms criteria #1–4 met and P1/P2 closed; no StarPointRenderer billboard, no CRT (criterion #4, D-06).
</success_criteria>

<output>
Create `.planning/phases/05-rendering-overhaul/05-04-SUMMARY.md` when done.
</output>
