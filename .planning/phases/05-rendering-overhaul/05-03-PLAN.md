---
phase: 05-rendering-overhaul
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - Shaders/luminous_pass.gdshader
  - Scripts/Render/SkyboxRenderer.cs
  - Shaders/skybox.gdshader
  - Main.tscn
autonomous: false
requirements: [RND-05, RND-07]

must_haves:
  truths:
    - "Galaxies render in the post-process pass as a procedural disc placeholder when far, fading OUT as the ship approaches (resolving into the galaxy's individual stars) and fading IN by distance when leaving (D-03, headline vision)"
    - "The galaxy disc crossfade is driven by the descriptor's continuous galaxy_disc_weight (LuminousLod.GalaxyDiscWeight) — no discrete swap, no pop (RND-07)"
    - "SkyboxRenderer + skybox.gdshader + the Sky Environment wiring in Main.tscn are REMOVED entirely; the post-process pass is now the sole renderer of distant points and galaxy discs (D-07)"
    - "Galaxies are visible and approachable from Universe space (closes P2, D-09); galaxy-space star findability (P1) remains intact from Plan 2"
    - "The D-59 galaxy tilt-foreshortening (TILT_FLOOR) and the d>0.0 antipodal-ghost gate are preserved verbatim in luminous_pass.gdshader"
  artifacts:
    - path: "Shaders/luminous_pass.gdshader"
      provides: "Full galaxy disc crossfade branch (spiral/elliptical/tilt) ported from skybox.gdshader"
      contains: "galaxy_disc_weights"
  key_links:
    - from: "Shaders/luminous_pass.gdshader"
      to: "Shaders/skybox.gdshader (ported)"
      via: "galaxy_disc_coords_tilted / spiral_galaxy / elliptical_galaxy copied verbatim before skybox.gdshader is deleted"
      pattern: "galaxy_disc_coords_tilted"
    - from: "Main.tscn"
      to: "(removed Sky Environment + SkyboxRenderer node)"
      via: "Sky ShaderMaterial / Environment and SkyboxRenderer node deleted"
      pattern: "SkyboxRenderer"
  prohibitions:
    - "Must NOT remove the d>0.0 antipodal-ghost gate or the TILT_FLOOR=0.15 clamp when porting the galaxy math (Pitfall 4 / T-05-03 landmine — causes sky-spanning ring or disc collapse)."
    - "Must NOT delete skybox.gdshader BEFORE its galaxy disc functions are copied verbatim into luminous_pass.gdshader and verified (Pitfall 6)."
    - "Must NOT use a discrete SOI swap for the galaxy disc↔stars crossfade — use the continuous galaxy_disc_weight (D-03)."
    - "Must NOT reintroduce a galaxy MESH in WorldRenderer — galaxies are post-process only; the WorldRenderer galaxy skip guard stays (D-28 / T-03-06)."
    - "Must NOT form absolute-from-root metres then subtract — galaxy direction stays on the UniMath LCA path from the Plan-1 builder."
---

<objective>
Complete the galaxy half of the luminous vision: port the procedural galaxy-disc math (spiral/elliptical/tilt) verbatim from `skybox.gdshader` into `luminous_pass.gdshader`, driven by the descriptor's continuous `galaxy_disc_weight`. A far galaxy renders as a glowing disc placeholder; approaching, the disc fades out as its individual stars take over; leaving, the disc fades back in by distance. Once the post-process pass carries everything the skybox drew, REMOVE `SkyboxRenderer` + `skybox.gdshader` + the Sky `Environment` wiring in Main.tscn entirely. This closes P1 + P2 (D-09). Play-test intergalactic / Universe space.

Purpose: Deliver the headline "galaxies are made of stars" distance crossfade and retire the Sky-shader skybox — the post-process pass becomes the single source of distant-body rendering (D-07).
Output: Full galaxy crossfade in luminous_pass.gdshader; SkyboxRenderer + skybox.gdshader + Sky Environment deleted; P1/P2 closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rendering-overhaul/05-CONTEXT.md
@.planning/phases/05-rendering-overhaul/05-RESEARCH.md
@.planning/phases/05-rendering-overhaul/05-PATTERNS.md
@.planning/phases/05-rendering-overhaul/05-02-SUMMARY.md
@.planning/todos/pending/galaxy-space-star-meshes-invisible.md
@.planning/todos/pending/galaxy-visibility-in-universe-space.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (05-03) completes the galaxy branch of `Shaders/luminous_pass.gdshader` and REMOVES `Scripts/Render/SkyboxRenderer.cs` + `Shaders/skybox.gdshader` + the Main.tscn Sky Environment/ShaderMaterial wiring (D-07). The descriptor pipeline (Plan 1) and the star pass (Plan 2) are unchanged. `WorldRenderer._lastRenderPositions` cleanup remains Plan 4.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Port full galaxy disc crossfade into luminous_pass.gdshader</name>
  <files>Shaders/luminous_pass.gdshader</files>
  <read_first>
    - .planning/phases/05-rendering-overhaul/05-PATTERNS.md (luminous_pass.gdshader galaxy loop lines 393-429; Shared Patterns — TILT_FLOOR lines 741-749, d>0.0 gate lines 754-763; critical invariants lines 779-785)
    - .planning/phases/05-rendering-overhaul/05-RESEARCH.md (Pattern 3 galaxy branch lines 319-340; Don't Hand-Roll galaxy disc row lines 395; Pitfall 4 antinode ghost lines 435-443; Open Question 3 lines 608-611)
    - Shaders/skybox.gdshader (galaxy_disc_coords_tilted, spiral_galaxy, elliptical_galaxy functions + GALAXY_LOD_THRESHOLD / GALAXY_DISC_SCALE / TILT_FLOOR / detail_blend constants + the d>0.0 gate — these are copied verbatim)
    - Shaders/luminous_pass.gdshader (the Plan-2 shader with the galaxy stub to replace)
  </read_first>
  <action>
    Copy verbatim from `skybox.gdshader` into `luminous_pass.gdshader` (do NOT rewrite — D-59 tilt + TILT_FLOOR anti-collapse are already solved): the helper functions `galaxy_disc_coords_tilted()`, `spiral_galaxy()`, `elliptical_galaxy()`, and the constants `GALAXY_LOD_THRESHOLD = 2e-4`, `GALAXY_DISC_SCALE = 80.0`, `TILT_FLOOR = 0.15`, `detail_blend = 0.35`. Replace the Plan-2 galaxy stub with the full galaxy loop per 05-PATTERNS.md lines 393-429: skip when `galaxy_disc_weights[i] < 0.001` (inside galaxy — no disc); compute `d = dot(normalize(EYEDIR), galaxy_dirs[i])` and `point_disc = smoothstep(1.0 - size, 1.0, d)`; if `size < GALAXY_LOD_THRESHOLD` render the point-disc; ELSE IF `d > 0.0` (CRITICAL antipodal-ghost gate — Pitfall 4) derive UV via `galaxy_disc_coords_tilted(EYEDIR, galaxy_dirs[i], disc_nrm)` divided by `max(size * GALAXY_DISC_SCALE, 0.001)`, pick `spiral_galaxy(uv, seed)` (type 0) or `elliptical_galaxy(uv)` (type 1), apply the grain/detail_blend mix, and `galaxy_bright = mix(disc_bright, point_disc, 0.3) * galaxy_colors[i].a`; ELSE `galaxy_bright = 0.0`. Multiply the whole contribution by `galaxy_disc_weights[i]` so the disc fades continuously by distance (the disc↔stars crossfade — the Plan-2 star points provide the "resolves into stars" side). `GALAXY_DISC_SCALE` is an [ASSUMED] recalibration knob — note it may need play-test tuning in the new context (RESEARCH Open Question 3). Keep the depth/sky_pixel gating consistent with the star branch where appropriate. Do not touch the star loop from Plan 2.
  </action>
  <acceptance_criteria>
    - Source assertion: `Shaders/luminous_pass.gdshader` contains `galaxy_disc_coords_tilted`, `spiral_galaxy`, `elliptical_galaxy`, the `TILT_FLOOR` constant, and the `d > 0.0` antipodal-ghost gate.
    - Source assertion: the galaxy loop multiplies its contribution by `galaxy_disc_weights[i]` (continuous crossfade, not a discrete swap).
    - Source assertion: `Shaders/skybox.gdshader` still EXISTS at this task's completion (it is deleted only in Task 2, after the port is verified).
    - Test-command: project opens in Godot with no shader-compile error (verified at the play-test gate, Task 3).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "galaxy_disc_coords_tilted\|TILT_FLOOR\|d > 0.0" C:/Users/frede/workspace/godot/eco-space/Shaders/luminous_pass.gdshader</automated>
  </verify>
  <done>luminous_pass.gdshader carries the full galaxy disc crossfade (spiral/elliptical/tilt copied verbatim, antipodal gate + TILT_FLOOR preserved) driven by continuous galaxy_disc_weight; skybox.gdshader still present until Task 2 verifies the port.</done>
</task>

<task type="auto">
  <name>Task 2: Remove SkyboxRenderer + skybox.gdshader + Sky Environment wiring</name>
  <files>Scripts/Render/SkyboxRenderer.cs, Shaders/skybox.gdshader, Main.tscn</files>
  <read_first>
    - .planning/phases/05-rendering-overhaul/05-RESEARCH.md (State of the Art — deprecated files lines 567-578; Pitfall 6 lines 455-463)
    - .planning/phases/05-rendering-overhaul/05-PATTERNS.md (critical invariant #3 lines 782)
    - Scripts/Render/SkyboxRenderer.cs (the file being deleted — confirm no OTHER live code references its public API beyond Main.tscn before deleting)
    - Main.tscn (the Sky Environment / ShaderMaterial node + the SkyboxRenderer node wiring to remove; confirm Camera3D, WorldEnvironment glow, the dither CanvasLayer, and the Plan-1/2 nodes stay)
    - Scripts/Render/WorldRenderer.cs (confirm the galaxy skip guard `body.ObjectType == UniObject.Type.Galaxy` stays — galaxies remain post-process only)
  </read_first>
  <action>
    Delete `Scripts/Render/SkyboxRenderer.cs` (and its `.uid` sidecar). Delete `Shaders/skybox.gdshader` (and its `.uid`). In `Main.tscn` (use `Edit`, scoped removals — never a full Write): remove the `SkyboxRenderer` node, and remove the Sky `Environment` / Sky `ShaderMaterial` wiring that hosted `skybox.gdshader` (the `WorldEnvironment` may remain for GLOW, but its `sky`/`background_mode = Sky` reference to the deleted Sky ShaderMaterial must be removed or repointed to a plain clear color so the scene still loads). Remove any `ext_resource` lines pointing at the deleted SkyboxRenderer.cs / skybox.gdshader. Verify nothing else in the codebase references `SkyboxRenderer` (no dangling `GetNode<SkyboxRenderer>` / `new SkyboxRenderer`); if any consumer exists, repoint it to the descriptor builder or remove the dead reference. Do NOT touch WorldRenderer's galaxy skip guard, the dither pass, the Plan-1 builder, or the Plan-2 luminous pass. Confirm a clean `dotnet build` (no missing-type errors from the deletion).
  </action>
  <acceptance_criteria>
    - Source assertion: `Scripts/Render/SkyboxRenderer.cs` and `Shaders/skybox.gdshader` no longer exist.
    - Source assertion: `Main.tscn` no longer references `SkyboxRenderer` or `skybox.gdshader`, and the scene still loads (no broken ext_resource).
    - Source assertion: no remaining `.cs` file references the type `SkyboxRenderer`.
    - Test-command: `dotnet build EcoSpace.csproj` exits 0; `dotnet test EcoSpace.Tests/EcoSpace.Tests.csproj` exits 0 (no regression).
  </acceptance_criteria>
  <verify>
    <automated>test ! -f C:/Users/frede/workspace/godot/eco-space/Scripts/Render/SkyboxRenderer.cs && test ! -f C:/Users/frede/workspace/godot/eco-space/Shaders/skybox.gdshader && dotnet build C:/Users/frede/workspace/godot/eco-space/EcoSpace.csproj</automated>
  </verify>
  <done>SkyboxRenderer + skybox.gdshader + the Sky Environment wiring are deleted; no dangling references; scene loads; build + tests clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Play-test gate — galaxy crossfade + P2 visibility + skybox removal clean (Universe/Galaxy space)</name>
  <action>Human play-test gate per D-08 Plan 3. The executor pauses and the user verifies in-engine per the steps below; no code is written in this task.</action>
  <what-built>The full galaxy distance crossfade in the post-process pass (disc fades out approaching → resolves into stars; fades in leaving) with the Sky-shader skybox removed entirely. Closes P2 (Universe-space galaxy visibility) and confirms P1 still holds.</what-built>
  <how-to-verify>
    1. Run Main in Godot 4.6.2. Confirm a CLEAN build and NO shader-compile error, and that the scene still loads after the skybox/Sky-Environment removal (no broken ext_resource, no pure-black missing-renderer hole).
    2. Universe space (P2 closure): fly out to intergalactic / Universe space — confirm the 3 authored galaxies are VISIBLE and approachable as glowing discs (previously they vanished). Confirm each disc shows its spiral/elliptical character and is tilt-foreshortened at an angle (D-59 / TILT_FLOOR preserved — a galaxy seen at ~45° looks tilted, not face-on and not collapsed to a line).
    3. No antipodal ghost (Pitfall 4): confirm each galaxy disc appears only in its real direction — there is NO mirrored sky-spanning ring/ghost on the opposite side.
    4. Approach crossfade: fly from Universe space toward a galaxy and into its SOI — confirm the disc fades OUT continuously as the galaxy's individual stars take over (no abrupt pop/swap). Fly back out — confirm the disc fades IN by distance and blends with the outgoing galaxy's still-visible stars when you turn to look (the headline crossfade).
    5. Regression: confirm Star space and Galaxy space still look correct (star glow/points + P1 findability intact) and the home galaxy is still suppressed while inside its SOI.
    6. Note any GALAXY_DISC_SCALE recalibration needed (RESEARCH Open Question 3) — the disc may render too large/small in the new context.
  </how-to-verify>
  <resume-signal>Type "approved" if galaxies are visible/approachable in Universe space (P2 closed), the disc↔stars crossfade is continuous with no pop or antipodal ghost, tilt is preserved, and the skybox removal left no artifacts; otherwise describe the issue (note any disc-scale/threshold tuning) before Plan 4.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none external) | Single-player, offline, local desktop space-sim. No network, no untrusted input, no auth, no persistence, no external services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Galaxy tilt collapse if TILT_FLOOR removed | mitigate | TILT_FLOOR=0.15 clamp copied verbatim and preserved; play-test gate verifies tilt is intact (landmine flagged in must_haves.prohibitions) |
| T-05-05 | Denial of Service | Galaxy disc antipodal ghost / sky-spanning ring | mitigate | `d > 0.0` front-hemisphere gate copied verbatim from skybox.gdshader; play-test confirms no ghost |
| T-05-06 | Information Disclosure | (n/a) | accept | No data exposure surface in a local rendering pass |

Posture: local/offline/no-untrusted-input. No high-severity threats; ASVS L1 (block-on high) passes trivially. No package installs — no supply-chain threat. File deletions are codebase-internal (no external dependency change).
</threat_model>

<verification>
- `dotnet build EcoSpace.csproj` exits 0; `dotnet test` exits 0 (no regression).
- SkyboxRenderer + skybox.gdshader removed; no dangling references; scene loads (automated + play-test).
- Galaxies visible/approachable in Universe space (P2); continuous disc↔stars crossfade with no pop or antipodal ghost; tilt preserved (P1 intact) — human play-test in Universe + Galaxy space.
</verification>

<success_criteria>
- Galaxy distance crossfade (disc ↔ resolved stars) is continuous and driven by the shared descriptor's galaxy_disc_weight — no pop (RND-07, criterion #2).
- P1 + P2 closed at this gate (D-09); the Sky-shader skybox is removed and the post-process pass is the sole distant-body renderer (D-07).
- No regression in Star/Galaxy space; tilt + antipodal gate preserved (D-04).
</success_criteria>

<output>
Create `.planning/phases/05-rendering-overhaul/05-03-SUMMARY.md` when done.
</output>