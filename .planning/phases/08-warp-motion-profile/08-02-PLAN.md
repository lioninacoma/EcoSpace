---
phase: 08-warp-motion-profile
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - Scripts/Math/UniMath.cs
  - Scripts/Flight/FlightController.cs
  - Scripts/Hud/WarpConfirmationScreen.cs
autonomous: true
requirements:
  - WR-01
  - WR-04
  - D-12
  - D-13
  - D-15

must_haves:
  truths:
    - "UniMath.NormalizedDirection no longer rejects large real separations as coincident; its threshold is correct for integer-unit space and its comment is accurate (WR-01)."
    - "Open-space manual top speed reaches the tier ceiling instead of half of it (WR-04)."
    - "The warp confirmation screen shows the peak cruise speed v_c (not a capped dist/time figure) and applies no travel-time clamp."
  artifacts:
    - path: "Scripts/Math/UniMath.cs"
      provides: "Corrected NormalizedDirection coincidence threshold + accurate comment."
      contains: "NormalizedDirection"
    - path: "Scripts/Hud/WarpConfirmationScreen.cs"
      provides: "Peak cruise-speed v_c display using the same closed-form as the profile."
      contains: "WarpAccelFraction"
  key_links:
    - from: "Scripts/Hud/WarpConfirmationScreen.cs"
      to: "Scripts/Flight/FlightController.cs"
      via: "reads WarpAccelFraction to compute v_c = (dist − SOIMeters) / (T_sel·(1−f))"
      pattern: "WarpAccelFraction"
    - from: "Scripts/Flight/FlightController.cs"
      to: "Scripts/Math/UniMath.cs"
      via: "UpdateSpeedEnvelope open-space sentinel resolves to tierCeiling"
      pattern: "tierCeiling / "
---

<objective>
Fold the Phase-7 review cleanups WR-01 (D-13) and WR-04 (D-15) into the warp/flight code,
and update the warp confirmation screen (D-12) to display the new profile's **peak cruise
speed `v_c`** instead of the old capped `dist/time` figure. These are the dependent
follow-ons that complete Phase 8 once the core profile + export contract land in 08-01.

Purpose: WR-01 directly affects warp auto-orient direction; WR-04 fixes a manual-flight
open-space speed bug; D-12 makes the confirmation screen consistent with the new
varying-speed trapezoid (and drops the now-removed WarpMaxSpeed reference).
Output: A corrected `UniMath.NormalizedDirection` threshold/comment, a fixed
`UpdateSpeedEnvelope` open-space sentinel, and a `v_c` peak-speed line on
`WarpConfirmationScreen`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-warp-motion-profile/08-CONTEXT.md
@.planning/phases/08-warp-motion-profile/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**Modified symbols (this plan):**
- `UniMath.NormalizedDirection` — coincidence threshold corrected from the unit-space `mag < 1e-3` to the intended integer-unit coincidence cutoff; signature unchanged (all callers unaffected); stale "in metres" comment rewritten (D-13 / WR-01).
- `FlightController.UpdateSpeedEnvelope` — open-space sentinel branch changed so open-space `targetMax` resolves to `tierCeiling` (not `tierCeiling × 0.5`) (D-15 / WR-04).
- `WarpConfirmationScreen.RefreshDisplay` — the speed line now shows peak `v_c = (dist − target.SOIMeters) / (T_sel·(1−f))`, reading `WarpAccelFraction` from `FlightController`; the `WarpMaxSpeed` reference and the dist/time cap are removed; no travel-time clamp added (D-12).

**No new symbols.** This plan only modifies existing methods. It depends on 08-01 having
added `WarpAccelFraction` and removed `WarpMaxSpeed` from `FlightController`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 NormalizedDirection threshold + WR-04 open-space sentinel</name>
  <files>Scripts/Math/UniMath.cs, Scripts/Flight/FlightController.cs</files>
  <read_first>
    - Scripts/Math/UniMath.cs (lines 200-234) — `NormalizedDirection`; `u = rel.ToDouble3Units()` is INTEGER UNITS (Long3), not metres; the `mag < 1e-3` threshold and "in metres" comment are both wrong (WR-01 / D-13). Re-read live before editing.
    - Scripts/Flight/FlightController.cs (`UpdateSpeedEnvelope`, around the open-space sentinel — was line 652-653, and the `targetMax = Mathf.Clamp(nearest * _speedPerMeter, ...)` at ~660). Verified at plan time: sentinel at 652-653, targetMax at 660 — RE-READ before editing.
    - CLAUDE.md §"Position Math (UniVec3 / UniMath)" — UniMath read-only contract; signature must not change.
    - .planning/phases/08-warp-motion-profile/08-CONTEXT.md (D-13, D-15) and 08-PATTERNS.md sections 6 + "UniMath.cs (WR-01 only)".
  </read_first>
  <action>
    **WR-01 (D-13), `UniMath.NormalizedDirection`:** `u` is `rel.ToDouble3Units()` = integer Units, where the smallest non-zero magnitude is 1.0 (not a metric epsilon). Correct the `mag < 1e-3` coincidence threshold to the intended unit-space cutoff (a value just below 1.0 unit, e.g. treat `mag < 0.5` as coincident — pick the cutoff that means "rounds to zero units"), and rewrite the stale comment that claims the fallback is "in metres" — it is in integer Units. Keep the method signature and the read-only contract unchanged so callers (`_WarpProcess`, `LuminousDescriptorBuilder`) are unaffected. Touch only `NormalizedDirection` (around lines 225-234).
    **WR-04 (D-15), `UpdateSpeedEnvelope` open-space sentinel:** the bug is that at default `SpeedPerMeter = 0.5`, `nearest = tierCeiling / System.Math.Max(_speedPerMeter, 1.0)` divides by 1.0, then `targetMax = Mathf.Clamp(nearest * _speedPerMeter, ...)` multiplies by 0.5 → open-space target lands at `tierCeiling × 0.5`. Change the sentinel so that `nearest * _speedPerMeter` resolves to `tierCeiling`: set `nearest = tierCeiling / _speedPerMeter` (guard `_speedPerMeter > 0` with a small floor, e.g. `System.Math.Max(_speedPerMeter, <small-epsilon>)`). Touch ONLY this open-space branch — do not alter the proximity-damp math or the target ease-out below it. This roughly doubles open-space manual top speed (a deliberate feel change per D-15; SpeedPerMeter/tier-factor re-tune is expected during play-test, out of scope here).
  </action>
  <verify>
    <automated>dotnet test EcoSpace.Tests/EcoSpace.Tests.csproj --filter "FullyQualifiedName~UniMath"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EcoSpace.csproj` succeeds with 0 errors; existing `UniMath` tests stay green.
    - `NormalizedDirection`'s threshold is a unit-space coincidence cutoff (not `1e-3`), the "in metres" comment is corrected to say integer Units, and the method signature is unchanged.
    - The `UpdateSpeedEnvelope` open-space sentinel makes `nearest * _speedPerMeter` evaluate to `tierCeiling` (open-space `targetMax` = `tierCeiling`, not `tierCeiling × 0.5`), guarded against `_speedPerMeter ≤ 0`.
    - Only the open-space sentinel branch is changed; proximity-damp and target-ease-out math are untouched.
  </acceptance_criteria>
  <done>NormalizedDirection no longer mis-rejects large real separations as coincident and its comment is accurate; open-space manual top speed reaches tierCeiling; build clean and UniMath tests green.</done>
</task>

<task type="auto">
  <name>Task 2: WarpConfirmationScreen peak cruise-speed v_c display (D-12)</name>
  <files>Scripts/Hud/WarpConfirmationScreen.cs</files>
  <read_first>
    - Scripts/Hud/WarpConfirmationScreen.cs (`RefreshDisplay`, lines 200-235) — the existing speed line `double warpSpeed = System.Math.Min(dist / _selectedTravelTimeSec, _flight?.WarpMaxSpeed ?? 2e20);` references `WarpMaxSpeed`, which 08-01 REMOVED — this line must change or the build breaks. Re-read live before editing.
    - Scripts/Hud/Hud.cs (`FormatSpeed`, lines 464-471) — the adaptive m/s→km/s→AU/s→ly/s formatter to reuse.
    - Scripts/Flight/FlightController.cs — confirm `WarpAccelFraction` exists (added in 08-01) and `WarpMaxSpeed` is gone.
    - .planning/phases/08-warp-motion-profile/08-CONTEXT.md (D-12, D-16) and 08-PATTERNS.md "WarpConfirmationScreen.cs (D-12)".
  </read_first>
  <action>
    In `RefreshDisplay`, replace the existing capped `dist/time` speed line (which references the now-removed `_flight?.WarpMaxSpeed`) with the **peak cruise speed `v_c`**: compute `profileDist = System.Math.Max(0.0, dist - target.SOIMeters)`, read `f = _flight?.WarpAccelFraction ?? (1.0 / 3.0)`, then `vc = profileDist / (_selectedTravelTimeSec * (1.0 - f))` (guard the divisor — the slider MinValue already keeps `_selectedTravelTimeSec ≥ 5 s`, but guard `(1 - f)` against 0 with the same `Max` idiom). Display via `Hud.FormatSpeed(vc)` (e.g. `_speedLabel.Text = $"WARP  {Hud.FormatSpeed(vc)}"`). Remove the `WarpMaxSpeed` reference and the `Min(...)` cap entirely (D-05/D-12); add NO travel-time clamp (D-12 — the cap is dropped, so the time input is honored as-is). Preserve the read-only HUD contract (D-53 — no sim mutation) and the phosphor-green `AddRow` styling (D-16); reuse the existing `UniMath.Distance(ship, target, gameObjects)` (line ~222) and bounds-safe ship/target lookups (lines ~209-215). Update the stale comment that cites D-15/D-07 capping.
  </action>
  <verify>
    <automated>dotnet build EcoSpace.csproj 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EcoSpace.csproj` succeeds with 0 errors (the removed `WarpMaxSpeed` reference no longer breaks the build).
    - `WarpConfirmationScreen.cs` no longer references `WarpMaxSpeed`; it reads `WarpAccelFraction` and computes `vc = (dist − target.SOIMeters) / (T_sel·(1−f))`.
    - No travel-time clamp/floor is added in the speed-display path; the divisor `(1−f)` is guarded.
    - The speed label uses `Hud.FormatSpeed(vc)`; read-only contract and phosphor-green styling preserved.
  </acceptance_criteria>
  <done>The confirmation screen shows the peak cruise speed v_c consistent with the profile's closed form, the WarpMaxSpeed reference is gone, no time clamp is applied, and the project builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same as plan 08-01: EcoSpace is a single-player, offline desktop sim with **no network,
auth, or persisted-data surface**. The only boundary is editor-exported tuning values →
runtime math. No untrusted-input boundary exists in this phase.

| Boundary | Description |
|----------|-------------|
| editor `[Export]` value → display/flight math | Developer-supplied `WarpAccelFraction` / `SpeedPerMeter` could be out-of-range |

## STRIDE Threat Register

Numerical-robustness correctness guards only — no high-severity (network/auth/data) threats.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service (div-by-zero) | `WarpConfirmationScreen` v_c computation | mitigate | Slider MinValue keeps `T_sel ≥ 5 s`; guard `(1 − f)` divisor; `WarpAccelFraction` is clamped to `[0,0.5]` at its export (08-01). |
| T-08-05 | Denial of Service (div-by-zero) | `UpdateSpeedEnvelope` open-space sentinel | mitigate | Guard `_speedPerMeter` with a small-floor `System.Math.Max` before dividing `tierCeiling / _speedPerMeter`. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (existing .NET 8 / Godot deps only); nothing to verify. |
</threat_model>

<verification>
- `dotnet build EcoSpace.csproj` → 0 errors (proves the removed `WarpMaxSpeed` reference is gone from the HUD).
- `dotnet test EcoSpace.Tests/EcoSpace.Tests.csproj` → all tests green.
- Grep confirms `WarpMaxSpeed` is absent from `WarpConfirmationScreen.cs` and `WarpAccelFraction` is present.
- Grep confirms `NormalizedDirection` no longer uses `mag < 1e-3` and the open-space sentinel divides by `_speedPerMeter` (not `Max(_speedPerMeter, 1.0)`).
</verification>

<success_criteria>
- WR-01 fixed: NormalizedDirection threshold is unit-space-correct with an accurate comment; callers unaffected.
- WR-04 fixed: open-space manual top speed reaches `tierCeiling`.
- D-12 done: confirmation screen shows peak `v_c` with no time clamp and no WarpMaxSpeed reference.
- Project compiles under Godot 4.6.2 Mono / .NET 8 with 0 errors.
</success_criteria>

<output>
Create `.planning/phases/08-warp-motion-profile/08-02-SUMMARY.md` when done.
</output>
