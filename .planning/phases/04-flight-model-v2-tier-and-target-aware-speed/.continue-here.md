---
context: phase
phase: 04-flight-model-v2-tier-and-target-aware-speed
task: 5
total_tasks: 8
status: in_progress
last_updated: 2026-06-17T15:20:25.638Z
---

# Paused mid `/gsd-plan-phase 04`

Planning pipeline for Phase 4 (Flight Model v2 — tier & target-aware speed). Research
and pattern-mapping are done; the **planner has not run yet**. No source code touched.

<current_state>
In the gsd-plan-phase workflow, between pattern-mapping (done) and the gsd-planner spawn.
- `04-RESEARCH.md` — written + committed (c90bf6d), HIGH confidence
- `04-PATTERNS.md` — written by pattern-mapper, **on disk but uncommitted** at pause
- No PLAN.md files exist yet. Working tree otherwise clean.
</current_state>

## Two locked rulings (re-apply on resume — do NOT re-litigate)

1. **Cross-SOI scope → follow CONTEXT D-45, not ROADMAP SC#5.**
   ROADMAP Success Criterion #5 says "select a target in another SOI (overriding D-12)".
   CONTEXT.md D-45 (newer, from discuss-phase) deliberately narrows this: targeting stays
   **current-tier**, D-12 is **preserved**, true cross-SOI targeting stays in **backlog 999.1**.
   The world-pinned target **circle (D-46) IS in scope**. The planner must NOT plan cross-SOI
   target selection. (Follow-up: ROADMAP SC#5 should be amended to match.)
2. **Skip the UI-SPEC gate** — it's a false positive. The ui-safety-gate fired on
   HUD/circle/marker keywords, but this is a Godot game HUD whose visual contract is already
   locked in CONTEXT D-46. Do not run /gsd-ui-phase; proceed straight to the planner.

<completed_work>
- Task 1: Init + load CONTEXT.md (exists) — Done
- Task 2: Research decision (chose "research first") — Done
- Task 3: gsd-phase-researcher → 04-RESEARCH.md — Done (committed c90bf6d)
- Task 4: gsd-pattern-mapper → 04-PATTERNS.md — Done (uncommitted on disk)
</completed_work>

<remaining_work>
- Task 5: Spawn gsd-planner (opus) with CONTEXT + RESEARCH + PATTERNS → PLAN.md files — Not started
- Task 6: gsd-plan-checker (sonnet) + revision loop (max 3) — Not started
- Task 7: Requirements coverage (§13) + Decision coverage gate (§13a) — Not started
- Task 8: state.planned-phase, ROADMAP wave annotations, commit plans (§13b–d) — Not started
</remaining_work>

<decisions_made>
- Follow CONTEXT D-45 over ROADMAP SC#5 (cross-SOI deferred to 999.1; target circle in scope)
- Research first (not skipped)
- Skip UI-SPEC gate (game HUD, design already in D-46)
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-flight-model-v2-tier-and-target-aware-speed/04-CONTEXT.md` — locked decisions D-40..D-47 (authoritative scope)
2. `.planning/phases/04-flight-model-v2-tier-and-target-aware-speed/04-RESEARCH.md` — HIGH-confidence implementation findings
3. `.planning/phases/04-flight-model-v2-tier-and-target-aware-speed/04-PATTERNS.md` — concrete code excerpts of the 5 analog sites
4. `CLAUDE.md` §"Position Math (UniVec3 / UniMath)" — all cross-frame distance MUST route through UniMath LCA helpers; never raw absolute-from-root ToDouble3 accumulation

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN] The reverted 260617-j6b direction-aware clamp: exempting the proximity clamp to the GLOBAL max made in-system flight unusable. → Fix here is a **per-tier** ceiling so there is no global value to jump to; the proximity damp is **symmetric** and "receding" returns to the **tier ceiling**, never the global intergalactic max.
- [ANTI-PATTERN] Raw `ToDouble3()` absolute-from-root accumulation for cross-frame distance. → Use `UniMath.Distance` / `RelativeMetres` (LCA path) for the target-distance term.
- [ANTI-PATTERN] HUD mutating sim state. → `Hud.ActiveTargetIndex` is a **read-only** getter; FlightController reads it, never writes back.

## Infrastructure State
- No running services. Verification of this phase requires an **in-game Godot play-test** (speed feel + SOI behavior are not unit-testable).

## Research highlights (so the planner doesn't re-derive)
- **No new files** — three existing files change: `Scripts/Flight/FlightController.cs`, `Scripts/Hud/Hud.cs`; `Scripts/Render/WorldRenderer.cs` read-only.
- Tier ceiling source = `parent.SOIMeters` for the ship's current `Space` × `k` (new `[Export] TierSpeedFactor`). Guard Root sentinel `double.MaxValue` with `< double.MaxValue/2.0`.
- Envelope reshape is essentially one clamp change (FlightController.cs ~line 434: `_maxSpeed` → `tierCeiling`); keep both lerps on every path.
- Target ease-out: `v → min(tierCeiling, distToTarget × k')` eased; `k'` = new `[Export] SpeedPerTarget`.
- Target circle: `Hud._Draw`, gated on `WorldRenderer.GetRenderPosition`; reuse `UpdateDirectionMarker` camera-projection (`UnprojectPosition`, `cameraLocal.Z>0` guard); `QueueRedraw()` in `_Process`. Verify Godot 4.6 `DrawArc` API on first build (only MEDIUM-confidence item).

<context>
The plan-phase orchestrator was running interactively. After the operator said "stop", then
"/gsd-pause-work", I halted before spawning the planner. RESEARCH.md and PATTERNS.md together
already give the planner a near-complete map; the planner spawn is the next real action.
</context>

<next_action>
Start with: re-run `/gsd-plan-phase 04`. Choose "use existing research" when prompted, re-apply
the two locked rulings above, then let it spawn the gsd-planner (opus). Optionally commit
04-PATTERNS.md first (it's uncommitted) — though plan-phase will commit it alongside the plans.
</next_action>
