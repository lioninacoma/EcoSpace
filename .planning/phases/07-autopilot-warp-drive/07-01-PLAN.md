---
phase: 07-autopilot-warp-drive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - project.godot
  - Scripts/Flight/FlightController.cs
autonomous: true
requirements: [WARP-01, WARP-02, WARP-03, LOOK-01, SPEED-01]
must_haves:
  truths:
    - "Manual throttle never exceeds ManualMaxSpeed (1e6 m/s) regardless of tier or target"
    - "Holding Left Alt lets the player look around without changing ship heading; view eases back to heading on release"
    - "Pressing J with an active target begins warp; the ship auto-orients toward the target and flies at distance/time speed"
    - "Warp auto-disengages when the ship enters the target's SOI, easing speed down to ManualMaxSpeed"
    - "During warp, throttle and steering are ignored; only look-around responds"
  artifacts:
    - path: "project.godot"
      provides: "warp_engage (J) and look_around (Left Alt) InputMap actions"
      contains: "warp_engage"
    - path: "Scripts/Flight/FlightController.cs"
      provides: "WarpState machine, ManualMaxSpeed/WarpMaxSpeed exports, look-around camera offset, EngageWarp/DisengageWarp/_WarpProcess"
      contains: "enum WarpState"
  key_links:
    - from: "FlightController._WarpProcess"
      to: "UniMath.Distance / UniMath.RelativeMetres"
      via: "cross-frame distance + orient direction"
      pattern: "UniMath\\.(Distance|RelativeMetres)"
    - from: "FlightController.EngageWarp"
      to: "WarpState.Warping"
      via: "public API called by WarpConfirmationScreen"
      pattern: "WarpState\\.Warping"
---

<objective>
Add the FlightController warp state machine, the manual speed cap, and the look-around camera — plus the two InputMap actions that drive them. This plan delivers all flight-logic and input-binding changes for Phase 7; the warp UI panel (Plan 02) consumes the public `EngageWarp`/`IsPanelOpen` contract created here.

Purpose: Phase 7 splits flight into a slow, precise manual path (capped at km/s) and an autopilot-only warp path (FTL-equivalent), and decouples camera view from ship heading. All of that lives in FlightController.
Output: `project.godot` gains `warp_engage` (J) and `look_around` (Left Alt); `FlightController.cs` gains `WarpState`, `ManualMaxSpeed`/`WarpMaxSpeed`/`WarpOrientRate` exports, `_cameraOffset` look-around, `EngageWarp`/`DisengageWarp`/`_WarpProcess`, a `_Process` state switch, and a `ManualMaxSpeed` clamp.
</objective>

<execution_context>
@C:/Users/FSchubert/workspace/misc/godot/Projects/EcoSpace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/FSchubert/workspace/misc/godot/Projects/EcoSpace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-autopilot-warp-drive/07-CONTEXT.md
@.planning/phases/07-autopilot-warp-drive/07-RESEARCH.md
@.planning/phases/07-autopilot-warp-drive/07-PATTERNS.md
@Scripts/Flight/FlightController.cs
@Scripts/Math/UniMath.cs
@project.godot
</context>

<artifacts_this_phase_produces>
Symbols this phase creates (exclude from drift/source-grounding verification — they are NEW):

**project.godot InputMap actions:**
- `warp_engage` (physical_keycode 74 / J)
- `look_around` (physical_keycode 4194326 / Left Alt — VERIFY in Godot editor; see Task 1)

**FlightController.cs new symbols:**
- `private enum WarpState { Manual, Confirming, Warping }`
- `private WarpState _warpState`
- `public double ManualMaxSpeed` (export, backing `_manualMaxSpeed = 1e6`)
- `public double WarpMaxSpeed` (export, backing `_warpMaxSpeed = 2e20`)
- `public double WarpOrientRate` (export, backing `_warpOrientRate`)
- `private Basis _cameraOffset`
- `private float _lookEaseRate`
- `private double _selectedTravelTimeSec`
- `public void EngageWarp(double travelTimeSec)`
- `public void DisengageWarp()`
- `private void _WarpProcess(double delta)`
- `private void UpdateLookAround(double delta)` (look-around ease/accumulate, called in warp; folded into UpdateAttitude for manual)
- `public bool IsWarping` (read accessor for HUD/Plan-02 cosmetic use — optional, see Task 3)

Plan 02 adds `WarpConfirmationScreen` (new file) + Main.tscn node; not produced here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add warp_engage (J) and look_around (Left Alt) InputMap actions to project.godot</name>
  <files>project.godot</files>
  <read_first>
    - project.godot — read the full [input] section so the new entries match the exact serialization format of existing actions (toggle_target_panel / Tab is the closest analog).
    - .planning/phases/07-autopilot-warp-drive/07-PATTERNS.md §"project.godot" — exact entry strings for both new actions.
  </read_first>
  <action>
    Append two new actions to the [input] section of project.godot, matching the exact Object(InputEventKey,...) serialization of the existing `toggle_target_panel` entry.
    - `warp_engage`: single InputEventKey with `physical_keycode`:74, `unicode`:106 (J key — verified against existing pattern W=87/119, S=83/115, X=88/120). `deadzone`:0.5.
    - `look_around`: single InputEventKey with `physical_keycode`:4194326, `unicode`:0 (Left Alt — same unicode:0 convention as the Tab entry). `deadzone`:0.5.
    Per RESEARCH.md Assumption A2, the Left Alt physical key code (4194326) is the one risk. After adding it, the executor must confirm in the Godot editor's Input Map panel that `look_around` shows "Alt" as its bound key. If the editor shows a different/empty binding, rebind it via the editor (which rewrites the correct physical_keycode) and keep that value. Do NOT guess a second value — verify against the editor.
    No conflict exists: J and Left Alt are unused by any existing action (RESEARCH.md confirmed full [input] read).
  </action>
  <verify>
    <automated>grep -v '^#' project.godot | grep -c 'warp_engage\|look_around'</automated>
  </verify>
  <acceptance_criteria>
    - project.godot [input] section contains a `warp_engage` action with physical_keycode 74
    - project.godot [input] section contains a `look_around` action with physical_keycode 4194326 (or the editor-confirmed Left Alt code)
    - Both entries use the same Object(InputEventKey,...) field ordering as the existing toggle_target_panel entry
    - Godot editor Input Map panel shows `warp_engage` bound to J and `look_around` bound to Alt
  </acceptance_criteria>
  <done>Both actions exist in project.godot and resolve to J and Left Alt in the editor Input Map.</done>
</task>

<task type="auto">
  <name>Task 2: Add WarpState enum, speed-cap + warp exports, and the ManualMaxSpeed clamp</name>
  <files>Scripts/Flight/FlightController.cs</files>
  <read_first>
    - Scripts/Flight/FlightController.cs — read the full file. Note the existing export pattern (e.g. MaxSpeed/TierSpeedFactor getter/setter with `System.Math.Max(0.0, value)` guard), the `UpdateSpeedEnvelope` method (the `targetMax` computation ending with the `targetEaseMax` clamp and the two easing lerps), and the existing `_easedSpeed`/`_contextMax`/`CurrentSpeed` fields.
    - .planning/phases/07-autopilot-warp-drive/07-PATTERNS.md §"FlightController.cs" — "Existing export pattern", "ManualMaxSpeed clamp in UpdateSpeedEnvelope".
    - .planning/phases/07-autopilot-warp-drive/07-CONTEXT.md — D-07, D-09, D-10.
  </read_first>
  <action>
    In FlightController (namespace Flight), add the warp state machine type and the new exports, then wire the manual speed cap.
    1. Declare `private enum WarpState { Manual, Confirming, Warping }` and `private WarpState _warpState = WarpState.Manual;` alongside the other private flight-state fields.
    2. Add three `[Export]` properties following the EXACT existing export pattern (backing field + `System.Math.Max(0.0, value)` guard in the setter):
       - `ManualMaxSpeed` backing `_manualMaxSpeed = 1e6` (D-09: manual cap, 1,000 km/s).
       - `WarpMaxSpeed` backing `_warpMaxSpeed = 2e20` (D-07: warp safety cap; matches the old intergalactic MaxSpeed magnitude).
       - `WarpOrientRate` backing `_warpOrientRate = 1.5` (Slerp weight rate for auto-orient; tuning knob, Task 4).
       Keep the existing `MaxSpeed` export untouched — it remains the open-universe ceiling used inside the tier-ceiling math; do NOT delete or rename it. (The autopilot path uses `WarpMaxSpeed` as its own cap; manual uses `ManualMaxSpeed`.)
    3. In `UpdateSpeedEnvelope`, immediately AFTER the existing `targetMax = System.Math.Min(targetMax, targetEaseMax);` line (the target ease-out clamp) and BEFORE the `_contextMax = Mathf.Lerp(...)` easing lerp, insert:
       `if (_warpState == WarpState.Manual) targetMax = System.Math.Min(targetMax, _manualMaxSpeed);`
       This applies the manual cap only on the manual path (D-10). The tier ceiling (D-40) and proximity damp (D-42) computations stay unchanged above this line; the existing easing lerps run unchanged below it (D-07 invariant preserved — no early return inserted).
    Do NOT change `_Process` yet (Task 3 does the state switch). After this task, `_warpState` is always `Manual`, so behavior is identical except manual speed is now capped at 1e6 m/s.
  </action>
  <verify>
    <automated>cd "C:/Users/FSchubert/workspace/misc/godot/Projects/EcoSpace" && dotnet build EcoSpace.csproj -v quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FlightController.cs contains `enum WarpState { Manual, Confirming, Warping }`
    - FlightController.cs contains a `_warpState` field of type WarpState initialized to WarpState.Manual
    - FlightController.cs contains `[Export] public double ManualMaxSpeed` with `_manualMaxSpeed = 1e6` and a `System.Math.Max(0.0, value)` setter guard
    - FlightController.cs contains `[Export] public double WarpMaxSpeed` with `_warpMaxSpeed = 2e20`
    - FlightController.cs contains `[Export] public double WarpOrientRate`
    - UpdateSpeedEnvelope contains `if (_warpState == WarpState.Manual) targetMax = System.Math.Min(targetMax, _manualMaxSpeed);` placed after the targetEaseMax clamp and before the contextMax easing lerp
    - The existing MaxSpeed export is still present and unmodified
    - `dotnet build EcoSpace.csproj` succeeds with 0 errors
  </acceptance_criteria>
  <done>Build is clean; the enum + three exports exist; manual speed is clamped to ManualMaxSpeed only when _warpState == Manual.</done>
</task>

<task type="auto">
  <name>Task 3: Add the _Process state switch, EngageWarp/DisengageWarp, _WarpProcess (distance/time speed + SOI disengage), and look-around camera</name>
  <files>Scripts/Flight/FlightController.cs</files>
  <read_first>
    - Scripts/Flight/FlightController.cs — re-read `_Process`, `UpdateAttitude` (the `_camera.Basis = _shipBasis;` write and the `_shipBasis.Orthonormalized()` call), `ApplyMotion` (the `double.IsFinite(CurrentSpeed)` guard, the `forward = -_shipBasis.Z` motion, the `_world.TranslatePos(_world.ShipIndex, ...)` call), and the bounds-safe `(uint)idx < (uint)gameObjects.Count` lookup pattern in UpdateSpeedEnvelope.
    - Scripts/Math/UniMath.cs — `Distance(a, b, objs)` and `RelativeMetres(from, to, objs)` signatures (both take UniObject a/b and the GameObjects list, return double / Double3).
    - .planning/phases/07-autopilot-warp-drive/07-PATTERNS.md §"FlightController.cs" — "_Process routing pattern", "UpdateAttitude pattern", "Orthonormalize pattern", "Index bounds guard pattern", "UniMath.Distance usage pattern"; and §"Shared Patterns" (IsFinite guard, Cross-Frame Distance, Orthonormalize).
    - .planning/phases/07-autopilot-warp-drive/07-RESEARCH.md — Pattern 3 (_WarpProcess), Pattern 4 (Warp Auto-Orient Basis Slerp), Pattern 5 (Look-Around), and Pitfalls 1, 2, 4, 6.
    - .planning/phases/07-autopilot-warp-drive/07-CONTEXT.md — D-03, D-06, D-07, D-08, D-11, D-12, D-13, D-14, D-17, D-18, D-19.
  </read_first>
  <action>
    Implement the warp execution path and the look-around camera in FlightController.

    A) Look-around state + helper. Add fields `private Basis _cameraOffset = Basis.Identity;` and `private float _lookEaseRate = 1f / 0.3f;` (ease back over ~0.3 s, D-13). Add `private void UpdateLookAround(double delta)`:
       - If `Input.IsActionPressed("look_around")`: accumulate the steering deltas into `_cameraOffset` instead of `_shipBasis`. Reuse the same pitch/yaw composition the manual path uses, but multiply into `_cameraOffset` (build pitchBasis/yawBasis from `_cursor`-derived steer, multiply `_cameraOffset = (_cameraOffset * pitchBasis * yawBasis).Orthonormalized();`). Do NOT mutate `_shipBasis`. Suspend roll while looking around (do not apply Q/E roll to `_shipBasis`) per Pitfall 6.
       - Else (not held): ease `_cameraOffset` back toward `Basis.Identity` via Quaternion Slerp: `var t = Mathf.Clamp(_lookEaseRate * (float)delta, 0f, 1f); _cameraOffset = new Basis(new Quaternion(_cameraOffset).Normalized().Slerp(Quaternion.Identity, t)).Orthonormalized();`
       Note: in manual flight the player's heading IS `_shipBasis`; in warp `_shipBasis` is driven by the orient Slerp. In both cases the camera writes `_shipBasis * _cameraOffset` (below), so ease-back to identity naturally returns the view to ship heading / warp heading (D-13).

    B) UpdateAttitude (manual path) integration. In the manual branch, when `Input.IsActionPressed("look_around")` is true, route the mouse-derived pitch/yaw into `UpdateLookAround` (i.e. suspend `_shipBasis` rotation and roll, ship holds heading — D-12) instead of mutating `_shipBasis`; when false, run the existing steering exactly as today AND still call the ease-back branch of `UpdateLookAround` so the view returns to heading. Change the final camera write from `_camera.Basis = _shipBasis;` to `_camera.Basis = (_shipBasis * _cameraOffset).Orthonormalized();`. Keep the existing `_shipBasis = _shipBasis.Orthonormalized();` call.

    C) _Process state switch. Replace the body of `_Process` (after the `_world == null` / `delta <= 0.0` guards) with a switch on `_warpState`:
       - Change the panel-gate early return to `if (IsPanelOpen && _warpState == WarpState.Manual) return;` (Pitfall 3: warp must keep running while a panel could be open transiently).
       - `case WarpState.Manual:` → existing calls: HandleThrottleInput(); UpdateAttitude(delta); UpdateSpeedEnvelope(delta); ApplyMotion(delta); UpdateReticlePosition();
       - `case WarpState.Confirming:` → empty (WarpConfirmationScreen owns input; IsPanelOpen is true).
       - `case WarpState.Warping:` → UpdateLookAround(delta); _WarpProcess(delta); ApplyMotion(delta); and still write the camera as `_camera.Basis = (_shipBasis * _cameraOffset).Orthonormalized();` (either inside _WarpProcess after the orient Slerp, or via a shared camera-write — keep it consistent with the manual path). Throttle and steering-cursor accumulation are NOT called in warp (D-18, on rails).

    D) EngageWarp / DisengageWarp public API.
       - `public void EngageWarp(double travelTimeSec)`: set `_selectedTravelTimeSec = System.Math.Max(1.0, travelTimeSec);` (guard against 0/negative → div-by-zero, V5/Tampering), zero `_cursor` (Pitfall: stale steering delta on disengage), set `_warpState = WarpState.Warping;`. (IsPanelOpen is set false by the panel's ClosePanel before this is called — Pitfall 3.)
       - `public void DisengageWarp()`: set `_warpState = WarpState.Manual;`. Do NOT hard-stop — leave `_easedSpeed` as-is so UpdateSpeedEnvelope's existing lerp eases it down toward ManualMaxSpeed on subsequent manual frames (D-19). Add a runtime field `private double _selectedTravelTimeSec = 120.0;` (default 2 min, D-17, not persisted).

    E) _WarpProcess(double delta). Implement per RESEARCH Pattern 3 + 4:
       - Resolve `gameObjects = _world?.GameObjects`; bounds-guard ship via `_world.ShipIndex` and target via `_hud?.ActiveTargetIndex ?? -1` using the `(uint)idx < (uint)gameObjects.Count` pattern. If ship or target is null → `DisengageWarp(); return;`.
       - `double dist = UniMath.Distance(ship, target, gameObjects);` (LCA path — the ONLY safe cross-frame distance; never UniVec3.Distance or raw ToDouble3()).
       - D-08 disengage: `if (dist < target.SOIMeters) { DisengageWarp(); return; }`.
       - D-06 speed: `double warpSpeed = System.Math.Min(dist / _selectedTravelTimeSec, _warpMaxSpeed);` — recompute `dist` every frame; NEVER decrement `_selectedTravelTimeSec` (Pitfall 1 — the deceleration curve comes from dist shrinking, not time running out).
       - D-03 auto-orient: `Double3 relMetres = UniMath.RelativeMetres(ship, target, gameObjects);` guard `if (relMetres.Magnitude() > 1e-3)`; build the desired Basis with `-Z` aligned to the normalized direction toward the target, using `_shipBasis.Y` as the up reference; guard the `right` cross product `if (right.LengthSquared() < 1e-6f) right = _shipBasis.X;` (Pitfall 4 degenerate guard); Slerp via Quaternion: `currentQuat.Slerp(desiredQuat, Mathf.Clamp((float)(_warpOrientRate * delta), 0f, 1f))` and write back `_shipBasis = new Basis(lerpedQuat).Orthonormalized();`.
       - Write speed: `_easedSpeed = warpSpeed; CurrentSpeed = _easedSpeed;` so ApplyMotion (with its existing IsFinite + epsilon guards) moves the ship forward along `-_shipBasis.Z`. SOI transitions during this motion are handled automatically by GameWorld.TranslatePos (no new code).
       - Add a `public bool IsWarping => _warpState == WarpState.Warping;` read accessor for the HUD/Plan-02 (cosmetic "WARP" label).
    Guard NaN/Infinity: if `!double.IsFinite(warpSpeed)` after the Min, `DisengageWarp(); return;` (V5 Tampering mitigation).
  </action>
  <verify>
    <automated>cd "C:/Users/FSchubert/workspace/misc/godot/Projects/EcoSpace" && dotnet build EcoSpace.csproj -v quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FlightController.cs `_Process` body is a `switch (_warpState)` with Manual/Confirming/Warping cases
    - The panel gate reads `if (IsPanelOpen && _warpState == WarpState.Manual) return;`
    - FlightController.cs contains `public void EngageWarp(double travelTimeSec)` that sets `_warpState = WarpState.Warping` and guards travelTimeSec with `System.Math.Max(1.0, travelTimeSec)`
    - FlightController.cs contains `public void DisengageWarp()` that sets `_warpState = WarpState.Manual` and does NOT zero CurrentSpeed (no hard stop)
    - FlightController.cs contains `private void _WarpProcess(double delta)` containing `UniMath.Distance(ship, target, gameObjects)` and `if (dist < target.SOIMeters)` disengage and `dist / _selectedTravelTimeSec` speed and `System.Math.Min(..., _warpMaxSpeed)`
    - _WarpProcess uses `UniMath.RelativeMetres` for the orient direction and a Quaternion.Slerp into _shipBasis with a `LengthSquared() < 1e-6f` degenerate guard
    - `_selectedTravelTimeSec` is NEVER decremented anywhere in FlightController (grep finds no `_selectedTravelTimeSec -=` or `_selectedTravelTimeSec -` decrement)
    - FlightController.cs contains `private Basis _cameraOffset` and `private void UpdateLookAround(double delta)`; the camera write is `_camera.Basis = (_shipBasis * _cameraOffset).Orthonormalized();`
    - `_cameraOffset` is `.Orthonormalized()` on every frame it is mutated
    - FlightController.cs contains `public bool IsWarping => _warpState == WarpState.Warping;`
    - `dotnet build EcoSpace.csproj` succeeds with 0 errors and 0 warnings
  </acceptance_criteria>
  <done>Build is clean; warp speed = dist/time with WarpMaxSpeed cap, auto-orient via Slerp, SOI disengage; look-around accumulates into _cameraOffset and eases back; manual path unchanged except look-around + cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor export → flight math | ManualMaxSpeed / WarpMaxSpeed / WarpOrientRate values flow into per-frame speed and orientation; bad values (negative, NaN) could corrupt motion |
| warp screen → FlightController | EngageWarp(travelTimeSec) crosses from UI into the flight tick; travelTimeSec is a divisor |
| world state → warp math | UniMath.Distance / target.SOIMeters drive disengage + speed; degenerate geometry could produce NaN/Infinity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | EngageWarp travelTimeSec divisor | mitigate | `_selectedTravelTimeSec = System.Math.Max(1.0, travelTimeSec)` — never 0/negative, blocks div-by-zero in dist/time |
| T-07-02 | Tampering | _WarpProcess warpSpeed | mitigate | `System.Math.Min(dist/time, _warpMaxSpeed)` cap + `if (!double.IsFinite(warpSpeed)) DisengageWarp()`; existing `double.IsFinite(CurrentSpeed)` guard in ApplyMotion is last-resort |
| T-07-03 | Tampering | auto-orient Basis Slerp | mitigate | `relMetres.Magnitude() > 1e-3` guard + `right.LengthSquared() < 1e-6f` degenerate-cross guard (Pitfall 4); Orthonormalized() on _shipBasis |
| T-07-04 | Tampering | _cameraOffset skew accumulation | mitigate | `.Orthonormalized()` every mutated frame (existing FlightController pattern, Pitfall 2) |
| T-07-05 | Tampering | editor exports (ManualMaxSpeed/WarpMaxSpeed/WarpOrientRate) | mitigate | `System.Math.Max(0.0, value)` setter guard (existing export pattern) blocks negative + NaN |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable); local single-player game, no network |

ASVS L1 note: local single-player Godot game, no network/auth/session/crypto surface. The only live attack surface is input-handling correctness and state-machine invariants — covered by V5 Input Validation guards above.
</threat_model>

<verification>
- `dotnet build EcoSpace.csproj` succeeds with 0 errors, 0 warnings after each code task.
- `grep -v '^#' project.godot | grep -c 'warp_engage\|look_around'` returns 2.
- No `_selectedTravelTimeSec` decrement anywhere (Pitfall 1 guard).
- Manual behavior unchanged except: manual speed capped at 1e6 m/s and Left Alt enables look-around.
- In-game play-test (deferred to Plan 02's checkpoint, which exercises the full warp loop end-to-end including this plan's state machine).
</verification>

<success_criteria>
- WarpState machine (Manual/Confirming/Warping) drives _Process.
- ManualMaxSpeed (1e6 m/s) caps all manual throttle; tier ceiling/proximity damp now autopilot-only.
- EngageWarp/DisengageWarp public API exists; _WarpProcess flies the ship at dist/time speed (WarpMaxSpeed-capped), auto-orients via Slerp, auto-disengages inside target.SOIMeters.
- Look-around (Left Alt) accumulates into _cameraOffset without changing ship heading, eases back over ~0.3 s; available in manual and warp.
- warp_engage (J) and look_around (Left Alt) InputMap actions exist in project.godot.
- Build clean.
</success_criteria>

<output>
Create `.planning/phases/07-autopilot-warp-drive/07-01-SUMMARY.md` when done.
</output>
