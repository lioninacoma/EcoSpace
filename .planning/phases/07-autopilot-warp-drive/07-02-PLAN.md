---
phase: 07-autopilot-warp-drive
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - Scripts/Hud/WarpConfirmationScreen.cs
  - Main.tscn
autonomous: false
requirements: [WARP-01, WARP-04, WARP-05]
must_haves:
  truths:
    - "Pressing J with an active target opens a phosphor-green warp confirmation screen showing target name, distance, travel-time input, and computed warp speed"
    - "Pressing J with NO active target does nothing (no screen opens)"
    - "Adjusting the travel-time slider updates the displayed warp speed live"
    - "Enter engages warp (ship flies to target); Esc or J cancels and resumes manual flight"
    - "The screen never mutates GameObjects/LocalPos/ChildIndices (read-only, D-53)"
  artifacts:
    - path: "Scripts/Hud/WarpConfirmationScreen.cs"
      provides: "Warp confirmation Control panel — follows TargetSelectorPanel pattern; calls FlightController.EngageWarp on Enter"
      contains: "class WarpConfirmationScreen"
    - path: "Main.tscn"
      provides: "WarpConfirmationScreen node under CanvasLayer wired to World/Hud/Flight"
      contains: "WarpConfirmationScreen"
  key_links:
    - from: "WarpConfirmationScreen._UnhandledInput"
      to: "FlightController.EngageWarp"
      via: "ui_accept (Enter) → EngageWarp(_selectedTravelTimeSec)"
      pattern: "EngageWarp"
    - from: "WarpConfirmationScreen"
      to: "Hud.ActiveTargetIndex / FlightController.IsPanelOpen"
      via: "reads target, sets panel gate"
      pattern: "ActiveTargetIndex|IsPanelOpen"
---

<objective>
Build the warp confirmation screen — a phosphor-green Control panel (mirroring TargetSelectorPanel) that opens on J when a target is set, lets the player pick a travel time, shows the computed warp speed, and engages warp on Enter via the `EngageWarp` API from Plan 01. Wire it into Main.tscn and run the full-phase play-test that exercises the complete warp loop end-to-end.

Purpose: The warp screen is the only player-facing entry to autopilot. It owns the travel-time input and the engage/cancel decision; the flight logic (Plan 01) owns the physics.
Output: `Scripts/Hud/WarpConfirmationScreen.cs` (new), `WarpConfirmationScreen` node in Main.tscn wired to World/Hud/Flight, and a play-test confirmation of the whole Phase-7 feature set.
</objective>

<execution_context>
@C:/Users/FSchubert/workspace/misc/godot/Projects/EcoSpace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/FSchubert/workspace/misc/godot/Projects/EcoSpace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-autopilot-warp-drive/07-CONTEXT.md
@.planning/phases/07-autopilot-warp-drive/07-RESEARCH.md
@.planning/phases/07-autopilot-warp-drive/07-PATTERNS.md
@Scripts/Hud/TargetSelectorPanel.cs
@Scripts/Hud/Hud.cs
@Scripts/Math/UniMath.cs
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift/source-grounding verification — they are NEW):

**Scripts/Hud/WarpConfirmationScreen.cs (new file, namespace Hud):**
- `public partial class WarpConfirmationScreen : Control`
- Exports: `WorldPath`, `HudPath`, `FlightPath` (NodePath), `PhosphorGreen` (Color)
- Private refs: `_world` (TestSetup), `_hud` (Hud), `_flight` (Flight.FlightController), `_vbox` (VBoxContainer), `_timeSlider` (HSlider), and `_targetLabel`/`_distLabel`/`_timeLabel`/`_speedLabel` (Label)
- `private double _selectedTravelTimeSec = 120.0`
- Methods: `OpenPanel()`, `ClosePanel()`, `RefreshDisplay()`, `AddRow(...)` (copied helper)

**Main.tscn:**
- A `WarpConfirmationScreen` node under `CanvasLayer` with script `res://Scripts/Hud/WarpConfirmationScreen.cs` and wired NodePath exports

Consumes from Plan 01 (already created, NOT produced here): `FlightController.EngageWarp`, `FlightController.IsPanelOpen`, `FlightController.WarpMaxSpeed`, `FlightController.IsWarping`, the `warp_engage` InputMap action.
Consumes existing: `Hud.ActiveTargetIndex`, `Hud.FormatSpeed`, `Hud.FormatDistance`, `TestSetup.ShipIndex`, `UniMath.Distance`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create WarpConfirmationScreen.cs following the TargetSelectorPanel pattern</name>
  <files>Scripts/Hud/WarpConfirmationScreen.cs</files>
  <read_first>
    - Scripts/Hud/TargetSelectorPanel.cs — read in full. This is the structural template: `_Ready` node resolution (NodePath with FindChild fallback for World/Hud/Flight), OpenPanel/ClosePanel (Visible + MouseFilter + Input.MouseMode + `_flight.IsPanelOpen`), `_UnhandledInput` toggle structure with `GetViewport().SetInputAsHandled()`, the AddRow phosphor-green Label helper, and the `[Export] Color PhosphorGreen` default `new Color(0.1f, 1.0f, 0.3f)`.
    - Scripts/Hud/Hud.cs — `ActiveTargetIndex` (line ~91), `FormatSpeed` (line ~464), `FormatDistance` (line ~478) signatures. Note Hud type is `Hud.Hud`.
    - Scripts/Math/UniMath.cs — `Distance(a, b, objs)` signature.
    - .planning/phases/07-autopilot-warp-drive/07-PATTERNS.md §"WarpConfirmationScreen.cs" — every code anchor (file header, exports, refs, _Ready, OpenPanel/ClosePanel, _UnhandledInput, AddRow, distance display, HSlider pattern).
    - .planning/phases/07-autopilot-warp-drive/07-CONTEXT.md — D-02, D-04, D-05, D-15, D-16, D-17; Claude's Discretion (layout/slider UX, m/s scientific-notation speed display).
    - .planning/phases/07-autopilot-warp-drive/07-RESEARCH.md — Pattern 6, Code Examples (HSlider, FormatSpeed), Assumptions A3/A4 (HSlider initializer + ValueChanged-during-_Ready).
  </read_first>
  <action>
    Create `Scripts/Hud/WarpConfirmationScreen.cs`, `namespace Hud`, `public partial class WarpConfirmationScreen : Control`. Mirror TargetSelectorPanel exactly for structure; this panel is read-only (D-53 — NEVER write GameObjects/LocalPos/ChildIndices).
    1. Exports: `WorldPath`, `HudPath`, `FlightPath` (NodePath) and `PhosphorGreen` (Color, default `new Color(0.1f, 1.0f, 0.3f)`).
    2. Private refs `_world` (TestSetup), `_hud` (Hud), `_flight` (Flight.FlightController), `_vbox` (VBoxContainer). Runtime field `private double _selectedTravelTimeSec = 120.0;` (D-17, 2-min default, not persisted). UI children: `_timeSlider` (HSlider) and four Labels (`_targetLabel`, `_distLabel`, `_timeLabel`, `_speedLabel`).
    3. `_Ready`: resolve the three NodePaths with the FindChild fallback pattern from TargetSelectorPanel (Hud → FindChild("Hud"), World → FindChild("Main") as TestSetup, Flight → FindChild("FlightController")). Build a `VBoxContainer`, add it, then build the rows: a title row ("WARP DRIVE"), `_targetLabel`, `_distLabel`, `_timeLabel`, the `_timeSlider`, `_speedLabel`, and a hint row ("ENTER engage   ESC/J cancel"). Style all Labels phosphor-green via the AddRow helper (copy it from TargetSelectorPanel). Configure the HSlider: `MinValue=1.0, MaxValue=60.0, Step=0.5, Value=2.0` (minutes; D-05/D-17). Subscribe `_timeSlider.ValueChanged += value => { _selectedTravelTimeSec = value * 60.0; RefreshDisplay(); };`. Per Assumption A4, guard RefreshDisplay against being called before refs/labels exist (null-check `_speedLabel`/`_hud`) so the ValueChanged that may fire during initialization is safe. Start `Visible = false; MouseFilter = MouseFilterEnum.Ignore;`.
    4. `OpenPanel()` / `ClosePanel()`: copy the TargetSelectorPanel lifecycle exactly — Open sets Visible=true, MouseFilter=Stop, Input.MouseMode=Visible, `_flight.IsPanelOpen = true`, then RefreshDisplay(); Close sets Visible=false, MouseFilter=Ignore, Input.MouseMode=Captured, `_flight.IsPanelOpen = false`. (D-04: IsPanelOpen gates flight while confirming; Plan-01 _Process treats this as the Confirming state via `IsPanelOpen && _warpState == Manual`.)
    5. `RefreshDisplay()`: read `int tgt = _hud?.ActiveTargetIndex ?? -1;` and the ship via `_world.ShipIndex`; bounds-guard both with `(uint)idx < (uint)gameObjects.Count`. If either is null, show a "NO TARGET" state and return. Otherwise: `_targetLabel.Text` = target name; `double dist = UniMath.Distance(ship, target, gameObjects);` → `_distLabel.Text = "DIST  " + Hud.FormatDistance(dist);`; `_timeLabel.Text` = the selected minutes; `double warpSpeed = System.Math.Min(dist / _selectedTravelTimeSec, _flight?.WarpMaxSpeed ?? 2e20);` → `_speedLabel.Text = "WARP  " + Hud.FormatSpeed(warpSpeed);` (D-15; reuse FormatSpeed per RESEARCH open-question-3 recommendation). All distances via UniMath.Distance ONLY (LCA path; never UniVec3.Distance / ToDouble3()).
    6. `_UnhandledInput(InputEvent @event)`:
       - `if (@event.IsActionPressed("warp_engage"))`: if Visible → ClosePanel() (cancel, D-15); else if `_hud?.ActiveTargetIndex >= 0` → OpenPanel() (D-02 no-op when no target). `GetViewport().SetInputAsHandled(); return;`
       - `if (!Visible) return;`
       - `if (@event.IsActionPressed("ui_cancel"))` (Esc) → ClosePanel(); SetInputAsHandled().
       - `else if (@event.IsActionPressed("ui_accept"))` (Enter) → ClosePanel() FIRST (so IsPanelOpen is false before warp starts — Pitfall 3), THEN `_flight?.EngageWarp(_selectedTravelTimeSec);` SetInputAsHandled(). (Order matters: close before engage.)
    Do NOT add a Tab/`toggle_target_panel` handler — that is TargetSelectorPanel's action; this panel only responds to warp_engage/Esc/Enter while open.
  </action>
  <verify>
    <automated>cd "C:/Users/FSchubert/workspace/misc/godot/Projects/EcoSpace" && dotnet build EcoSpace.csproj -v quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Scripts/Hud/WarpConfirmationScreen.cs exists with `namespace Hud` and `public partial class WarpConfirmationScreen : Control`
    - It declares `[Export] NodePath WorldPath/HudPath/FlightPath` and `[Export] Color PhosphorGreen`
    - `_Ready` builds an HSlider with MinValue=1.0, MaxValue=60.0, Step=0.5, Value=2.0 and subscribes ValueChanged to set `_selectedTravelTimeSec = value * 60.0`
    - `OpenPanel` sets `_flight.IsPanelOpen = true`; `ClosePanel` sets `_flight.IsPanelOpen = false`
    - `_UnhandledInput` handles `warp_engage` (toggle, no-op open when ActiveTargetIndex < 0), `ui_cancel` (close), `ui_accept` (ClosePanel then `_flight.EngageWarp(_selectedTravelTimeSec)`)
    - Enter path calls ClosePanel() BEFORE EngageWarp()
    - RefreshDisplay computes distance via `UniMath.Distance` and warp speed via `dist / _selectedTravelTimeSec` capped by WarpMaxSpeed, formatted with `Hud.FormatSpeed`/`Hud.FormatDistance`
    - The class never assigns to GameObjects[..], .LocalPos, or .ChildIndices (grep finds no such writes)
    - `dotnet build EcoSpace.csproj` succeeds with 0 errors, 0 warnings
  </acceptance_criteria>
  <done>Build is clean; the panel opens on J (with a target), shows target/distance/time/speed, engages warp on Enter via EngageWarp, cancels on Esc/J, and is read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Wire WarpConfirmationScreen into Main.tscn under CanvasLayer</name>
  <files>Main.tscn</files>
  <read_first>
    - Main.tscn — read the CanvasLayer subtree and the existing TargetSelectorPanel node entry: its node declaration, its `script` ext_resource, and its wired NodePath exports (WorldPath/HudPath/FlightPath). The new node must mirror this exactly.
    - .planning/phases/07-autopilot-warp-drive/07-PATTERNS.md §"Main.tscn" — the three required steps.
    - .planning/phases/07-autopilot-warp-drive/07-CONTEXT.md — D-16 (Control node under CanvasLayer, sibling of TargetSelectorPanel).
  </read_first>
  <action>
    Add a `WarpConfirmationScreen` node to Main.tscn as a sibling of `TargetSelectorPanel` under `CanvasLayer`.
    1. Add an `[ext_resource type="Script" path="res://Scripts/Hud/WarpConfirmationScreen.cs"]` entry (new uid) if not already present.
    2. Add the node: type `Control`, name `WarpConfirmationScreen`, parent the same CanvasLayer that holds TargetSelectorPanel, attach the script ext_resource.
    3. Wire the NodePath exports `WorldPath`, `HudPath`, `FlightPath` to the SAME target nodes TargetSelectorPanel points at (copy its export values verbatim). Optionally set anchors so the panel sits centered/visible; layout exact rect is Claude's Discretion (retro style, matching TargetSelectorPanel placement conventions).
    Prefer editing the .tscn text directly (mirroring the TargetSelectorPanel block) so the wiring is deterministic; if any NodePath/uid detail is ambiguous, open Main.tscn in the Godot editor, add the node + script, and set the three NodePaths there.
  </action>
  <verify>
    <automated>grep -c 'WarpConfirmationScreen' Main.tscn</automated>
  </verify>
  <acceptance_criteria>
    - Main.tscn contains a `WarpConfirmationScreen` node under the same CanvasLayer as TargetSelectorPanel
    - The node has script `res://Scripts/Hud/WarpConfirmationScreen.cs`
    - The node's WorldPath/HudPath/FlightPath exports point at the same nodes as TargetSelectorPanel's exports
    - `grep -c 'WarpConfirmationScreen' Main.tscn` returns >= 2 (ext_resource + node)
    - The project opens in the Godot editor with no missing-script or unresolved-NodePath errors
  </acceptance_criteria>
  <done>WarpConfirmationScreen is present and wired in Main.tscn; the scene loads cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full-phase play-test — manual cap, look-around, and warp loop</name>
  <what-built>
    Phase 7 complete: manual speed capped at 1,000 km/s; Left Alt look-around in flight and warp; J opens the warp confirmation screen; Enter flies the ship on autopilot to the selected target and auto-disengages on SOI arrival.
  </what-built>
  <read_first>
    - .planning/phases/07-autopilot-warp-drive/07-CONTEXT.md — the full decision set, to confirm observed behavior matches D-01 through D-19.
  </read_first>
  <how-to-verify>
    Launch the project in the Godot editor (F5) and verify:
    1. MANUAL CAP: Full-throttle in open space — speed tops out around 1,000 km/s (HUD shows ~km/s scale, NOT ly/s). In-system near a planet/star it is still slower (tier/proximity damp). No jump to intergalactic speed in manual flight.
    2. LOOK-AROUND (normal flight): Hold Left Alt and move the mouse — the view pans but the ship does NOT change heading (crosshair/heading holds). Release Alt — the view eases back to ship heading over ~0.3 s. Throttle still works while looking.
    3. NO-TARGET GUARD: With no target selected, press J — nothing happens (no screen).
    4. WARP OPEN: Select a target via the Tab tree selector, then press J — the phosphor-green warp screen opens showing target name, distance, a travel-time slider (default 2 min), and a computed warp speed. Drag the slider — the speed updates live (lower time → higher speed).
    5. WARP ENGAGE: Press Enter — the screen closes, the ship auto-orients toward the target and accelerates; speed DECREASES as it closes in (deceleration curve, never increasing). Cross at least one SOI boundary mid-warp with no crash/pop. On entering the target's SOI the ship auto-disengages and speed eases down to manual scale.
    6. WARP LOOK-AROUND: During warp, hold Left Alt — you can look around while the ship stays on rails.
    7. WARP CANCEL: Engage warp again, then press Esc or J mid-warp — warp disengages and manual control resumes (speed eases down, no hard stop).
    8. INTERGALACTIC: Target another galaxy, warp with ~2 min — the crossing completes in roughly the selected time, FTL-equivalent speed shown.
  </how-to-verify>
  <resume-signal>Type "approved" if all eight behaviors hold, or describe what failed (which step, observed vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player input → warp screen | J / Enter / Esc / slider drag cross from raw input into panel state and the EngageWarp call |
| warp screen → FlightController | EngageWarp(_selectedTravelTimeSec) and IsPanelOpen cross from UI into the flight tick |
| world state → screen display | ActiveTargetIndex / UniMath.Distance read for display; must not mutate sim state (D-53) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | RefreshDisplay index access | mitigate | `(uint)idx < (uint)gameObjects.Count` bounds guard on ship + target; null-check before use (TargetSelectorPanel pattern) |
| T-07-07 | Tampering | warp speed display divisor | mitigate | slider MinValue=1.0 min (never 0 minutes); display uses same `Min(dist/time, WarpMaxSpeed)` cap as the engine |
| T-07-08 | Repudiation | read-only contract (D-53) | mitigate | Panel never writes GameObjects/LocalPos/ChildIndices; selection/engage flow only through Hud.SetTargetIndex (existing) and FlightController.EngageWarp |
| T-07-09 | Tampering | IsPanelOpen state on engage | mitigate | ClosePanel() (sets IsPanelOpen=false) called BEFORE EngageWarp() so warp _Process is not gated (Pitfall 3) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable); local single-player game |

ASVS L1 note: no network/auth/session/crypto. Attack surface is UI input handling + the read-only sim contract — covered above.
</threat_model>

<verification>
- `dotnet build EcoSpace.csproj` clean after Task 1.
- `grep -c 'WarpConfirmationScreen' Main.tscn` >= 2 after Task 2.
- WarpConfirmationScreen never mutates sim state (grep: no GameObjects[..]= / .LocalPos = / .ChildIndices writes).
- Play-test checkpoint (Task 3) confirms all eight Phase-7 behaviors end-to-end.
</verification>

<success_criteria>
- J opens a phosphor-green warp confirmation screen when a target is set; no-op when not.
- Travel-time slider (1–60 min, default 2) updates the computed warp speed live.
- Enter engages warp (ClosePanel then EngageWarp); Esc/J cancels.
- Ship auto-orients, flies dist/time speed, decelerates into the target, auto-disengages at SOI, eases to manual speed.
- Look-around works in both manual and warp; manual speed capped at km/s.
- Read-only contract preserved (D-53). Build clean; play-test approved.
</success_criteria>

<output>
Create `.planning/phases/07-autopilot-warp-drive/07-02-SUMMARY.md` when done.
</output>
