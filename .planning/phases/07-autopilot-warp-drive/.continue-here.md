---
context: phase
phase: 07-autopilot-warp-drive
task: 3
total_tasks: 3
status: play-test-approved
last_updated: 2026-06-22T12:15:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: HSlider-focus-capture — HSlider accepts keyboard focus by default; `ui_accept` (Enter) is consumed by the slider before reaching `_UnhandledInput`. Always set `FocusMode = FocusModeEnum.None` on any HSlider inside a `_UnhandledInput`-driven panel. Structural mitigation: fix is in commit 7569a2e — verify `FocusModeEnum.None` is still present in `WarpConfirmationScreen.cs` before touching that file.
- [ ] CONSTRAINT: look-around-cursor-bleed — `_cursor` accumulated in `_Input` regardless of whether `look_around` (Left Alt) was held. Fix: separate `_rawMouseDelta` field for camera pan; `_cursor` gated on `!look_around`; zeroed each look-around frame. Structural mitigation: fix in commit 7569a2e — do NOT collapse the dual-field pattern back to a single accumulator.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| HSlider-focus-capture | HSlider's default FocusMode captures `ui_accept` (Enter), silently swallowing the keypress before `_UnhandledInput` fires. | blocking | Always set `FocusMode = FocusModeEnum.None` on HSlider in any panel whose engagement is driven by `_UnhandledInput`. |
| look-around-cursor-bleed | `_cursor` was shared between look-around camera offset and ship steering. Holding Alt panned using the accumulated steering cursor, then releasing Alt applied that delta as a steering impulse. | blocking | Use dedicated `_rawMouseDelta` for look-around; gate `_cursor` accumulation on `!look_around`; zero `_cursor` each look-around frame. |

<current_state>
Phase 07 (autopilot-warp-drive) is fully implemented, all bugs fixed, play-test approved.
All changes committed to main. No dirty working tree.

Ready for the two remaining post-play-test gates:
1. Code review gate: /gsd-code-review 7
2. Phase verification: spawn gsd-verifier for phase 07
Then mark phase 07 complete in STATE.md and ROADMAP.md.
</current_state>

<completed_work>
Plan 01 (all 3 tasks) — COMPLETE (previous session):
- Task 1: warp_engage (J) and look_around (Left Alt) added to project.godot InputMap
- Task 2: WarpState enum + ManualMaxSpeed/WarpMaxSpeed/WarpOrientRate exports + manual speed cap
- Task 3: _Process state switch, EngageWarp/DisengageWarp, _WarpProcess, UpdateLookAround, IsWarping

Plan 02 Tasks 1–2 — COMPLETE (previous session):
- Task 1: WarpConfirmationScreen.cs — phosphor-green panel, HSlider (5s–3600s), ClosePanel-before-EngageWarp
- Task 2: WarpConfirmationScreen wired into Main.tscn under CanvasLayer

Bug fixes (7569a2e) — COMPLETE:
- Look-around: _rawMouseDelta + _cursor suppression during Alt hold
- Warp Enter: HSlider FocusMode = None
- Slider: minutes → seconds (5s–3600s, step 5s, default 120s)

Session fixes (5919259):
- Bug 1: Warp orientation — forward.Cross(currentUp) for right-handed basis (was currentUp.Cross(forward) → left-handed → quaternion garbage → ship faced wrong direction + jitter)
- Bug 2: Warp timing — T_int = T_sel / ln(d0/SOI) calibrated in EngageWarp; _WarpProcess uses _warpInternalTimeSec
- Direction precision: UniMath.NormalizedDirection uses ToDouble3Units (unit-space normalization)
- project.godot: look_around keycode 4194326→4194328 (Ctrl→Alt, corrected by editor)

Math improvements:
- UniVec3.ToDouble3Units() = (Double3)Units + Offset/Scale — unit-space position (exact int64 + fractional)
- UniMath.NormalizedDirection(from, to, objs) → Double3 using ToDouble3Units — precise at any scale

Plan 02 Task 3 (play-test) — APPROVED:
- All 8 behaviors verified in-game
- Tech debt P3 filed: warp-timing-inaccuracy-under-speed-cap (timing off when dist/T_int exceeds WarpMaxSpeed cap)
</completed_work>

<remaining_work>
Post-play-test gates (not yet run):
- Code review gate: /gsd-code-review 7
- Phase verification: spawn gsd-verifier agent for phase 07
- Update STATE.md + ROADMAP.md to mark phase 07 complete
</remaining_work>

<decisions_made>
- T_int = T_sel / ln(d0/SOI) accepted as good-enough timing formula (P3 tech debt for speed-cap inaccuracy)
- NormalizedDirection uses ToDouble3Units (unit-space) rather than ToDouble3 (metres) — scale cancels, int64 exact
- look_around bound to Left Alt (physical_keycode 4194328); HSlider FocusMode = None to prevent Enter capture
- Travel time slider in seconds (5s–3600s, default 120s) — user preference for short in-system warps
</decisions_made>

<blockers>
None. Build is clean (0 errors, 0 warnings). Working tree is clean.
</blockers>

## Required Reading (in order)
1. `Scripts/Flight/FlightController.cs` — dual-field _rawMouseDelta + _cursor pattern before any edits
2. `Scripts/Math/UniMath.cs` — NormalizedDirection + ToDouble3Units approach before touching direction math
3. `.planning/todos/pending/warp-timing-inaccuracy-under-speed-cap.md` — P3 tech debt context

## Critical Anti-Patterns (do NOT repeat these)
- Left-handed basis in Quaternion: `currentUp.Cross(forward)` = LEFT vector → Quaternion(leftHandedBasis) ≈ identity → ship faces -Z always. Use `forward.Cross(currentUp)` for right.
- Float-cast of raw metres at large scale: `(float)2.4e22` loses ~3e15 m/axis; normalize in ToDouble3Units first.

## Infrastructure State
- Build: clean (0 errors, 0 warnings, dotnet build EcoSpace.csproj)
- Git: main branch, all changes committed (HEAD = 02c4238), no dirty tree
- Godot editor: not required for remaining gates

<context>
Phase 07 is one code review + one verification agent away from being marked complete.
Both gates are routine — the implementation is solid and play-test approved. The only
known wart is the P3 tech debt (timing inaccuracy under WarpMaxSpeed cap) which is
intentionally deferred.
</context>

<next_action>
Run /gsd-code-review 7, then spawn gsd-verifier for phase 07, then mark phase complete.
</next_action>
