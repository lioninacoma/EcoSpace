---
type: quick
slug: delete-phase5-add-rendering-overhaul
created: 2026-06-19
mode: quick
---

# Quick Task — Restructure Phase 5: delete the findability bundle, insert a Rendering Overhaul

**One-liner:** Replace the abandoned, over-bundled Phase 5 ("Outer-tier body findability
& galaxy visibility") with a new foundational Phase 5 ("Rendering Overhaul" — full
rewrite), and keep the three render debts as standalone tracked todos to be solved
*after* the overhaul, individually — not all in one phase.

## Why

Phase 5 tried to fix three distinct render problems at once and dead-ended on the
fragile StarPointRenderer (see HANDOFF.json / `.continue-here.md`). User decision:
do a foundational rendering rewrite first, then tackle the debts one at a time on top
of the new layer.

## Decisions (user, 2026-06-19)

- **Overhaul scope:** Full rendering rewrite — unify WorldRenderer + post-process
  (PostProcessRenderer/dithering) + body-lighting (body_lit) into one coherent
  rendering layer. Largest scope; likely multi-wave.
- **Old Phase 5 artifacts:** Delete (`05-outer-tier-body-findability-galaxy-visibility/`).
  Anti-patterns survive in HANDOFF.json + the kept galaxy-tilt commit (c98f56c).

## Tasks

1. Delete `.planning/phases/05-outer-tier-body-findability-galaxy-visibility/`.
2. ROADMAP.md: remove old Phase 5; add new Phase 5 "Rendering Overhaul" (goal, scope,
   success criteria, debt relationship); update phase list + progress table.
3. Confirm/refresh the three render debts as standalone todos (un-bundle the fix-order
   framing; note the disc-tilt one was largely resolved by the kept c98f56c work).
4. Scaffold `.planning/phases/05-rendering-overhaul/`.
5. Update STATE.md (current phase, roadmap evolution, pending todos, quick-task table)
   and HANDOFF.json (point at the new, unplanned Phase 5).
6. Atomic commits on main.

## Out of scope

- Authoring the actual rendering-overhaul PLAN.md (that's `/gsd-discuss-phase 5` /
  `/gsd-plan-phase 5` later).
- Any code changes to the renderer.
</content>
